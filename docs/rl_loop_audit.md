# RL Loop Audit — plan and standing record

**Status: LIVING DOCUMENT.** Update the status cells in place as checks run.
This is both the plan (what to verify, in what order, with which exact command)
and the record (what has actually been verified, when, with what evidence).

`docs/experiment_ledger.md` owns *experiments* — hypotheses, yardsticks,
verdicts. This document owns *invariants* — things that must be true for any
experiment's numbers to mean anything. A ledger verdict read off a pipeline that
fails an invariant here is not a verdict.

---

## 1. Why this exists: the recurring bug class

Nearly every production-affecting defect found in this pipeline has the same
shape: **a number that does not mean what its name says**, with nothing
asserting the two agree. Not crashes — silent semantic drift.

| defect | name said | actually was |
|---|---|---|
| `train_views_per_position` (#225) | views per ingested position | per *matching* position; true reuse 0.46 vs 2.5 configured |
| `selfplay_fraction` (#224) | share of games | share of *slots*; realized completed mix 83.5% vs 0.35 |
| `opening_fen_dole_max_fraction` | cap | `0` means **uncapped**, not "none allowed" |
| `sf_nodes: 5000` | label depth | a PID *floor*; labels use ~698k |
| `gate_passed: 1` | gate passed | `gate_games: 0` — no gate ran (metric DELETED, PR #285; see L4) |
| `holdout_generation` | ruler identity | inert (always 0); the ruler rotates anyway |
| `matching_games` | games generated | current-model games only |

**Corollary for auditing:** reading the code is not enough, because the code is
usually correct *locally*. The bug lives in the composition — between the config
that sets a number, the stage that resolves it, and the counter that reports it.
**Every check below must measure the realized value on the live run**, not
assert the code looks right.

---

## 2. Method rules — how not to fool ourselves

These are written from mistakes made *while doing this audit*, not hypotheticals.

1. **`scripts/audit_realized_config.py` is the authority on configured-vs-realized.**
   When a hand-rolled number disagrees with it, assume the hand-rolled one is
   wrong first. On 2026-07-26 an ad-hoc sum "found" a dole-cap violation, a
   mechanism was invented, and a fix was written, tested and committed before
   the premise was rechecked. The audit tool had said `ok` the whole time. The
   cap was fine.

2. **`outcome_stats` keys are NESTED. Never sum by substring.**
   It is a pipe-delimited string (not JSON — `json.loads` raises, and a
   `try/except` silently yields `{}` and zeros). `opening_fenlist_games` is a
   total; `selfplay_fenlist_games` repeats that subset; `selfplay_fenlist_stm_w/l`
   splits it again. Summing everything matching `fenlist` triple-counts. Prefer
   the `*_games` totals. Use `scripts/loop_health.py::parse_outcome_stats`.

3. **A mechanism must fit the magnitude, not just the direction.**
   The invented dole bug predicted exactly 4x; the (bad) data showed 2-3x. That
   mismatch was disconfirming evidence and was glossed over. If the proposed
   cause does not produce the observed number, it is not the cause.

4. **A median over a window hides a regime change.** `audit_realized_config.py`
   medians by design. When a knob changed mid-window, slice the window or read
   per-iteration rows.

5. **Never pick a yardstick that only exists when the bug fires.** The PR #224
   readout asked for `abandoned/started` from a log line that is only emitted at
   teardown — in the success case it emits nothing, so the ratio is undefined and
   absence-of-line is not a measurement.

6. **Frozen rulers are not strength rulers.** `value_regret`, `audit_targets`
   and the panels can all move opposite to real play
   (`offline_distillation_value_trap`, the 07-21 RULER MIRAGE row). Only a paired
   arena decides strength.

7. **Verify identity before believing a lopsided arena.** Dump
   `torch.load(ckpt)["arch"]` for both sides and confirm `policy_encoding`,
   `input_extra_features`, `input_history_encoding`, `history_rep_fix` match.

8. **Test a new parser against real output before trusting its file.** The
   ratchet's Elo regex was written against an assumed format and silently
   corrupts the CSV on positive Elo (see L1). A parser that is only exercised by
   the happy path you imagined is not verified. Run it against a captured log.

9. **Never edit a shell script while it is running.** Bash reads scripts
   incrementally by byte offset, so an edit that shifts offsets can make the
   running instance execute garbage. Kill and relaunch, or wait for it to exit.
   Two corollaries, both hit on 2026-07-26. **(a)** *Atomic rename is the safe
   install, and that safety means the running instance keeps the OLD file.*
   The ratchet was raised to `CONC=16` while its one-shot was mid-flight; the
   run correctly finished at the `CONC=4` it started with. A config edit is not
   in effect until the next launch — verify against the arena's own log
   (`keep N games active`), never against the file on disk. **(b)** *Run
   long-lived scripts from an immutable copy.* `cp foo.sh .foo.running.sh` and
   launch the copy, so later edits to the source cannot reach the live process.
   The boot512 ladder died silently in its wait loop from exactly this.

10. **Beware `pgrep -f` self-matching.** A pattern passed on the command line
    appears in that command's own `/proc` cmdline, so `pgrep -f` matches the
    watcher itself. An `until ! pgrep -f "..."` loop written inline can never
    exit — one spun for 12h. Put the pattern inside a script file instead.

11. **Price the instrument, not just the reading — and budget it in TIME, not
    in samples.** An observer that runs against live training is a load on the
    thing it measures, and that load is part of its verdict. The daily
    ratchet's first run cost ~23 iterations of training to buy one Elo point
    with a ±50 CI (L6) — it was net-negative, and nothing in its design would
    have said so. Before putting any job on a cadence, measure `timestamp` deltas and `matching_games` inside its window
    against a clean baseline window, and state the exchange rate in the entry.
    Then give it a hard wall-clock deadline and let the CI float: sizing by
    statistical power ("200 games resolves the effect") sets no upper bound on
    cost, and the cost is what the operator actually feels. **Standing budget:
    ~30 min/day of GPU outside training.** sims-1 screening is nearly free;
    sims-32 never is, at any concurrency.

12. **A "realized" value must come from the stage that resolves it, not from an
    input to that stage.** I5 was marked *measured* on the strength of the live
    yaml plus the trial's realized params — but `sf_wdl_frac` is resolved
    per-iteration from `wdl_regret`, so both of those are inputs and the answer
    (0.45, not 0.50) only exists in `progress.csv`. Same shape as J5
    (`params.json`) and J6 (`experiment_state`), and it got past an author who
    had just written both of those rows. Before writing VERIFIED, name the
    artifact the number was read from and ask whether anything downstream can
    still change it.

13. **Summing a `state_dict` double-counts shared parameters.** Weight tying
    lists one tensor under many keys; `sum(v.numel())` counts it once per key.
    Count unique storages (`v.untyped_storage().data_ptr()`). This produced a
    78.8M parameter count for a 63.08M model, and the wrong figure was written
    into CLAUDE.md as a *correction* of the right one (A6).

14. **A diff over the INTERSECTION of two key sets cannot see an absent key.**
    A4 compared the published reco against the yaml on the 64 keys they share
    and reported "63 exact" while `soft_policy_temp` — in the yaml, in no
    manifest, in no worker — sat outside the comparison entirely for five
    months (E13). Any "config X matches config Y" check must run over the
    **union** and report one-sided keys as their own outcome. Corollary from
    the fix: when enumerating what a consumer *should* receive, enumerate the
    **callee's parameters**, not the fields of one dataclass. The manual A7
    pass walked `GameConfig`/`SearchConfig` and found 7 unsourced fields; it
    could not see that `play_batch` takes a whole `diff_focus` group the worker
    never builds, because that gap is not a field of anything it was reading.

---

## 3. The loop, as stages

```
 (A) config resolve -> (B) opening select -> (C) selfplay generate
        -> (D) SF label -> (E) finalize to rows -> (F) upload/inbox
        -> (G) ingest + holdout split -> (H) sample -> (I) train step
        -> (J) checkpoint + best-model -> (K) publish to workers -> back to (C)
                                   \-> (L) evaluation / rulers
```

Each stage below lists its invariants, the exact instrument, and current status.

**Status vocabulary:** `VERIFIED <date>` (measured on the live run) ·
`FAILED <date>` (measured, invariant does not hold) · `OPEN` (never measured) ·
`CODE-ONLY` (argued from source, not measured — weaker, treat as OPEN for
anything load-bearing).

---

## A. Config resolution — yaml → trial → published reco → worker

| # | invariant | instrument | status |
|---|---|---|---|
| A1 | every ratio knob's realized value matches the yaml | `PYTHONPATH=. python3 scripts/audit_realized_config.py --last 20` | **VERIFIED 2026-07-26** — exit 0, all knobs ok |
| A2 | the live yaml validates and reloads (all-or-nothing) | grep `/tmp/chess_training.log` for validator/reject/reload-fail | **VERIFIED 2026-07-26** — clean after 2 edits |
| A3 | `pb2_bounds_*` do not silently OVERRIDE their base key | read `_build_mutation_param_space`; confirm trial `lr` in `progress.csv` | **VERIFIED 2026-07-26** — caught overriding `train.lr`; both now pinned 3e-5 |
| A4 | published reco matches the resolved config | diff `recommended_worker` in `runs/pbt2_small/server/trials/<prefix>_00000/publish/manifest.json` against the flattened yaml | **VERIFIED 2026-07-26** — 64 shared keys, 63 exact. The one difference is intentional: `sf_nodes` published **698289** vs yaml **5000**, because the yaml key is the PID *floor* and the published value is the live `base_nodes` ([[sf_label_nodes_are_not_sf_nodes]]). Any OTHER mismatch here is a finding |
| A5 | live yaml divergence from `main` is known and intentional | `git diff origin/main -- configs/pbt2_small.yaml` | **VERIFIED-DIVERGENT 2026-07-26** — 4 known deltas, see the reconciliation block below. **This is the highest-risk item here: a naive "take main's yaml" at the next restart reverts the LR fix and re-enables seeding.** |

| A6 | the documented parameter count is the number of parameters | count unique storages in the checkpoint's `model` state_dict, not the sum of entry sizes | **FAILED 2026-07-26 — and the wrong number is the one this line was recently "corrected" TO.** CLAUDE.md claims **78.8M params**, with `layer_smolgens` "42.4M of 78.8M (54%)", and explicitly says this replaced "the stale ~63M this line used to claim". The ~63M was right. Measured on `checkpoint_000042`: 496 state_dict entries summing to **78.81M**, but only **63.08M unique storages** — a 15.73M gap. Cause: the 16 `layer_smolgens.N.gen_weight.weight` entries are **one shared tensor** (16 entries, **1 distinct storage**, 1,048,576 params each), so 15 × 1,048,576 = 15,728,640 = exactly the gap. A `state_dict` lists shared parameters once per reference; summing `numel()` over it double-counts weight tying. True figures: **63,084,128 trainable params**, `layer_smolgens` **26.7M = 42.3%** (not 54%). This matters beyond bookkeeping — it is the denominator for params-per-FLOP reasoning, for the `matrix_optimizer_scope` coverage in I12, and for any scaling comparison against BT4 |

| A7 | every worker-affecting yaml key is actually PUBLISHED to the worker and CONSUMED by it | diff the dataclass fields of `SearchConfig` / `TemperatureConfig` / `GameConfig` against `_build_selfplay_configs` and the published reco | **FAILED 2026-07-26 — and this is the gap A4 structurally cannot see.** A4 compares the 64 keys the reco and the yaml *share* and found 63 exact. The defect class here is a key that is **absent** from the reco entirely, so it never enters that comparison. 7 `GameConfig`/`SearchConfig` fields are never sourced from the reco; six are harmless because they happen to equal their dataclass defaults (`fpu_reduction` 1.2, `fpu_at_root` 1.0, `drop_plies` 0, `categorical_bins` 32, `hlgauss_sigma` 0.04, `volatility_source`). **One diverges live: `soft_policy_temp` — see E13.** Fix the instrument as well as the key: A4's diff must be run over the union of the two key sets, not the intersection. **INSTRUMENT FIXED 2026-07-26 (PR #257), invariant STILL FAILING:** `scripts/audit_realized_config.py --reco-diff` now diffs over the union, with two self-invalidating allowlists (`_RECO_SERVER_RESOLVED` = published under another name; `_RECO_WORKER_DEFAULT` = unpublished but pinned equal to the dataclass default, so a yaml edit off the default turns it back into a finding). `soft_policy_temp` is fixed; the six "harmless" fields are now explicitly allowlisted with their defaults asserted against the real dataclasses. The union diff then found a **SEVENTH gap the manual pass missed: the entire `diff_focus` group** (`diff_focus_enabled/q_weight/pol_scale/slope/min`). It is not a `GameConfig`/`SearchConfig` field but a whole `play_batch` config group, so scanning those two dataclasses could not see it: the worker never builds a `DiffFocusConfig` at all and runs the code defaults 6.0/3.5/3.0/0.025 against the yaml's pinned 4.8/3.8/4.0/0.09. Unlike the six, these DIVERGE, so plumbing them is data-affecting and needs its own ledger entry — left open on purpose, pinned by name in `tests/test_reco_coverage.py::_KNOWN_UNPUBLISHED` so an eighth cannot hide behind it. `--reco-diff` exits 1 today; that is the instrument working |

### A5 reconciliation — read this BEFORE any restart

The live tree is never checked out while a run is live, so the live yaml drifts
from `main` by design. As of 2026-07-26 the deltas are:

| key | live | `main` | at the next restart |
|---|---|---|---|
| `lr` | **3e-5** | 0.0003 | **KEEP LIVE.** main's value is the one that cost −494 Elo. Must be changed together with `pb2_bounds_lr` (see A3) |
| `pb2_bounds_lr` | **[3e-5, 3e-5]** | [0.0003, …] | **KEEP LIVE** — this key OVERRIDES `train.lr`, it does not bound it |
| `opening_fen_dole_per_iter` | **0** | 1 | keep live until the no-seed readout closes; restoring costs a session restart (see C6) |
| `opening_fen_list_path` | `retire_32` | `retire_250` | **KEEP LIVE.** The monitor rewrites this every iteration; main's value is always stale |
| `gumbel_c_scale` | *absent* | **0.1** | **TAKE MAIN'S.** PR #249; value-identical to the resolved default so it is a no-op now, but the live yaml should gain the pin |
| `diff_focus_*` | 4.8/3.8/4.0/0.09 → **6.0/3.5/3.0/0.025** | 4.8/3.8/4.0/0.09 | **TAKE LIVE'S.** The live values are the ones selfplay has always run (the worker never builds a `DiffFocusConfig` from the reco); main still holds the Run-4 sweep winners that were never applied. Same landmine as `soft_policy_temp`: plumbing the key with main's values in place would silently start a data-affecting experiment |
| `soft_policy_temp` | 3.0 → **2.0** | 2.0 | **CLOSED 2026-07-26 — do not reopen.** Was the one delta this table did not list, and the only one that would have re-targeted training. See below |

**Rule: reconcile key-by-key, never by wholesale copy in either direction.**

**`soft_policy_temp` — why this row exists.** Until 2026-07-26 the live yaml said
`3.0` while selfplay ran the `2.0` dataclass default, because the key was never
published to the worker (E13). PR #257 makes the key real and correctly set
**main's** yaml to 2.0 — but main's yaml is not what a restart reads. The *live*
yaml still said 3.0, and this key was absent from this table, so an operator
reconciling key-by-key exactly as mandated would have kept 3.0 and shipped a
first-magnitude change to `soft_policy_ce` (~41% of weighted trunk gradient)
inside a PR whose stated purpose was to preserve behaviour, with no ledger entry
and no yardstick.

Realized temperature measured from the stored targets rather than read off any
config, by regressing `log(soft_i/soft_j)` on `log(p_i/p_j)` (slope = 1/T) over
1005 rows of three live shards: slope **0.5000**, i.e. **T = 2.0000 on 100% of
rows** (method rule 12). Had 3.0 shipped: mean top-1 target mass 0.5803 →
0.4219, target entropy 1.3631 → 2.0258 nats (**+48.6%**), KL(T2‖T3) 0.117 nats.

The live yaml is now pinned to the measured value. Verified the pin survives a
resume, which is not automatic: `setup()` overlays the yaml over the restored
trial config for every key that is neither PB2-searched nor in `_TOPOLOGY_KEYS` /
`_RESUME_CONSTRUCTION_BOUND_KEYS` / `_LAUNCH_FIXED_ASSET_PATH_KEYS`.
`soft_policy_temp` is in none of them and the only searched key is
`pb2_bounds_lr`, so the overlay applies 2.0 before the trainer and the first reco
are built — no `experiment_state` patch needed, even though `experiment_state`
still carries `3.0`.

**Adopting 3.0 is a real experiment** and needs its own ledger entry, not a
config-reconciliation decision.

**The same treatment was applied to `diff_focus_*` on 2026-07-26**, pre-emptively
rather than in response to a near-miss: those four keys were also dead (never
built from the reco) and also held values nobody had validated on this net. The
live yaml now stores what actually runs. **Generalised rule: when a config key
turns out to be dead, pin it to the realized value the same day.** Leaving the
aspirational value in place is what turns a harmless documentation bug into a
live unregistered experiment the moment somebody plumbs the key.

**Generalisation.** When a PR makes a previously-dead key live, the live value of
that key is a **deployment step, not a merge detail**. A behaviour-preserving PR
whose safety argument lives in a file the deploy procedure never touches is not
behaviour-preserving. Add it to this table the same day.

## B. Opening / seed selection

| # | invariant | instrument | status |
|---|---|---|---|
| B1 | seeded games ≤ `opening_fen_dole_max_fraction × games_per_iter` | per-iteration `opening_fenlist_games + opening_fenlist_sf_refute_games` vs `matching_games` | **VERIFIED 2026-07-26** — 70–113/iter, share 0.14–0.245 vs cap 0.25 |
| B2 | the dole goes to exactly ONE worker-poll per iteration (client-count-independent) | count `dole: received ... iter=N` across all worker logs; no `iter=` value twice | **VERIFIED 2026-07-26** — 26 events / 4 workers, 0 duplicates |
| B3 | seeding OFF means zero seeded games | same counters == 0 | **VERIFIED 2026-07-26** — 0 for 5 consecutive iters from iter 28 |
| B4 | seeds in the pool are actually lost positions (vetting holds) | `scratchpad/revet_active_pool.py` at 2M nodes | **FAILED 2026-07-26** — only 78/202 survive; 124 FPs, 23 winning for STM. Seeding disabled; see ledger PHANTOMS finding |
| B5 | `opening_fen_list_path` rewrites do not restart the selfplay session | worker log `live-applied ... no session restart` | **VERIFIED** (PR #224 readout, 188 applications, 0 restarts) |

## C. Selfplay generation

| # | invariant | instrument | status |
|---|---|---|---|
| C1 | realized selfplay/curriculum mix tracks `selfplay_fraction` | per-iteration `selfplay_games / matching_games` (NOT the median) | **VERIFIED 2026-07-26 in steady state** (0.46–0.53 before, 0.41–0.50 after) — but see C6 |
| C2 | games are not abandoned at session boundaries | worker `play_batch exit:` + config-application lines | **VERIFIED** (PR #224) for FEN-list rewrites — caveat: instrument only emits on failure, see method rule 5 |
| C3 | PID sample is healthy (curriculum W+D+L ≥ 30) | `pid_curriculum_w/d/l`, `pid_regret_reason` | **VERIFIED 2026-07-26 with a caveat** — 68–268/iter in steady state, but collapsed to 0/0/1/3 (`not_active`) for iters 28–31; see C6 |
| C4 | PID levers actually move (nodes lever not pinned) | `pid_nodes_active`; `wdl_regret` trajectory vs `sf_pid_wdl_regret_stage_end` | **VERIFIED-BY-DESIGN 2026-07-26 (an earlier FAILED verdict here was premature)** — `nodes_active = (not regret_enabled) or (gate_enabled and regret_stage_complete)`. Regret IS enabled, so nodes unlocks only once `wdl_regret <= 0.0075`. Live regret is **0.088**, 12× above. The pin is correct staging, not a broken lever. The real issue is C7 |
| C7 | stage 1 (regret) descends toward `stage_end` once winrate exceeds target | `wdl_regret`, `pid_raw_winrate`, `pid_regret_delta` per iteration | **VERIFIED-WITH-CONTEXT 2026-07-26 (an earlier FAILED verdict was retracted — the controller is behaving correctly)**. Sequence: regret opened 0.0393; **raw winrate at iters 1–3 was 0.41 / 0.36 / 0.41 — the restarted net was genuinely LOSING** — so the airbag fired at iter 2 exactly as specified (`raw_wr + 1.5·se = 0.364 + 0.047 = 0.411 < 0.45` floor, n=250) and eased regret to 0.0896. Raw winrate then recovered past target; `pid_ema_winrate` 0.513 → **0.589** (EMA lag is why tightening was delayed), and regret has been descending since iter 32 (Δ −0.0003, −0.0013, −0.0015). **The apparent 32-iteration "stall" was a correct airbag rescue plus EMA lag, not a defect.** NOTE the airbag reads `raw_wr`, NOT `ema_wr` — comparing the EMA against the floor gives the wrong answer |
| C8 | *(understanding, not a defect)* stage 2 is gated on MODEL STRENGTH, not on the controller | — | **NOTE — not a pass/fail row.** The controller targets winrate 0.50, so it stops tightening once the net holds 50%. Regret only reaches `stage_end` 0.0075 if the net can hold 50% against near-full-strength SF. **"The nodes lever never fires" is therefore a measure of the net not being strong enough yet, not a bug to fix.** Do not "fix" it by lowering `stage_end` without deciding that a nodes-based ladder is wanted at a weaker regret level |
| C9 | root Gumbel noise is applied at the configured scale and schedule, and is EXCLUDED from the stored target | recover the played move from consecutive stored plies (`scratchpad/probe_played_vs_target.py`), then measure `P(played == policy_target argmax)` by move number | **VERIFIED 2026-07-26 — an unusually clean natural experiment.** n=2,567. The agreement between the move actually played and the target's argmax rises exactly in step with the configured noise decay: moves 1–11 (scale 0.75) **0.7455**, move 12 (0.75) 0.7705, move 13 (0.50) **0.8281**, move 14 (0.25) **0.9298**, moves 15+ (0.00) **0.9122**. Noise is doing precisely what it is configured to do and is not leaking into the target: the `g` term enters only the top-*m* candidate score in both `gumbel.py:_select_top_m_with_gumbel` and `gumbel_c.py:541-571`, never `logits_imp`. Fast plies force scale to 0 (`network_turn.py:859-863`). **Follow-up worth having:** at zero noise the two still disagree 8.8% of the time. Some of that is inherent — the survivor is ranked at intermediate visit counts while the target is a softmax over all legal moves at final visits — but it was not possible to rule out a scoring inconsistency between the two sites |
| C10 | the realized full-ply (playout-cap) rate matches `playout_cap_fraction` | mean ply gap between stored rows within a game, split by `is_selfplay` | **VERIFIED 2026-07-26** — selfplay mean gap 4.068 ⇒ **0.2458**; curriculum 7.607 ⇒ 0.2629; configured 0.25. `full_ply_pair_fraction` is 0, so the draws are iid as designed |
| C11 | the search actually spends the configured 256/32 sim budget | broker `pos_s` ÷ `complete_gps` across all four worker logs, against the budget-implied evals per game | **VERIFIED-BY-THROUGHPUT 2026-07-26 (±12%, not an exact count)** — 5,700 pos/s ÷ 0.701 games/s = **8,127 NN evals per completed game**, against 7,286 predicted at (full 256, fast 32). The alternatives are far away: 4,682 at (128,32), 3,380 at (64,32). This discriminates strongly against any smaller realized budget but is not a direct count — the exact instrument is the DEBUG gumbel-profile line, which `log_level: INFO` suppresses |
| C12 | *(understanding, not a defect)* selfplay search uses the RL shape, not the PLAY shape | `GumbelConfig(...)` construction in `network_turn.py:725-739` vs `PLAY_SEARCH_DEFAULTS` | **NOTE — CODE-ONLY, and it contradicts an assumption that has appeared in tuning discussions.** Selfplay leaves `c_visit_root` / `c_scale_root` / `q_visit_exp_root` at their sentinel defaults, so `_root_sigma_scale` collapses to `c_scale·(c_visit + max_visit)` — the same **linear** transform as the descent. **The root-LOG transform (`c_scale_root=7`, `q_visit_exp_root=-1`) and `c_scale=0.025` are UCI/match-only.** Selfplay runs `c_scale` 0.1, `c_visit` 50, linear at both sites, deliberately (`gumbel.py:70-82`). So [[gumbel_cscale_miscalibrated]]'s +301 Elo at 8000 sims and [[gumbel_root_log_transform]] describe the *match* engine, and neither has ever applied to the data generator |
| C13 | the selfplay thread pool survives worker startup | count `inference broker timed out` / `slot acquire timed out` per worker log; corroborate with `active_threads` | **FAILED 2026-07-26 (mechanism established, magnitude NOT) — a startup race that costs ~90% of selfplay concurrency for the life of every session.** All four workers launch with `--selfplay-threads 32`, and in a 30-second window at 17:06:58–17:07:28 — while the broker was still booting (`AOT packages loaded` at 7.08s, `model ready` only at **67.30s** since boot) — **28–29 threads per worker died** on `inference broker timed out after 30.000s` (8) and `inference slot acquire timed out after 60.0s … all slots held` (21). They are never respawned: `worker.py:3193` already carries the comment that futures are joined only at session end, "which in continuous mode never arrives — so … the batch quietly runs short-handed". Corroboration: `active_threads` reads **3** in 57 of 63 stats windows and never exceeds 3 for the whole session. **Two honesty caveats, both load-bearing.** (1) `active_threads` counts threads that *completed a game* in the window (`_completion_telemetry_delta` returns `len(active)` over positive per-thread deltas), so it is a completion proxy, not a liveness count. (2) **The throughput cost is unestablished and may be small** — the box is GPU/broker-bound (K4: forward pass is 76.4% of batch time), `in_flight_avg` is still 24.0, and iters 40/41 ran at 515/530s. Iteration 42 took **2,839s**, but it is confounded by the restart, the ingest wait and the holdout refill. **This is chronic, not new** — the same pattern appears in the previous session's logs. Pre-committed instrument before anyone "fixes" it: compare games/h across a session where the workers are started *after* `model ready sha=` appears against one where they are not |
| C6 | toggling a `_RECO_RESTART_KEYS` key does not silently distort the data mix | worker log `restarting selfplay session [restart_keys=...]`, then per-iteration mix for ~6 iters | **FAILED 2026-07-26** — `opening_fen_dole_per_iter` is restart-gated; writing it abandoned all in-flight games, driving selfplay share to **1.00 for 4 iterations** (configured 0.50) and blinding the PID. Recovers by ~iter +6. **Any config toggle on a restart key contaminates the following ~6 iterations — start readouts after it, and never treat such a toggle as a free A/B** |
| C5 | search converts sims into strength at the expected rate | sims-1 vs sims-32 paired arena vs a FIXED banked ref | **RESOLVED 2026-07-26 — the deficit was LR DAMAGE, not the architecture.** The damaged 512 net widened 91.5 -> 251.9 Elo across the two rungs (~160 Elo of lost search conversion). The control settles it: **boot512, the undamaged donor, does not widen at all** — sims-1 **-41.9** [-95.3, +9.6] vs sims-32 **-43.7** [-88.1, -0.6], ~2 Elo apart with heavily overlapping CIs, 200 paired games per rung, run with training STOPPED so neither rung was contention-contaminated. Search converts sims to strength normally on this architecture; what failed to convert was a net whose matrix group had been run at 6e-3. **Retires the alternative branch:** the value head's ranking is NOT the emergency priority. Caveat: boot512 still sits ~43 Elo below the banked 46M at BOTH budgets, so the donor was behind its predecessor — judge the climb on the `vs_boot512` ratchet series, not on this ladder |
| C16 | C `gss_score_and_halve` matches the Python reference **as shipped** | 6 edge-case boards, deterministic hash evaluator, `target_batch` at the production default | **FAILED 2026-07-26, reproduced by hand** — 3/6 boards diverge and the two implementations pick a **different move** (`actions_eq=False`); L1 up to 1.90 at 256 sims. At `target_batch=1` they agree through 64 sims; above that a **separate, unrelated residual** appears (C27), so the agreement is not unconditional. The parity claim in `gumbel.py:278-306` is true about the formula and false about the algorithm |
| C17 | a simulation adds information: no leaf is evaluated twice in one GPU batch | distinct encoded rows vs `MCTSTree.node_count()` at the production shape | **FAILED 2026-07-26** — `gss_step` accumulates leaves across several halving reps to fill `GSS_GPU_BATCH=1024` while `gumbel_c.py` passes `vloss_weight=0`. With no virtual loss a later rep re-walks an **unchanged** tree, reaches the **same** leaf, and back-propagates the **same** value. Production shape builds 677,801 nodes vs 1,030,737 at per-rep flush (**−34%**); duplicate fraction 29–76% at 256 sims by batch size, 6–8% at 32 sims, so essentially the whole deficit lands on the full-sim plies — the ones that produce the policy targets (E1). **Do NOT "fix" without a ledger entry:** the duplicate visits still increment `N`, which inflates `max_visit`, which feeds the root `q_scale`, so removing them would flatten the training targets as a side effect. `target_batch` is already a plumbed argument, so the A/B is cheap at the sims-1 rung. **⚑ MEASURED 2026-07-27 — IT CORRUPTS THE TRAINING TARGET, −4.5 cp.** `--target-batch` was exposed on `scripts/audit_targets.py` (it had always existed on `run_gumbel_root_many_c`, never on the CLI, which is why this sat untested). Same checkpoint, same 2000 audit positions, only leaf-duplication differing: the production training target (leg d, 256 sims) goes **54.2 → 49.7 cp E[regret]** (top-1 48.3 → 44.1). **Controls held bit-identical**: leg (a) raw policy 85.3/62.4 and leg (c) pure-SF soft target 51.5/33.7 unchanged, and in the value table the pure-SF candidates (i)/(ii) are identical while only the search-dependent (iii)/(iv) move — so nothing but `target_batch` changed. **The ordering FLIPS**: at tb=0 the training target was the worst of the three search/teacher candidates, at tb=1 it is the best (49.7 vs PLAY 51.1 vs SF soft 51.5). **Dose-response separates it from a pure node-count effect**: duplication is 6–8% at 32 sims vs 29–76% at 256; leg (b) at 32 sims gained ~7% more nodes and got *slightly worse* (+0.9), while leg (d) at 256 sims gained 4.5 cp — the effect tracks DUPLICATION, not nodes. Value impact is negligible ((iii) 0.0484 → 0.0481), so this is a **policy-target** defect specifically. **Still DO NOT fix without a ledger entry**: removing duplicates lowers `max_visit` → lowers root `q_scale` → FLATTENS the target, so the fix is not a clean subtraction of noise. **⚑ ROOT CAUSE AND FIX MEASURED 2026-07-27 (second pass).** `gss_prepare_batch` never called `tree_apply_vloss_path` at all — the virtual-loss machinery was fully built (`tree_select_child`/`tree_gumbel_select_child` both honour it, `start_gumbel_sims` has taken a `vloss_weight` argument since the walker work) and simply **not wired into the Gumbel batch path**, so passing `vloss_weight>0` was a no-op and nothing incremented `virtual_loss[]`. Wiring it in (apply on leaf append, remove in `gss_finish_batch` just before backprop) plus a **duplicate-flush cursor** — an in-flight leaf is the only unexpanded node carrying virtual loss, so `virtual_loss[leaf]>0` is an exact "already in this batch" test; flush and resume there — gives, at the audit's own 256-board shape, deterministic pseudo-net, same nominal 256 sims: **tb=0/vloss=0 → 46 GPU batches, 38.7% duplicate rows, 735,325 nodes; tb=1 → 275 batches, 0%, 1,161,270 nodes; tb=0/vloss=1 → 45 batches, 0%, 1,183,687 nodes.** Virtual loss beats per-rep flushing on BOTH axes: more distinct nodes AND production's GPU round-trip count. On the frozen audit set (2000 positions, checkpoint `pre_sfp0_restore_20260727`, controls (a)/(c) bit-identical across all three arms) the production training target goes **54.2 → 46.8 cp E[regret] (top-1 48.3 → 43.3)** at `vloss_weight=1`, against 49.7/44.1 at `target_batch=1` — the virtual-loss fix recovers **7.4 cp, 1.6x what per-rep flushing recovers**, and it is the only one of the two that keeps the batching. Wall clock for the identical 2000-position audit: **tb=0 285.8s / 236.1s (two runs), vloss=1 302.4s, tb=1 987.6s** — per-rep flushing costs **3.5x**, virtual loss costs nothing outside the run-to-run spread. The PLAY leg moves the same way (50.2 → 48.5 at vloss, 51.1 at tb=1), so this is not a training-shape-only effect. **AND THE STATED HAZARD IS REFUTED.** This row claimed the fix would flatten the target by lowering `max_visit` and hence the root `q_scale`. Measured at 256 boards: **root `max_visit` is 69.22 and total root visits 256.00 in ALL THREE arms** — identical to the digit. Root visit counts are set by the sequential-halving budget arithmetic (`gss_score_and_halve` deducts `visits_per_action * n_cands` whatever the leaf turned out to be), so a duplicate wastes an NN eval but adds no root visit. `q_scale` therefore does NOT change, and whatever flattening happens comes from the completed-Q values, not from the transform. Sharpness does move, modestly. **Paired over the same 2000 rows** (controls (a) raw policy and (c) SF soft target both **exactly 0.0000** paired diff, so the pairing is clean): entropy **0.6996 → 0.7438, paired diff +0.0442 nats, 95% CI [+0.0285, +0.0600]**, softer on 57.1% of rows; mean top-1 **0.7477 → 0.7313, −0.0164 [−0.0240, −0.0088]**; rows at top-1 ≥ 0.99 **22.5% → 20.4%**. For scale, the SF MultiPV teacher on the same rows sits at **1.3086 nats / top-1 0.5441 / 12.9% at ≥0.99** (the live sf_p0 teacher measures 1.352 / 0.525 / 14.1% on live shards — the same picture from the other side), so the target-to-teacher gap is 0.609 nats and the fix closes **7.3%** of it. It moves TOWARD the teacher and comes nowhere near passing it. **⚑ AND THE NODE CONFOUND IS SETTLED.** Both fixes buy ~60% more distinct nodes per nominal sim, so a node-matched control was run: `--rl-sims 426` at tb=0/vloss=0, calibrated so tb=0 builds the same 1.18M distinct nodes the vloss arm builds at 256. It scores **49.6 / 45.8** — i.e. 66% more nominal sims buys 4.6 of the 7.4 cp. `target_batch=1` scores 49.7, **indistinguishable from the node-matched control: its entire −4.5 cp is more search and nothing else.** Virtual loss scores 46.8, **2.8 cp beyond the node-matched control** — that residual is genuine distribution improvement, and it is the in-flight penalty steering descent apart, not merely the absence of duplicates. Note the control also sharpens the target (entropy 0.5925, 28.7% at top-1 ≥ 0.99 at 426 sims), so "more sims" and "the fix" move sharpness in OPPOSITE directions. **Still needs its own ledger entry before it goes live** — it changes every stored policy target |
| C25 | the cross-implementation test can actually detect a divergence | read + mutation-test `tests/test_mcts_uci_parity_gates.py` | **FAILED-THEN-FIXED 2026-07-26 (PR #263)** — the Gumbel test asserted legal masks only and **never compared `py_prob` to `c_prob`**, while its PUCT sibling twenty lines above does; it also ran at `simulations=1` against a zero evaluator, where every leaf is worth the same so revisiting one costs nothing. Structurally incapable of failing on C16/C17. Fixed by adding the missing assertion plus a parametrized gate under a non-degenerate evaluator, **verified failable by mutation** (goes red on the move, 228 != 229) |
| C26 | every PLAY/EVAL entry point uses `PLAY_SEARCH_DEFAULTS` | grep the call sites | **FAILED 2026-07-26 (partial)** — `scripts/arena_standard.py` is fine, so the standard arena and the daily ratchet do run the tuned play shape. But `play_match_batch`'s `_pick` calls `pick_moves_for_boards` with **no `gumbel_overrides`**, so `chess_anti_engine/arena.py` (the in-loop gate) and `scripts/match_checkpoints.py` play the SELFPLAY shape — `c_scale` 0.1 not 0.025, `topk` 16 not 32, root LINEAR not LOG. The `PLAY_SEARCH_DEFAULTS` docstring claims it is referenced "from every such entry point" and names "the training-gate match". Bounded today because the in-loop gate is dead (L4, `gate_games: 0`), but `match_checkpoints.py` is a live tool. **FIX IN FLIGHT: PR #265** defaults `play_match_batch` to `PLAY_SEARCH_DEFAULTS`; every number ever read from `match_checkpoints.py` was measured at the untuned shape and is not comparable across that change. **SUPERSEDED 2026-07-29 (PR #286) — the first clause was WRONG and the invariant itself is now the wrong question.** `scripts/arena_standard.py` was NOT fine: it seeded `_parse_gumbel_overrides` from `PLAY_SEARCH_DEFAULTS` even with no flag, so it *did* apply the play shape — but it passed **no `vloss_weight` and no `target_batch` at all** (function args, not `GumbelConfig` fields, so no `dataclasses.replace` surface could reach them), i.e. the arena and the daily ratchet ran the play shape at `vloss_weight=0`: the pre-C17 duplicate-leaf search, diverging from production on 10% of moves at 32 sims and 27.5% at 256. So "the standard arena and the daily ratchet do run the tuned play shape" was false in the way that mattered. **And running the PLAY shape there is no longer the goal:** an arena judging the TRAINING loop must run the training shape, which is what the ledger's 2026-07-28 finding established. `--search-shape {play,training}` is now REQUIRED with no default, the realized shape is stored per JSONL row, and `daily_gate_ratchet.sh` pins `training` and stamps every CSV row with it. Read C26 as: *every PLAY/EVAL entry point states which shape it runs and runs that one* — pinned by `tests/test_arena_search_shape_plumbing.py` and `tests/test_ratchet_search_shape.py`. Pre-2026-07-29 ratchet rows are labelled `legacy_play_vloss0` and `ratchet_slope.py` refuses to fit across the break |
| C27 | C and Python agree at matched batching across the **production** sim budget | 6 edge-case boards, deterministic hash evaluator, `target_batch=1`, sims 64→256 | **FAILED 2026-07-26** — a residual divergence appears from **96** sims up and grows with the budget: L1 0.0038 / rel 0.006 at 96, rel 0.097 at 128, rel 0.135 at 256. Production runs **256**, so the tested-clean regime (≤64) is not the regime that builds training targets. 1 of 6 boards diverges at every budget above the cap, and the selected **move agrees at every budget** — a distribution-shape difference, not a different decision. **NOT explained by C16/C17**: both are artifacts of cross-rep leaf accumulation and this survives `target_batch=1`. Pinned by a STRICT xfail (PR #263) so the test fails if it ever disappears. **⚑ LOCALISED 2026-07-27 — IT IS THE FIXTURE, NOT THE SEARCH, AND IT IS AN ARTIFACT.** Every named candidate was killed by a runtime kill-switch bisect (scratch build, one flag each): transposition table, forced-move-chain collapse, solved-state propagation, 2-fold-as-draw, 3-fold, and the 50-move claim all left the divergence **bit-identical** (L1 0.057483 at 128 sims with each disabled, and with all of them disabled together). The actual cause is **exact-tie ordering**: `_root_logits` returns ZEROS, so the root prior is uniform over every legal move, and `add_noise=False` zeroes the Gumbel noise too — root candidate ranking is then decided entirely by iteration order, and C and Python order ties differently. Remove the ties by EITHER route and the residual is **exactly 0.0 on all 6 boards at 96, 128 and 256 sims**: non-uniform root logits → L1 0.000000, or `add_noise=True` → L1 0.000000. Production roots come from a network, so exact ties are measure-zero and this does not reach the training targets. **Two corrections to the row above**: (a) "cause unknown" is retired; (b) "the selected move agrees at every budget" is FALSE — at **94** sims the two implementations pick a **different move** (the claim held only at the three budgets that were sampled). Pinned going forward by `test_gumbel_cross_impl_matches_with_a_non_degenerate_root_prior`, which asserts EXACT parity at 96/128/256 once the ties are gone; the strict xfail stays because the tie-break order genuinely does differ |
| C28 | every knob the yaml offers the selfplay search actually reaches it | the `search.*` names in `network_turn.py`'s `GumbelConfig(...)` vs the `selfplay:` allowlist | **FAILED 2026-07-27, FIX IN FLIGHT: PR #284** — `fpu_reduction` and `fpu_at_root` are in the live-yaml allowlist and in `SearchConfig`, but the distributed worker never resolves them from the reco and `network_turn.py` reads them only on the PUCT branch, which `mcts: gumbel` makes unreachable: editing either is ACCEPTED and does nothing. Harmless only because the yaml's `1.2`/`1.0` happen to equal the dataclass defaults. Related and fixed 2026-07-29 (PR #286): `gumbel_vloss_weight` DOES reach the worker, but was absent from `main`'s yaml while live ran `1` — so an arena or a restart off `main` silently got the pre-C17 search. The general guard is now `tests/test_arena_search_shape_plumbing.py::test_every_config_driven_knob_reaches_the_arena_or_is_provably_inert`, which fails the day a `search.*` knob moves off its default without the measurement path following it |

**Stage-C addendum provenance (2026-07-26).** C16/C17/C25/C26 come from the first
audit of the C search kernel against its Python reference. C16, C25 and C26 were
**re-verified by hand** before being recorded; C17's mechanism was reproduced
(parity at `target_batch=1`, divergence at the production value) but its
node-count magnitudes come from a CPU hash-evaluator harness, not live selfplay.

**C17 does NOT explain C5, and C5 is the reason to believe that.** The obvious
story — duplicated leaves mean sims buy less strength — is refuted by C5's own
control: boot512, the undamaged donor, does **not** widen across the sims-1/sims-32
rungs (−41.9 vs −43.7 Elo, ~2 apart, heavily overlapping CIs). Search converts
sims to strength normally on this architecture *while C17 is live*, which bounds
C17's Elo cost rather than implicating it. C17 is a real inefficiency in node
economy; it is not a demonstrated strength loss, and it must not be cited as one.

Further findings from that pass that are real in code but latent or untested in
production — the root-`Q` convention, the Python descent silently ignoring
`halving_div` / `q_visit_exp` / `q_visit_floor` / `q_global_scale`, the
`GSS_MAX_CANDS = 64` clamp, and a missing `isfinite` guard at the C root-halving
site — are deliberately NOT recorded as invariants, because none of them has an
instrument that can currently see it fire.

## D. SF labelling

| # | invariant | instrument | status |
|---|---|---|---|
| D1 | label depth is the live PID budget, not the yaml floor | `_eff_sf_nodes`; `opponent_sf_nodes` in `progress.csv` | **VERIFIED 2026-07-26** — 698289, `sf_label_nodes_cap: 0` |
| D2 | label coverage: rows that should have `sf_wdl` do | `replay_has_sf_wdl_frac` | **VERIFIED 2026-07-26** — 0.947–0.999 over the last 5 iters |
| D3 | fast plies get 0.25x scale by design, not by accident | `sf_fast_ply_node_scale`; `stockfish_turn.py:236-239` | **VERIFIED 2026-07-26** — 0.25 resolved from the code default (no yaml override), applied only when the ply is neither a full-node ply nor an `sf_refute` full move. Note it affects only SF's in-play move choice: with `record_fast_ply_value: false`, fast-ply rows never reach training at all (see E1) |
| D4 | vet ruler ≥ label ruler (no gate shallower than the labels) | `harvest_gate_step.py --sf-nodes` default | **VERIFIED 2026-07-26** — raised 300k → 2M |
| D5 | *(decision, not a check)* the SF teacher's cost is ACCEPTED, do not re-propose cutting it | `ps` by process class | **DECIDED 2026-07-26 — keep the strong teacher.** Measured: Stockfish label generation holds **27.1 of 32 cores** (32 procs at the full ~698k PID budget, `sf_label_nodes_cap: 0`), i.e. ~85% of the machine; load average sits at ~48/32. Capping labels at e.g. 200k would free ~19 cores. **Rejected by the operator:** the value head is SF-label-bound and the WDL blend's SF component is load-bearing (zeroing it crashed winrate 0.64 → 0.40), so cheapening the teacher trades away the thing the labels exist to improve. **Consequence to design around, not fix:** this box is permanently CPU-saturated during training. Anything else that needs CPU must be scheduled around it or made batch-efficient — which is why arena concurrency, not sim count, is the lever that matters (see L1) |
| D6 | the SF label position really is P1 — one ply after the record — and `sf_policy_target` lives in P1's own STM frame | decode step-0 planes → board, restore castling/EP from planes 104-107/110, push every legal move, compare each child's compact-lc0_1858 legal-index set to the stored `sf_legal_mask` | **VERIFIED 2026-07-26** — instrument validated first (reproduced the row's own `legal_mask` 699/699), then every one of n=699 labeled rows has ≥1 child whose legal set equals `sf_legal_mask` (287 unique, 412 ambiguous, **0 unmatched**); `sf_legal_mask == legal_mask` in **0/699**; `sf_policy_target` mass inside `sf_legal_mask` median **1.000000** vs median **0.000000** inside the record's own legal set. The P1 + POV-flip story is real, not folklore |
| D7 | the `sf_wdl` POV flip points the right way, not merely self-consistently | Pearson r between label q = W−L and the record-POV game outcome | **VERIFIED 2026-07-26** — r = **+0.655** over 13,318 rows; mean q by W/D/L = +0.426 / −0.150 / −0.497, monotone. A flipped label would give r ≈ −0.65, so the test discriminates |
| D8 | `sf_wdl` is the configured cp-logistic, refit from stored scores rather than read off the yaml | refit `cp_to_wdl` from `sf_label_meta` cols 2-3; grid-search slope × draw-width | **VERIFIED 2026-07-26** — at slope 0.0060 / dw 120.0 with the POV flip: max-abs-error median 9e-5, p99 2.4e-4 (the float16 storage floor); frac error >0.01 = **0.0000** over 13,740 rows. Grid search over slope ∈ [0.004, 0.010] × dw ∈ [60, 150] returns **exactly (0.0060, 120.0)**. Method rule 12 satisfied: realized, not configured |
| D10 | no training target keys off the move the handicapped SF actually played | grep every consumer of `sf_played_*`; check the one loss path that reads them | **VERIFIED 2026-07-26, conditionally — see D11** — the played move reaches exactly one loss path: `finalize._suffix_sf_regret_features` → `future_sf_regret_*` → `losses.py:395-409`, which SHIFTS the WDL outcome target. That branch is gated on `use_adjusted_wdl_target`, **false** live. `sf_wdl` / `sf_policy_target` / `sf_move_index` are built from `res.bestmove_uci` before `_push_curriculum_opponent_move` runs. The independence is load-bearing, not vacuous: played ≠ best in **51.8%** of 4,795 curriculum rows, played-rank spanning 1…27 |
| D11 | the D10 invariant cannot be switched off from the live yaml without a restart | `_TRAINER_LIVE_KEYS` (`config_yaml.py:204-205`) vs `trainable_config_ops.py:547-554` | **FAILED 2026-07-26 (armed hazard, not a current defect)** — `use_adjusted_wdl_target` is BOTH a validated live-reload key AND applied live. One live-yaml edit, no restart, and the value target starts keying off the handicapped move at `adjusted_wdl_regret_source: sum`, `scale: 1.0`, **`cap: 0.0` = uncapped**, over a whole-game suffix sum. Given the measured per-move regrets that saturates within a normal game. **The dormant default is the dangerous one:** flipping one boolean should not silently arm an uncapped target shift |
| D12 | D1's "label depth is the live PID budget" generalizes from *nodes* to actual search DEPTH | `sf_label_meta` col 1 on live rows + a matched CPU-only SF run at `go nodes 698289`, MultiPV 40 vs 1 | **FAILED 2026-07-26 (D1 measured the wrong quantity)** — D1 is named "label depth" and measures nodes. Realized depth: median **12**, p25 11 (p99 212 = mate/TB rows). Cause is MultiPV, not the TT: at the same 698,289 nodes, MultiPV=40 → depth median **13** vs MultiPV=1 → **19** (n=6 P1 positions from live shards); Hash 16MB→256MB changed nothing, killing the small-TT hypothesis. **The teacher is a depth-≈13 search, ~6 plies shallower than the same CPU buys**, and "698k nodes" is NOT comparable to the frozen audit set's "≥1M nodes, MultiPV ≥10". D5 priced the teacher in cores; nobody had priced it in plies |
| D13 | MultiPV covers the legal set, so the label-smoothing floor is the exception | valid `sf_multipv_raw` rows vs `sf_legal_mask` popcount | **VERIFIED 2026-07-26** — 13,740 rows: fully covered **87.1%**; the other 12.9% take the designed `sf_policy_label_smooth: 0.01` uniform floor. No row truncated below min(legal, 40). Target sharpness: top-1 mass median **0.583**, exp(H) median 2.55; 20.9% effectively one-hot, 42.2% with top-1 < 0.5 — a genuinely soft teacher |
| D14 | nothing silently falls back to a cheaper or absent label | `has_sf_*` fractions + what the uncovered rows are | **VERIFIED 2026-07-26** — the six `has_sf_*` flags move together exactly (`sf_policy & ¬sf_wdl` = 0, `& ¬multipv` = 0, `& ¬meta` = 0 over 13,769 rows). Coverage 0.9979, matching `replay_has_sf_wdl_frac` 0.9983, so D2's ruler is not lying. The 0.21% unlabeled are **100%** explained: zero legal moves at P1 (the net's move ended the game) |
| D15 | the label budget is the full PID budget PER ROW, with no fast-ply-scaled labels leaking in | `sf_label_meta` col 0 across 13,740 labeled rows | **VERIFIED 2026-07-26** — nodes median **698,524** vs `opponent_sf_nodes` 698,289, p05 696,407. The <0.30× band holds 0.62% of rows and **99.5% of those are explained** by mate score, TB-range cp, or a single legal move. Exactly **1 row in 13,318** is an unexplained short search |
| D16 | async labels attach to the record they were queried for | adjacent same-game labeled pairs sharing BOTH `sf_label_meta` and `sf_legal_mask` | **VERIFIED 2026-07-26** — 12,579 adjacent pairs: identical meta in 1 (0.008%), identical legal mask in 0.36%, and **0 pairs share both**, which a duplicated attachment would necessarily do. The submit-time capture holds under live load |
| D17 | `wdl_regret` bounds the opponent's error in the units an operator reads it in | reconstruct best-vs-played cp gap from `sf_multipv_raw` for curriculum rows | **FAILED 2026-07-26 (bounded in WDL space by construction, unbounded in cp)** — at live `wdl_regret = 0.0896` the played move's cp regret over 2,621 rows is median 0, p75 26, p90 85, **p95 172, p99 1,569**, mean 199; 8.4% of opponent moves throw away >100cp. Mechanism: the filter runs in SF's native permille WDL, which saturates — in **26.4%** of positions EVERY MultiPV candidate is inside the band, so SF picks uniformly among up to 40 moves (acceptable-set median 5, p90 28). The cp tail is mostly TB/mate positions where all moves still win, so this is not "the opponent hangs games" — but **"regret 0.09" reads as a small handicap and is not one.** Any difficulty reasoning that treats `wdl_regret` as cp-like is wrong |
| D18 | every training target is a function of the network's input | the P1 decode identifies the net move the label is conditioned on; compare to `argmax(policy_target)` | **FAILED 2026-07-26 (design property, previously unstated)** — `sf_policy_target` is the reply distribution after a specific SAMPLED net move, and that move is **not in `x`** (which encodes P0 only). On the 287 uniquely-identified rows it equals `argmax(policy_target)` in 260 (**90.6%**), so ~9.4% of `policy_sf` rows ask the net to predict the reply to a move it would not have chosen — unlearnable from the input. **Worse for metrics than for training:** the loss masks `sf_pol_logits` with `sf_legal_mask`, i.e. the P1 legal set, which **leaks the played move at train time and is unavailable at inference**, so any "sf policy accuracy" read off training is inflated. `w_sf_move` is 0.02 so the training cost is small; the metric hazard is the actionable part. The 90.6% is measured on the 41% of rows with a uniquely-identifying legal set and may be biased |
| D19 | the opponent's move filter and the label share one score metric | `stockfish_turn.py:1145-1147` vs `1153-1158`; wdl-absent fraction | **CODE-ONLY 2026-07-26 (impact small; latent silent-failure mode)** — the move-selection candidate list omits the cp-logistic kwargs and so scores on SF's NATIVE WDL, while the label list two lines later passes the live logistic params. Measured divergence is small (acceptable-set median 5 native vs 6 logistic). **The hazard is the fallback:** if `pv.wdl` were ever absent (ShowWDL off, engine swap, parser change) `_pv_wdl_score` returns None for every candidate, the list empties, and `_choose_curriculum_opponent_move` silently plays SF's **best** move with no handicap — no log line, no metric. Currently safe: wdl-absent fraction **0.00000** over 13,740 rows |

**Stage-D addendum (2026-07-26).** D had 5 invariants and zero failures on the
stage that produces every training target. That was under-examination, not
health: D6–D19 add three FAILED and two code-level hazards. The two worth
carrying forward are **D12** (the teacher is ~6 plies shallower than its node
count implies, because MultiPV 40 buys width with depth) and **D17** (the
regret knob is bounded in WDL units and emphatically not in cp). Both were
invisible because the existing rows measured the input to the stage rather than
what the stage resolved — method rule 12 again.

Three things D deliberately does NOT claim: the D18 figure is measured on the
41% of rows whose legal set identifies the net move uniquely and may be biased;
the D12 depth tax is an n=6 median, so the direction is solid and the magnitude
is not; and the escalation path (`sf_label_escalate_q_gap: 0.0`) has never
executed live, so D14's clean result says nothing about whether it works.

## E. Finalize → rows

| # | invariant | instrument | status |
|---|---|---|---|
| E1 | playout-capped rows are excluded from the POLICY corpus | `replay_has_policy_frac` in `result.json` | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — `replay_has_policy_frac = 1.0` on each of the last 10 live iterations, i.e. zero value-only rows in the window, consistent with `record_fast_ply_value: false`. Same pass confirmed `replay_pmass_gap_share = 0.0`, so the KILLED #104 gap-priority knob is genuinely inert |
| E2 | seed labels attach to the seed position (offset 0) | join `content_seed_id` to first labelled ply | **VERIFIED 2026-07-26** — offset 0 for 100% of 669 cohort games |
| E3 | `policy_sf` targets are the OPPONENT reply, POV-flipped | `sf_move_index` / `sf_policy_target` vs `sf_legal_mask` and `legal_mask` in live shards | **VERIFIED 2026-07-26 (measured, was CODE-ONLY) — decisive.** Over 6,069 live rows carrying `has_sf_move` + `has_sf_legal_mask`: `sf_move_index` is legal under `sf_legal_mask` (the NEXT position) in **100.00%** of rows but under `legal_mask` (THIS position) in only **8.47%** — coincidental overlap, as expected across a ply. The two masks are **never** identical (0.00%). `sf_policy_target` puts **1.0000 of its mass (min 1.0000)** inside `sf_legal_mask` vs 0.0891 inside `legal_mask`. The head is unambiguously trained on the opponent's reply, which is why upweighting `w_sf_move` hurt |
| E5 | `wdl_target` is side-to-move POV at that ply, not white POV | partition each `game_id`'s rows by `ply_index % 2` over live replay shards; a decisive game must give one parity pure `{0}` and the other pure `{2}` | **VERIFIED 2026-07-26** — 3 disjoint window slices (oldest / middle / newest, 163k rows, 8.4k games). 5,656 games carry rows at both parities and there are **0 violations**: no parity ever holds more than one distinct target within a game. Newest slice: 1,015 games `(even=W, odd=L)`, 721 `(even=L, odd=W)`, 440 all-draw; never a `(W,D)` or `(W,W)` pair. A sign flip here would have been invisible to every ruler in use, so this is a load-bearing negative result |
| E6 | every termination class writes the correct result, and none silently defaults to draw at scale | `checkmate_rate` / `stalemate_rate` / `adjudication_rate` / `tb_adjudication_rate` / `draw_rate` in `progress.csv`, games-weighted, plus the residual | **VERIFIED 2026-07-26** — iters 33–41, 4,257 games: checkmate **0.4092**, TB-adjudicated **0.4040** (~32.6% of those are draws), other natural draw (3-fold / 50-move / insufficient) **0.1858**, stalemate **0.00047**, max-ply→SF adjudication **0.00047**. The accounting closes: checkmate + TB-decisive = 0.6815 vs 1 − draw_rate = 0.6824, Δ 0.001 = the 2 stalemates + 2 adjudicated games. **The 450-ply cap is not flooding the buffer with fake draws** — confirmed independently row-side, exactly **1 game in 3,897** reached it. Note there is **no resign mechanism at all**: `_compute_resign_weights` scales sample weights only and never ends a game |
| E7 | terminal and near-terminal rows carry real targets, not placeholders | slice rows by `last_ply_in_game − ply_index` | **VERIFIED 2026-07-26** — on the final row of each game (n=4,005): `corr(sf_signal, outcome) = +0.913`, sign agreement on decisive rows 0.976, `moves_left` mean 0.0154 (≈7 plies) with min exactly 1/450. No off-by-one: `moves_left·450 + ply_index` is constant within a game to 0.22 plies (fp16 rounding). One benign boundary effect: `has_sf_wdl` is **0.956** on the last row vs 0.996 overall, because `sf_wdl` is queried *after* the net's move and a game-ending move has no successor to query. Those rows fall back to the game one-hot, which is the correct target for a terminal position |
| E8 | `sf_wdl` is POV-consistent with `wdl_target`, with the right sign, on every subset | Pearson corr of `sf_wdl[0]−sf_wdl[2]` against outcome (+1/0/−1), plus a per-game parity test | **VERIFIED 2026-07-26 — decisive.** 73,250 rows, global **+0.654**, and **no negative subset anywhere**: selfplay +0.693, curriculum +0.488, even-ply +0.650, odd-ply +0.642, and ply[0,10) +0.202 rising monotonically to +0.845 at ply[100,200) (the model of the game sharpens as it resolves — the expected shape). The clinching test is per-game: `corr(mean sf_signal on even plies, mean on odd plies) = −0.946`, sign-opposite in 92.6% of the 2,139 both-parity games. `sf_wdl` flips with side-to-move exactly as the outcome does. `search_wdl` gives the identical −0.946, and `corr(search, sf) = +0.925`. **One asymmetry, explained not excused:** on drawn rows mean `sf_signal` is −0.157, but split by mode it is −0.0195 for selfplay (symmetric, as it must be, since the net plays both sides) and −0.299 for curriculum (the net plays one side, so every row is its own POV). That is the handicapped-opponent regime — objectively worse, holding a draw — not a sign error |
| E9 | *(understanding, not a defect)* `sf_wdl` is an afterstate value, not a position value | `_sf_result_wdl_for_record` + `flip_wdl_pov` | **NOTE — CODE-ONLY, and load-bearing for value work.** `sf_wdl` is Q(s,a): SF's evaluation of the position *after the net's chosen move*, POV-flipped back. So the `wdl` head at ply *t* is partly supervised by SF's opinion of ply *t+1* conditional on the move the net actually played. It is an on-policy afterstate value, not V(s). This is the value-side analogue of `policy_sf` training on the opponent's reply, and anyone reasoning about the value ceiling needs it |
| E10 | `policy_target` is the completed-Q Gumbel improved policy, not visit counts, and carries no temperature | shape of the stored distribution against `gumbel_topk=16` | **VERIFIED 2026-07-26 — decided by a counting argument.** At most 16 actions can ever be visited, yet **73.8% of rows carry more than 16 nonzero entries** (11,031 distinct values in a single shard). Visit counts could not produce that. The target is `softmax(log prior + σ(completedQ))` over all legal moves (`gumbel_c.py:1007-1019`), and no temperature is applied to the main target. With `temperature`/`selfplay_temperature` both 0.0, `_resample_actions_with_temperature` no-ops, so **the move played is the sequential-halving survivor while the target is the unsampled improved policy** — the intended split. Rank of the played move within the target: 1×2301, 2×135, 3×62, 4×25, 5×19, ≥6×25 — no temperature-sampling tail |
| E11 | `policy_target` sums to 1 and lives entirely on legal moves | direct read of `policy_target` + `legal_mask` | **VERIFIED 2026-07-26** — sum mean 1.000004, range [0.999617, 1.000395], no row off by more than 1e-3 (fp16 granularity), and **off-legal mass exactly 0.0 on all 16,000 rows**. One caveat with a downstream consequence: **8.86% of legal entries store exactly 0**, which is fp16 underflow (the smallest stored nonzero is 5.96e-8, the fp16 subnormal floor) rather than a mapping loss. Under the `p^(1/T)` soft transform those stay 0 where the true value would be ~1e-4; the effect on soft CE was not quantified |
| E12 | the 4672 → 1858 policy projection is lossless and injective for every legal move class | `scratchpad/probe_4672_1858.py`, plus in-search vs stored entropy | **VERIFIED 2026-07-26 — exhaustive, and this one had real risk in it.** 13,389 positions, **394,660 legal moves including 55,821 underpromotions: 0 unmapped, 0 aliases, 0 round-trip failures, 0 mask popcount mismatches.** `COMPACT_TO_FULL_POLICY` is a strict bijection onto 1,858 of the 4,672 ids (2,814 unused). Corroborated live: in-search diagnostic entropy in 4672 space is 0.6323 against a stored 1858 entropy of 0.6289, so the gather plus fp16 costs ~0.003 nats. A lossy mapping here would have put target mass on the wrong move with nothing to catch it |
| E13 | realized `soft_policy_temp` equals the configured value | fit the exponent from `log(policy_soft_target)` vs `log(policy_target)` on live shards | **FAILED-THEN-FIXED 2026-07-26 — the yaml's 3.0 had never been in effect, and this is the largest live defect found today.** Fitted exponent 1/T = **0.50000** (median; p1 0.49986, p99 0.50013 over 944 row-fits, independently re-measured) ⇒ **realized T = 2.0 against a configured 3.0**. Not a rounding artifact. **Mechanism, verified end to end:** `distributed_runtime.py` builds `recommended_worker` naming `sf_policy_temp` and `sf_policy_label_smooth` but **omitting `soft_policy_temp`** — confirmed against the live `publish/manifest.json`, which has **83 keys and does not contain it**. `worker.py:_build_selfplay_configs` does not read it either, so `GameConfig` takes its dataclass default of **2.0** (`selfplay/config.py:141`), and `finalize.py:936` — the only producer of `policy_soft_target`, and it runs on the worker — applies that. The yaml has read `soft_policy_temp: 3.0` since commit `6862c5151` on **2026-03-02**, with the comment "LC0 uses 3, was hardcoded 2". It was never un-hardcoded. **Why it matters:** `w_soft: 1.0`, and the trunk-gradient probe puts `soft_policy_ce` at **41.1%** of weighted trunk gradient — the second-largest term in the loss, built at the wrong sharpness for five months. **Fixing it needs TWO edits** (publish it in `distributed_runtime.py`, consume it in `worker.py`); a yaml-only change cannot take, which is also why `configs/exp_soft_policy_ablation.yaml` would have been a silent no-op in distributed mode. Because the replay window holds ~a day of data, flipping it is **data-affecting** — ledger entry and pre-committed yardstick first. Nothing is retroactively invalidated: no ledger verdict ever cited this key. **FIX SHIPPED 2026-07-26 (PR #257), behaviour-preserving:** the key is now published by `build_recommended_worker` and consumed by `_build_selfplay_configs`, and the production yaml is set to **2.0** in the same change — so the realized target is byte-identical to what it has always been, and the config finally says so. 2.0 → 3.0 remains an unstarted, data-affecting ledger experiment; do not read this row as evidence that 3.0 was tried. Regression cover: `tests/test_reco_coverage.py` |
| E14 | 32-sim fast-ply distributions do not become policy targets | `has_policy` rate + `future_policy_target` provenance | **FAILED-BY-DESIGN 2026-07-26 — true for the main head, deliberately false for the future head.** 100% of stored rows are `has_policy=1` / `is_network_turn=1`, and `record_fast_ply_value: false` drops fast rows entirely, so the MAIN policy target is always a full 256-sim search. But `future_policy_target` is read from the record at ply+2 **with no `has_policy` gate** (`finalize.py:943-955`, whose own comment says "Gating dropped 75% of targets"), so **~75% of future targets are 32-sim searches**. 97.9% of rows carry one, and the entropy difference is visible: H(future) 0.763 vs H(main) 0.636. This is intentional and cheap (`w_future: 0.01`), and is recorded as a documented exception rather than a defect — but "our policy targets are 256-sim searches" is only true of the head that matters |
| E4 | no all-zero / malformed policy or WDL rows reach the buffer | `PYTHONPATH=. python3 scripts/audit_row_integrity.py --shards 12` | **VERIFIED 2026-07-26** — 18,273 rows over the 10 newest live shards, CLEAN. Checks: `x` all-zero/NaN, policy all-zero/NaN/negative/sum≠1, `wdl_target` out of range, `sf_wdl` NaN or outside [0,1] |

## F. Upload → inbox

| # | invariant | instrument | status |
|---|---|---|---|
| F1 | `._tmp_` shards are never ingested | `is_tmp_shard_name` unit cases + `iter_shard_paths` over the live dir | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — filter correct on all probes including the near-misses `atmp_shard.zarr` and `shard_tmp_x.zarr` (both correctly INGESTED; the rule is a prefix match on `tmp_`/`._tmp_`, matching the writer in `shard.py:1310`). Live dir: 820 entries, 0 tmp-named, 820 selected, **0 leaked** |
| F2 | un-ingested shards survive a restart | trace `replay_shard_dir` vs `selfplay_shards` durability | **VERIFIED 2026-07-26** — replay dir is durable via `tune_replay_root_override`; `selfplay_shards/` on `/tmp` is a derived export with no reader. NOT a data-loss path |
| F3 | shard load failures are surfaced, not silently dropped | `[disk_buf] WARNING` in the training log + `tests/test_replay_shard_load_accounting.py` | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — **0** occurrences across the full run log, and the instrument is proven able to fire: 18 tests pass covering tracked-shard failure counted AND reported, the untracked-shard benign trim race, FileNotFound still counting as a fault, and the warn throttle. A zero here is evidence because the alarm is tested, not merely present |

## G. Ingest → replay + holdout split

| # | invariant | instrument | status |
|---|---|---|---|
| G1 | `replay_positions_ingested / matching_positions` ≈ 1 | `audit_realized_config.py` cross-check | **VERIFIED 2026-07-26** — 1.00 (was 5.61 pre-#225) |
| G2 | stale-model games do not exceed matching games | `distributed_stale_games` vs `matching_games` | **VERIFIED 2026-07-26** — 0/20 iters |
| G3 | replay window size matches config intent | `grep "buffer init" /tmp/chess_training.log` | **VERIFIED 2026-07-26** — `len(buf)=1497712 capacity=1500000` vs `replay_window_max: 1500000`, 815 shards, `startup_source=salvage seeded=True` |
| G4 | the holdout is a STABLE ruler | `test_size`, `holdout_generation`, turnover rate | **FAILED 2026-07-26 (re-read after the freeze armed) — the freeze delivers ONE of the four things stability needs.** (a) Composition frozen between readouts: **MET** (`holdout_frozen=1`, `test_replay=2000` since ~iter 46; `reset_holdout_on_drift: false` closes the re-roll path). (b) Survives a restart: **NOT MET LIVE** — PR #261 merged 19:52:36, this process started **17:04:53**, so `checkpoint_000058` has no `holdout.npz` and `trial_meta.json` no `holdout_frozen`; the current frozen ruler still dies at the next restart. (c) The eval is a pass over the set: **NOT MET**, and fixed by nothing merged — see G14. (d) Representative of the training window: **NOT MET** — see G13/G8. `holdout_generation` still inert (0 on every row) |
| G5 | the holdout ruler survives a restart | `test_replay` / `test_size` / `test_loss` per row across a restart boundary in `result.json` | **FAILED 2026-07-26 → CODE FIX MERGED (PR #261); re-read owed at the next restart.** The measurement below stands as written; what changed is the code, not the reading. **The ruler was destroyed by every restart.** `trainable_init.py:630` constructs `holdout_buf = ArrayReplayBuffer(tc.holdout_capacity, rng=rng)` unconditionally; nothing restores it from the checkpoint, so a resume starts with **zero** holdout rows. Measured across both of today's restarts on one trial: iter 1 = 210 rows, refilling to the 2000 cap only by **iter 14 (13 iterations, ~2h)**; iter 42, immediately after the 17:05 restart, = **194 rows**. Three consequences, all of which corrupt the ruler rather than merely delay it: **(a)** iterations 1–3 and 42 report `test_size 0` / `test_loss NaN` — **no holdout eval runs at all** until the buffer reaches `batch_size` (512); **(b)** iterations 4–13 evaluate against a *growing* set (659 → 1992 rows), so `test_loss` in that window is not comparable row-to-row; **(c)** `best_loss` is carried across the boundary unchanged (4.8982 at iter 41 → 4.8982 at iter 42) — a number measured against a ruler that no longer exists, which is then defended against a differently-constituted one. **Candidate artifact, not yet separated from real learning:** `best_loss` fell 5.1044 → 4.9215 over iters 4–7, exactly the fill window, i.e. precisely when the denominator was changing. This is a concrete mechanism for the long-standing "`best_loss` rises across restarts" puzzle. **Practical bound: `freeze_holdout_at` can never protect a ruler across a restart** — it arms on a wholly post-restart sample, and the next restart re-rolls it. Observed refill after the 17:05 restart: iter 42 = 194 rows, iter 43 = 847 (+653), so the 2000 cap and the freeze land around **iter 45–46**, not the ~iter 54 a flat 159-rows/iter estimate gives. The rate is not constant — it tracks ingested rows at `holdout_fraction 0.02`, and iter 43 ingested a post-restart surplus. **Fix (PR #261):** the holdout's rows are serialised into the Ray checkpoint as `holdout.npz` and its two scalars (`holdout_frozen`, `holdout_generation`) into the checkpoint's `trial_meta.json`, restored in `_init_replay_buffers`. The checkpoint rather than `_durable_trial_dir` because it is the only location that survives all three restart paths that actually happen — normal resume, `train.sh start` auto-resume, and **salvage restore**, which lands in a different trial dir; salvage copies a fixed file list out of the checkpoint, so the sidecar rides along by name. Cost measured on 2000 real production rows: 102.2 MB dense, **2.09 MB** compressed, 1.2s write / 0.9s read. Missing, corrupt, or wrong-shape (plane-count / policy-width) sidecars degrade to the old empty-and-refill and log; a restored `frozen` is honoured only when rows actually came back, and losing the rows of a checkpoint that had them bumps `holdout_generation`. **Re-read instrument, unchanged:** at the next restart, `test_replay` on the first new row should equal the pre-restart value and `test_loss` should be finite on iteration 1, instead of 0/NaN. Until that row exists this is a code fix, not a verification |
| G6 | `test_size` means the number of rows evaluated | `test_steps` × `batch_size` vs `holdout_capacity` | **FAILED 2026-07-26 (naming, low severity but exactly the house bug class).** `test_size` is set to `holdout_buf_size` (`trainable_report.py:856`), i.e. the **buffer** size, not the evaluated-row count. The eval is `trainer.eval_steps(holdout_buf, batch_size=tc.batch_size, steps=tc.test_steps)` = **5 × 512 = 2560 sampled draws from a ≤2000-row buffer**, drawn by the same random sampler as training — so it is a resample with replacement, not a pass over the set: some rows are scored several times and some not at all. The holdout metric therefore carries sampling noise *on top of* the composition churn in G4/G5, and `test_size: 2000` overstates how settled it is. **FIXED (PR #261)** — `test_size` now reports `test_steps × batch_size`, the rows the eval actually draws; the buffer size stays under the name that means it, `test_replay`, which `audit_realized_config.py` already reads. Chosen over a rename because Ray fixes the CSV header on row 1 and a resume appends without re-heading, so adding or removing a column would misalign every post-restart segment. **Historic rows still carry the old meaning** — before PR #261, `test_size == test_replay == holdout buffer size` |

| G7 | the window contains no duplicated rows | lexsort `(game_id, ply_index)` over every window shard; report extra rows / total | **FAILED 2026-07-26 — mechanism now established.** 20,513 extra rows = **1.368%** of 1,499,012 keyed rows, max multiplicity 16. **Two interleaved shard-index streams into ONE replay dir on 07-11 05:50–07:17** (origin trial `5fac4`): stream A 31387–31398, stream B 31401–31413, sharing rows at index offset 13/14/15 with mtimes matching to the second. `_shard_index` is seeded once by `_scan_existing_shards` then incremented locally, so a second writer **interleaves rather than collides**; the offset wanders because each writer draws its own 2% holdout split. **The leak into TODAY's frozen holdout is 0.18%, not 1.37%:** `_scan_existing_shards` never calls `_ingest_train_arrays`, so the salvage block enters the window with NO holdout split applied, and the current trial's own duplication is 89 extra rows / 681,118 local = 0.0131% ⇒ ~3.6 of 2000 frozen rows. Baked into `data/salvage/swap_512x16_20260711`, a designated revert point. **Detector merged (PR #266)**; row-level dedup judged NOT worth it — data-affecting, 0.013% live rate, and it would not have caught the 07-11 event (two processes, two buffers, no shared key set) |
| G8 | the window's content age matches what readouts assume | `stat().st_mtime` of every window shard, split by `is_symlink()` | **FAILED 2026-07-26 (re-measured; the mix is draining)** — 443 symlinks = **54.0%** of rows (809,310) aged 374.7–441.2h vs 377 local shards = 46.0% (689,232) aged 0.02–16.0h. **There is NO content between 16h and 374.7h**: the window is bimodal, two regimes, not a sliding window over one. At 23.7 shards/h the salvage block finishes evicting **2026-07-27 ~16:50**; full turnover 34.6h. Provenance confirmed from the run's own metadata, not mtimes: `trial_meta.json` carries `salvage_origin_dir`/`salvage_origin_used: true`. **Direction: it FAKES A POSITIVE for LEVEL-based readouts.** The frozen holdout was filled at iters 42–46 entirely from fresh ingest, so it is 100% local-regime while the training window is 54% old-regime heading to 0% — the training distribution converges onto the holdout's, and `test_loss` falls for compositional reasons alone. Any `test_loss` / `best_loss` / train-test-gap readout spanning 07-26 22:00 → 07-27 17:00 is confounded in the favourable direction. **Does NOT transfer to a variability yardstick** — the trend is −0.00115 nats/iter, ~3% of a 0.0708 mean |Δ| |
| G9 | eviction is FIFO by content age, not merely by shard index | mtime-vs-index monotonicity across the window | **VERIFIED 2026-07-26** — 0 inversions and 0 index gaps across all current-trial shards, so index order equals age order for a single writer. The one exception is a ~23-minute mis-ordering at the 07-11 double-write boundary (31399/31400 written 05:27 sitting after 31387–31398 written 05:50–07:17), which is immaterial |
| G10 | evicting a salvage-seeded (symlinked) shard must not touch the salvage pool | count shards in the pool vs links evicted from the live dir | **VERIFIED 2026-07-26 — currently safe, and worth an assertion precisely because it is one refactor away from catastrophe.** `delete_shard_path` (`shard.py:673`) special-cases `is_symlink()` and unlinks rather than `rmtree`-ing through. Measured: 280 links already evicted and `swap_512x16_20260711` still holds all **815** shards (`pre_durable_deploy_20260726` holds 809). A future change to a plain `shutil.rmtree` would destroy the designated revert point *while the run is using it* |
| G11 | window composition is stationary across the window's own span | per-age-decile `is_selfplay`, W/D/L, field presence | **FAILED 2026-07-26 (re-measured)** — oldest→newest deciles: `is_selfplay` 0.957 → 0.675, `wdl_draw` 0.259 → 0.334; `seed_id` and `opening_source_code` are absent from deciles 0–4 entirely, so no window-wide seeded fraction exists. Regime means: salvage `is_selfplay` 0.891 / draw 0.263, local 0.700 / 0.323. Implied drift as the salvage block drains with nothing in the loop changing: `is_selfplay` −10.4pp, draw +3.2pp, win −2.2pp. **Direction: variance-inflating, i.e. biases AGAINST a positive** — the opposite sign to G8's level confound, which is exactly why the two must be reported separately and never merged into one 'the window is drifting' caveat |
| G12 | a read-only tool cannot mutate the window it is reading | `DiskReplayBuffer.__init__` → `_scan_existing_shards` → `_enforce_window()` | **CODE-ONLY / HAZARD, not a defect** — constructing a `DiskReplayBuffer` against the live shard dir **deletes shards**, because `__init__` enforces the window. Any read-only probe must open zarr groups directly. Recorded so the next person auditing this stage does not destroy live data trying to measure it |
| G13 | the holdout is drawn from the same population as the training window | `PYTHONPATH=. python3 scripts/audit_replay_window.py` (G13 block) | **FAILED 2026-07-26 — NEW, and it is the leak that matters, not G7's 1.37%.** The split is per-**ROW** (`_ingest_train_arrays`: `rng.random(shard_n) < holdout_frac`), uniform over every ply of every game. Games contribute **17.8** recorded plies, so a holdout row has **22.6 expected same-game rows in the TRAINING set**, `P(no same-game sibling) = 7.2e-04`. And `wdl_target` is **constant within a game** — verified white-POV-canonical on **35,277 of 35,277** locally written games, modal-label share 1.0000. So the value label of essentially every holdout row is already in the training set on ~23 near-identical positions: `test_wdl_loss` measures memorisation, not generalisation. **`freeze_holdout_at: 2000` makes this PERMANENT** instead of rotating it out every ~13 iterations. Second failure of the same invariant: 0% of the holdout comes from the 54% of the window that is salvage-restored (G8). Fix is data-affecting — PRE-REG A owed, not a commit. **⚑ MEASURED 2026-07-27 (PRE-REG A discharged, `scripts/probe_holdout_split_leak.py` on `checkpoint_000122`) — the MECHANISM is confirmed and the MAGNITUDE on the value ruler is REFUTED.** Two runs: (a) `--set-a sidecar` (the literal frozen rows `test_wdl_loss` uses) gap **+0.0318 nats** [cluster 95% CI −0.0066 .. +0.0687] — *positive*, i.e. the leaky set scores HARDER than game-disjoint rows, the opposite sign to the leak hypothesis, and underpowered against the 0.05 bar because set A is frozen at 2000 rows and cannot be grown by compute; (b) `--set-a reconstruct`, the UPPER bound (rows the checkpoint literally trained on, so direct memorisation **+** the sibling leak) gap **−0.0056 nats** [cluster 95% CI **−0.0265 .. +0.0160**] — **the entire CI inside ±0.027, decisively under the pre-committed 0.05 threshold.** If rows actually trained on carry no `wdl_ce` advantage, rows merely holding same-game siblings cannot. Design effect 1.8× confirms the cluster-by-game bootstrap was load-bearing; an iid-row CI would have been ~1.8× too narrow. **The instrument is not null: the same comparison on `total` is −0.2982 nats [−0.3644 .. −0.2315]**, so the net memorises heavily — it just does not land in the value head, which fits `policy_target` being per-position while `wdl_target` is one coarse 3-class label shared across all ~18 plies of a game. **SELF-CORRECTION: the sentence above — "`test_wdl_loss` measures memorisation, not generalisation" — was inferred from a mechanism and a sibling count, never measured, and it is WRONG for the value head.** Same error class as the day's other four (clip RATE read as clip EFFECT; loader CAPACITY as throughput; a diff STAT as diff content; a `train_time_s` bar set while tripling the steps): a real number, asserted past what it could show. **What survives: the G8 leg** (0% of the holdout from the 54% salvage-restored window) is untouched by this probe and still FAILS. Confound not controlled: set B is drawn entirely from the newest shards (0–1.8h) against set A's 30.8h span, so recency could account for the (a) sign on its own. **Verdict: close the split-leak question; a per-game split buys nothing measurable for the value ruler and is NOT worth a data-affecting change.** G14 is the live problem, not G13 |
| G14 | the holdout eval is a PASS over the frozen set, not a resample of it | read `_run_holdout_evaluation` → `eval_steps` → `sample_batch_arrays`; sd of `test_loss` across the frozen span | **FAILED 2026-07-26 — NEW. Freezing the SET does not freeze the MEASUREMENT.** `eval_steps(holdout_buf, 512, steps=5)` is **2560 draws WITH REPLACEMENT from a ≤2000-row set**, and `sample_batch_arrays` additionally (a) WDL-**rebalances** each batch (`draw_cap_frac 0.90`, `wl_max_ratio 1.5`) so the eval's class mix is not the holdout's, and (b) draws **half of every batch ∝ `priority`**, because `holdout_buf.surprise_mix` is never assigned and keeps the class default 0.5. Measured ESS: **1051/2000 = 52.6%**. Measured noise floor on the frozen set, iters 48–61: **sd 0.0522 nats**, mean |Δ| 0.0708, trend −0.00115/iter. **This is what killed the freeze experiment** — the yardstick was measuring a resampling floor the freeze cannot touch (see the ledger's early-called FAILED). A deterministic unweighted full pass costs **4 batches instead of 5** and removes all three effects; PRE-REG B |
| G15 | a second process cannot write into the window dir undetected | `[disk_buf] CONCURRENT WRITER` in the training log; `tests/test_replay_disk_buffer.py` | **FAILED 2026-07-26, FIX MERGED BUT NOT LIVE (PR #266)** — held at FAILED on purpose, the same way G5/G6/J2 are: a code fix is not a measurement, and this one is read at trial construction so it cannot be true until the next restart. The silent mechanism behind G7 ran for 87 minutes on 07-11 and was found 15 days later. `DiskReplayBuffer` now takes an advisory `flock` on `<shard_dir>/.writer.lock` at its first shard write and logs the holder pid; **warn-only**, because refusing to write would turn a data-quality defect into an outage, and kernel-released so no stale lock can wedge a restart. The write path also steps over a shard index that already exists or is a symlink rather than overwriting it (the same-index variant destroys the other writer's rows, and writing through a dangling salvage link would land a shard inside the pool — the G10 hazard). Read at trial construction ⇒ inert until the next restart |

## H. Sampling

| # | invariant | instrument | status |
|---|---|---|---|
| H1 | realized views per ingested position matches config | `train_views_actual` | **VERIFIED 2026-07-26** — 2.52 vs 2.5 |
| H2 | priority/surprise weighting is actually non-uniform and safe | load `priority` from the newest shards; measure CV + mass concentration | **VERIFIED 2026-07-26** — CV **0.94**, 14,297 distinct values / 14,303 rows; top 10% of rows carry **31.5%** of priority mass (uniform = 10%), top 25% carry 57.1%. Sampling is 50% uniform / 50% priority (`surprise_mix`). `replay_sf_gap_priority_weight: 0` (experiment #104 killed), so the spread comes from `diff_focus` — which is therefore **not** inert as sampling pressure, whatever its Elo effect. Edge cases safe: 3 rows (0.021%) carry NEGATIVE priority, clamped by `np.maximum(pri, 0.0)` before use, with `nan_to_num` and a uniform fallback when the sum is non-positive |
| H4 | the sampler does not silently reweight the class mix | read `ArrayReplayBuffer.sample_batch_arrays` (`replay/buffer.py:232`), then measure live W/D/L and replay the clamp arithmetic | **VERIFIED-INERT 2026-07-26 — currently a no-op, but it is a live tripwire, not a dead branch.** Every training AND holdout batch is drawn with `wdl_balance=True` (the default; `_sample_batch_host` never passes `False`, so both paths agree — I4's train/holdout gap is *not* distorted by this). The rebalancer preserves the natural draw rate up to a 90% cap, then **clamps the win:loss ratio to 1.5:1**. Measured over 18,322 live rows from the 10 newest replay shards: **W 0.321 / D 0.321 / L 0.358**, natural **W:L = 0.895** — comfortably inside [0.667, 1.5], and a replayed 512-batch is `D=164 W=164 L=184`, **identical to the natural mix, clamp not fired**. **Why it still deserves an invariant:** the PID controller actively drives winrate, so the clamp's engagement is a *function of a knob we deliberately move*. If the net's row-level W:L ever leaves that band, the effective training distribution silently changes at the same moment the difficulty does, and the two effects would be inseparable after the fact. Re-measure this whenever winrate moves materially. Note the drift metrics deliberately sample with `wdl_balance=False` (`trainable_metrics.py:227`), so `data_wdl_balance` reports the natural mix while the losses are computed on the balanced one |
| H5 | sampling is uniform over the window, or its non-uniformity is documented | `_load_refresh_chunks` (`disk_buffer.py:820-822`) applied to measured shard ages and sizes | **FAILED 2026-07-26 — the window is not sampled uniformly and this is documented nowhere.** The shuffle refresh picks shards with `weights = np.arange(1, n_shards + 1); weights /= weights.sum()` — **linear in recency rank, so the newest shard is 820x more likely to be drawn than the oldest** (verified by reading the shipped line; there is no comment on it and no mention in any doc). Derived against measured ages: mean age of a trained row **166.1h vs 266.7h uniform**, median **10.4h vs 386.6h**; the newest 10% of shards carry 19.3% of sample weight and the oldest 50% carry 25.1%; the 07-11 era is 63.9% of rows but **40.6% of sample weight**. Kish **ESS = 1,120,141 of 1,498,953 rows (0.747)**, i.e. `replay_window_max: 1500000` behaves like a ~1.12M-row window. Checked and cleared one obvious follow-on: per-row weight is independent of shard size (a pick contributes all its rows), so the 36 short shards are not distorted — 0.00666 sample share vs 0.00663 row share. **Status caveat, stated plainly:** this is a derivation from the shipped selection rule applied to measured ages, not a read off the running sampler. Closing it properly needs one counter — an age-of-sampled-row histogram in `_apply_refresh_chunks` |
| H6 | a training batch never contains the same row twice | `data_unique_positions` in `progress.csv` | **VERIFIED-WITH-CAVEAT 2026-07-26** — it can, by design: `rng.choice(..., replace=True)` on both the uniform and priority halves. Measured over 44 iterations at n=256: unique fraction mean **0.99627**, min 0.98438, excess 0/2/4 rows per sample (28/11/5 iterations), mean 0.955 rows = **0.37%**. The analytic expectation for k=128 uniform + k=128 priority over a 25,000 pool at CV 0.94 is ≈1.6, same order — the mechanism fits the magnitude (method rule 3). Benign, but nothing states it is with-replacement. Caveat on the instrument: `data_unique_positions` samples n=256 with `wdl_balance=False`, so the real 512-row balanced batch is not directly instrumented |
| H7 | `priority` reflects the CURRENT model's surprise | grep for any write-back; per-shard mean priority vs recency rank | **FAILED (write-once) 2026-07-26, but no magnitude drift found** — `priority` is set at selfplay time (`network_turn.py:598`, `priority=float(difficulty)`), carried through finalize into the shard, and **never recomputed**; `_shape_shuffle_priority` only re-scales it at load. So with G8 in force, ~40% of sampled rows carry a surprise score computed by the **15-day-old 07-11 net**. The reassuring half: priority shows no age bias — spearman vs recency rank **−0.074** (p=0.033), decile means flat at 8.06–8.27, window CV 0.949 (matches H2's 0.94), 208 negatives (0.014%) all clamped. So the levels are fine. **Whether the RANKING still holds is OPEN** and needs GPU: score the salvage-era rows with the live net and correlate against stored `priority` |
| H8 | eviction under an in-flight prefetch degrades safely | `grep '\[disk_buf\]' /tmp/chess_training.log` | **VERIFIED 2026-07-26** — 25 `enforce_window` events and **0 warnings** of any kind: no `vanished`/`failed` alarm, no empty-refresh streak, no `BUG:` line. Structurally sound too: `_enforce_window` pops the record under `_prefetch_lock` before deleting, so a racing load is classified `vanished` (benign) rather than `failed`, and the sampler holds decoded arrays rather than file handles so it cannot be pulled out from under. This is F3/H3's alarm staying quiet on a path that is *exercised* — 25 real evictions, not zero opportunities |
| H3 | shuffle refresh does not drop shards silently | same instrument as F3 (`_refresh_failed_total` / `_refresh_vanished_total`) | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — the counter split distinguishes a genuine tracked-shard fault from the benign vanish race, both under the prefetch lock so concurrent sampling/prefetch threads cannot lose counts. Zero of either in production |

## I. Training step

| # | invariant | instrument | status |
|---|---|---|---|
| I1 | realized LR == configured, including after warm start | `param_group_lrs` / `progress.csv` `lr` | **VERIFIED 2026-07-26** — 3e-5; warm-start guard silent |
| I2 | the warm-start LR guard fires on a regime mismatch | `guard_warm_start_lr` | **VERIFIED 2026-07-26** — it caught the `pb2_bounds_lr` override |
| I3 | every head receives gradient (none silently dead) | `scripts/probe_head_grad_share.py` | **VERIFIED 2026-07-26 (measured, was the last OPEN item)** — no dead heads: all 11 loss components produce non-zero trunk gradient. Weighted trunk-grad share: POLICY (policy_ce+soft+future+sf_move) **65.09%**, VALUE (blended_wdl+sf_eval+categorical) **15.51%**, OTHER (volatility+sf_volatility+moves_left) **0.02%**; headline policy_ce:blended_wdl_ce = **5.477x**. `volatility`/`sf_volatility`/`moves_left` are near-inert at ~0.01% each but genuinely non-zero, so inert-by-weight, not dead. **GROUPING FIXED 2026-07-26 (PR: honest wdl_loss naming).** The original three groups summed to 80.62% because the probe put the diagnostic `wdl_ce` (19.38%) in the weighted denominator with weight `w_wdl` but in no group. It carries no gradient into the update (I7), so it is now excluded from the denominator entirely and reported in a separate DIAGNOSTIC block; the two zero-weight own-move terms (`sf_own_ce`, `sf_own_regret`) joined POLICY so the trained components are exactly partitioned, and the probe raises if they ever stop summing to 100%. **Renormalizing the recorded measurement over the trained components only: POLICY 80.74%, VALUE 19.24%, OTHER 0.02%, sum 100.00%. Use 19.2% as the value share when reasoning about `w_wdl`** — not 15.51% (wrong denominator) and not ~34.9% (counts a diagnostic as gradient). The raw per-component norms and the 5.477x headline are unchanged; only the denominator moved ([[value_head_trunk_gradient_share]]) |
| I4 | train/holdout gap is not widening | `policy_loss` vs `test_policy_loss` | **VERIFIED** (views readout: 0.13–0.18 flat) — but see G4, the holdout ruler moves |
| I6 | the three blend fractions sum to 1, the residual sits on the game outcome, and all three share one POV | realized `sf_wdl_frac` from `progress.csv` + offline reconstruction of the target over the same rows | **VERIFIED 2026-07-26** — `game_frac = 1 − sf − search` (`losses.py:424`); reconstructed targets sum to **1.000000** on all 73,250 rows, argmax equals `wdl_target` on **95.8%**, mean entropy 0.713 nats. Fallback-to-outcome rate is 0.41% for the SF part and 0.00% for search. `use_adjusted_wdl_target: false` and both `sf_search_dampen_*: 0.0`, confirmed live. Realized split is game 0.35 / SF 0.45 / search 0.20 — see the correction in I5 |
| I7 | the reported `wdl_loss` is the loss actually optimized | `losses.py` (`w_wdl · m_blended_wdl`) vs the `wdl_loss` column | **FAILED-THEN-FIXED 2026-07-26.** *Fix:* `compute_loss` now returns the blended soft CE — the tensor `total` is built from — under BOTH `wdl_ce` and `blended_wdl_ce`, so the `wdl_loss` column, `test_wdl_loss`, and the `wdl_loss_{selfplay,curriculum,open,mid,end}` splits all report the trained loss. The one-hot diagnostic survives under its own name, `wdl_onehot_ce` → `wdl_onehot_loss` / `test_wdl_onehot_loss`. **Column discontinuity: `wdl_loss` steps up by the ~0.026 gap at the deploy** (iter 42: 0.7599 → 0.7859) — it is a change of quantity, not of the net. Best-model selection reads `test_metrics.loss` (the total), never a per-head column, so nothing in selection moved; `blended_wdl_loss` keeps its old meaning and value. *Original finding:* **the metric named `wdl_loss` was not the trained loss.** The optimizer sees `w_wdl · m_blended_wdl` (the blended soft target). What gets *reported* as `wdl_loss` is `wdl_ce` (`losses.py:346`), a hard one-hot cross-entropy computed for diagnostics only. They differ on live rows: iter 42 `wdl_loss` **0.7599** vs `blended_wdl_loss` **0.7859**, and the gap is stable across iters 35–42. Everything derived from the wrong one inherits the defect: `wdl_loss_selfplay`, `wdl_loss_curriculum`, `wdl_loss_open/mid/end`, and **`test_wdl_loss`** — so the holdout's value column tracks a number no gradient ever came from. **This also re-frames the I3 caveat:** I3 flagged that `wdl_ce` (19.38% of trunk gradient) sat in no group, leaving "value = 15.51% or ~34.9%" unresolved. `wdl_ce` is diagnostic and carries no gradient into the update, so the grouping question and this one are the same defect seen from two ends. Resolve them together before using either number to set `w_wdl` |
| I8 | the blended target's draw mass tracks the realized draw rate | offline reconstruction, marginal and conditioned on outcome | **OPEN — measured, but no invariant was pre-committed, so this is a reading and not a verdict.** The blended target is **5.5 pp less drawish than reality**: draw mass 0.2658 vs a true draw rate of 0.3212. The cause is structural rather than a bug — the cp-logistic label caps its D component at **0.345** (slope 0.006, `draw_width` 120), so `sf_wdl` contributes 0.224 of draw mass and `search_wdl` 0.264. On rows from games that actually ended drawn, the target is `[W 0.189, D 0.551, L 0.260]`. **This needs an operator decision before it becomes an invariant:** deliberately under-weighting draws is a defensible anti-engine choice (draws are the failure mode being attacked), but nothing on record says it was chosen. Do not "fix" it without that call |
| I9 | grad-norm and clip rate are visible to the metric stream | `grep grad_norm/zclip` in `progress.csv` / `result.json` | **FAILED-THEN-FIXED 2026-07-26 (PR #260).** `grad_norm_{mean,median,p95,max}`, `grad_{clip,adaptive_clip,hard_clip}_rate` and `grad_norm_samples` are now per-iteration Ray metrics, aggregated over EVERY optimizer step (not the tb_log_interval subsample), so they land in `result.json`/`progress.csv` and survive restarts. Original finding: they were emitted **only** to TensorBoard (`train/grad_norm`, `zclip/{total_norm,effective_clip,clipped,hard_clipped,adaptive_clipped}`), every `tb_log_interval` = 10 steps (a constructor default; the key is in neither the yaml nor `trainer_kwargs_from_config`). There is no clip or grad-norm column in `progress.csv`, so `audit_realized_config.py` cannot gate on them and no ledger yardstick can cite them. Worse for an audit: TB event files rotate per Ray session, so **the history dies at every restart** — the very boundary G5 shows we most need to see across |
| I10 | the hard clip `zclip_max_norm` is not an LR cap in disguise | TB `zclip/*`, 233 samples, steps 56000–58320 | **VERIFIED 2026-07-26, WITH A DRIFT WATCH** — 5.15% of sampled steps clipped (adaptive 4.29%, hard 1.29%), mean applied shrink 0.9969, median shrink exactly 1.0. This is a tail guard, not a cap, which is the intended state after the 1.0 → 5.0 change. **But see I11: it is drifting toward being one.** Caveat on the measurement: it is a 1-in-10 sample, unbiased with respect to step index but unable to rule out a burst pattern aligned to the 10-step stride |
| I11 | the yaml's own justification for `zclip_max_norm: 5.0` still matches reality | same TB series vs the comment at the key | **FAILED 2026-07-26 23:5x — the threshold FIRED; #260 makes this observable, it does not make it true.** An earlier revision of this row read FAILED-THEN-FIXED off PR #260. That is wrong: #260 fixes **I9** (grad norm and clip rate become visible in the metric stream). I11 asks whether the yaml's stated justification still holds, and measured from live TB (trial 13a9f, `zclip/total_norm` + `zclip/hard_clipped`, 376 samples, steps 56000-59750) it does not: full-window median **4.468** / hard-clip **13.0%**; last-100 median **4.901** / **40.0%**; **last-50 median 5.055 — above the cap itself — with a 54.0% HARD-clip rate.** 250-step bucket medians climb near-monotonically 3.956 → 5.094 over ~3.7k steps. The comment at `configs/pbt2_small.yaml:411` justifies 5.0 by *"raw grad norms median ~2.2"* and implies the hard cap is inert — stale by ~2.3× and false. **This is a recurrence of the exact regime the 1.0 → 5.0 raise was made to escape**, so the realized LR is being damped by an unmeasured, time-varying factor on the very run that exists to read out 3e-5. Candidate cause with a matching ~17h timescale, NOT established: the G8 window drain. Pre-registered in the ledger: wait for the drain to complete, re-read, and only then decide — do not move the knob while the suspected cause is still running. **UPDATE 2026-07-27 — the pre-registered cause DOES NOT EXIST and the shape above was mis-read twice.** (a) *No drain:* the replay window sat pinned at its 1.5M cap on every iteration of the window, so there was nothing to wait for; the pre-registration was void from the moment it was written. (b) *The yaml's `~2.2` is stale by LINEAGE, not by drift.* Full TB reconstruction across all three surviving trials (`5fac4` 384x12, `4c17c` 512x16 @3e-4, `13a9f` 512x16 @3e-5; `train/grad_norm` and `zclip/total_norm` are byte-identical, one quantity logged twice) shows the 384x12 net held median **2.36-2.50, flat for the entire 20 days measurable** (06-21 to 07-11, zero drift). Today's 5.4 is a SUM: **+1.3 instantaneous at the 512x16 swap (07-11)**, **+0.4 instantaneous at the boot512 restart (07-26)**, and **+1.25 continuous inside the live trial**. Step counters reset per trial; these series must never be spliced. (c) *A short window fitted across a level shift is not a trend.* Read over iters 81-99 alone the series looks flat then steppy; over the trial's 525 logged points the decile medians climb monotonically 4.08 -> 5.30 and the last third is still rising (+0.181/1000 steps, t=+2.7). The ramp is real, decelerating, and NOT restart-driven — the two restarts inside `13a9f`, including the six-PR deploy at 01:58, each show <0.08 median change across the gap. **Best-supported cause, correlational not causal: the 10x LR cut.** The one clean natural experiment in the record is `5fac4` 07-01 to 07-03, where the flat-lower-LR experiment moved matrix LR 6e-3 -> 2e-3 and back with nothing else changing: median grad norm went 2.49 -> 4.64 -> 2.75, tracking LR INVERSELY and recovering fully on revert. The live trial runs at 10x lower base LR (3e-5, intentional — it matches the donor's regime, per [[warm_start_lr_regime_destroys_net]]) than either predecessor. This does not explain the monotone ramp, since LR has been constant throughout it. **Ruled out as causes:** parameter growth (||theta|| +0.09% over 2,824 steps), any single loss head (across the iter-96 shift every train head moved <0.5%, |d| <= 0.34, while grad norm moved +4.0%, d=4.04), and the #260/#262 deploy (the ramp predates it). **So the instability reading is weak — weights, every train head, and the frozen holdout are all flat — but the knob is still mis-set:** at median 5.4 against a cap of 5.0 the hard cap binds on 88% of steps, which makes it the step-size controller, not the airbag it was raised to 5.0 to be. Historically-inert ratio was cap/median ~2.1 (384x12: 2.4 median, 5.0 cap, 0.000 hard-clip) |
| I12 | `matrix_optimizer_scope` still covers the tensors it was written for | `_matrix_optimizer_filter("mlp_out")` over the live `arch`, cross-checked against the checkpoint's `param_groups` | **VERIFIED-BUT-NARROW 2026-07-26** — the pattern is **not** stale (the `diff_focus` failure mode does not repeat here): it matches exactly `blocks.N.ffn.N.weight` + `blocks.N.out_proj.weight`, 48 tensors, and the live group sizes `[48, 0, 143, 290]` agree. What the name hides is coverage: **18,033,664 of 63,084,128 trainable params = 28.6%**. The two largest excluded blocks are `layer_smolgens` (26.7M, 42.3%) and `q/k/v_proj` (12.6M, 19.9%) — both run on AdamW at 1/20 the matrix LR. "Aurora is the production optimizer" is true of under a third of the weights |
| I13 | per-group `weight_decay` equals the configured value | `checkpoint_0000NN/trainer.pt` → `opt.param_groups` vs the yaml | **FAILED-THEN-FIXED 2026-07-26 (PR #260).** `Trainer.load()` now re-applies the construction-time snapshot of every config-derived param-group key (`_reapply_configured_param_group_hparams`), with an explicit exception list for the keys the checkpoint legitimately owns (`lr`, `initial_lr`, SODA's `soda_k`) — so the class is fixed, not just `weight_decay`: `aurora_uw_floor` and the SODA markers had the same exposure. **The live value was NOT changed:** the yaml now reads `matrix_weight_decay: 0.0001`, which is what has actually been running, so turning the control surface back on is numerics-neutral and any future change to it is a real, ledger-worthy experiment. Original finding: **a config key that has been decorative for at least seven weeks.** The yaml says `matrix_weight_decay: 0`; the live Aurora group carries **`weight_decay: 1e-4`**. Verified directly on `checkpoint_000042`: `grp0 n=48 lr=6e-05 weight_decay=0.0001` while `grp3 weight_decay=0.0` and `grp2 weight_decay=0.0001` (that one matches its configured `aux_weight_decay`). **Mechanism:** `Trainer.load()` calls `self.opt.load_state_dict(ckpt["opt"])`, and torch's `Optimizer.load_state_dict` overwrites **every** group hyperparameter from the checkpoint. `lr` survives because `set_peak_lr` re-applies it afterwards, and the `aurora_*` polar knobs survive because `_apply_lr_gamma_weights` re-pushes them every iteration. **`weight_decay` is the one group hyperparameter with no re-application path**, so once a value is stamped into an optimizer state it propagates through every resume forever and any yaml edit is a permanent silent no-op. Bracketed by surviving checkpoints to between 2026-06-04 (46M-era pool: 0.0) and 2026-07-08 (`pre_v2feed_ckpt678`: 1e-4); a fitting but unproven origin is `scripts/offline_replay_epoch.py --matrix-weight-decay`, which **defaults to 1e-4**, run during the 512×16 offline recovery. **Report it as a broken control surface, not as damage:** decoupled decay at the matrix group's LR is ~6e-8/step, ≈0.35% total shrink over 58k steps. The comment at `trainable_config_ops.py:371` reasons that `matrix_weight_decay` was safe to leave out of `_OPTIMIZER_CONSTRUCTION_KEYS` because changing it on resume is "as safe as changing `lr`". Safe, yes. Effective, no — and that comment is the trap |
| I14 | no gain / norm / bias parameter carries weight decay | `_split_decay_groups` rule (`ndim<=1 or endswith(".bias")`) over all 481 live params | **FAILED-COSMETICALLY 2026-07-26** — clean for every LayerNorm, every bias, and all four `policy_*.log_temp` (290 tensors / 302,784 params in the wd=0 group). One exception: `smolgen_layer_head_scale`, an `nn.Parameter(torch.ones(16,16))` — a multiplicative gain that is stored 2-D and therefore fails a rule keyed on rank, landing in the wd=1e-4 group. **Stated honestly: 256 params, per-step shrink `lr·wd` = 3e-10, ≈1.7e-5 relative over 58k steps. Structurally wrong, numerically irrelevant.** Recorded because the rule is rank-based and the next 2-D gain someone adds inherits the same bug at whatever size it happens to be |
| I15 | rows lacking a target do not dilute that head's loss denominator | `masked_mean` semantics + a CPU probe + live per-head coverage | **VERIFIED 2026-07-26 — the audit's central dilution bug class does NOT apply here.** `masked_mean(x, m) = (x*m).sum() / m.sum().clamp_min(1)`: the denominator counts *present* rows, not batch rows. Probed by halving `has_policy_soft` coverage — `soft_policy_ce` moved 3.4468 → 3.5013 (ratio 1.016), not the 0.5 a batch-size denominator would give. Live coverage: policy / soft / search_wdl / moves_left / categorical / legal_mask **1.0000**; sf_wdl / sf_move / sf_policy 0.9952; future 0.9796; volatility 0.9371; **sf_volatility 0.2317**. Even at 23% coverage a `w_*` still means "weight on the mean over labelled rows", which is what it reads like |
| I16 | illegal moves are masked before the log-softmax | CPU probe against `compute_loss` | **VERIFIED 2026-07-26** — `policy_ce` is bit-identical (3.4409165382) with an illegal logit set to −20 / 0 / 20 / 100 / **1e4**, in fp32 and under bf16 autocast. Reduction is sum-over-actions then **mean over rows** (3.630260705947876 measured both ways; a per-legal-move denominator would have given 0.1815). One residual hazard, dormant: `apply_policy_mask_to_logits` disables masking entirely for rows with `has_legal_mask == 0` (probe: CE 7.94 → 99.91). Live `has_legal_mask` coverage is 1.0000 over 36,339 rows, so it never fires — but nothing asserts that |
| I17 | configured `w_*` equals applied `w_*` (no hidden rescale, warmup or anneal) | `progress.csv` `w_*` columns, 42/42 rows | **VERIFIED 2026-07-26** — no annealing mechanism exists; all 12 `TRAINER_WEIGHT_KEYS` are re-pushed verbatim every iteration. Reported values are constant and match the yaml: `w_wdl` 1.0, `w_soft` 1.0, `w_categorical` 0.3, `w_sf_move` 0.02. **Coverage gap worth closing:** only 4 of the 12 are reported at all — `w_policy`, `w_future`, `w_sf_eval`, `w_volatility`, `w_sf_volatility`, `w_moves_left`, `w_sf_own`, `w_sf_own_regret` have no metric, so for those this invariant is CODE-ONLY by omission |
| I18 | effective batch equals config, and the LR regime assumes the same number | `train_samples_seen / trainer_steps_done`, 42/42 rows | **VERIFIED 2026-07-26** — exactly **512.0** on every row. `accum_steps: 1`, and the loss division by `accum_steps` is a no-op. The yaml comment "`accum_steps: 1 # …effective batch = 256`" is stale by 2×, comment only |
| I19 | the reported `lr` is the LR the trunk actually trains at | `progress.csv` `lr` / `peak_lr` / `opt_lr_mean` vs TB `train/lr` | **VERIFIED-WITH-CAVEAT 2026-07-26; THE REPORTING IS NOW FIXED (PR #260)** — `opt_lr` is renamed `opt_lr_final` (explicitly the trough) and two honest columns are added: `opt_lr_mean` and `opt_lr_max`, the iteration mean/max of the matrix group's LR sampled at every `opt.step()`. `status.csv`'s `lr` column is now `lr_mean`. Every group shares the schedule's scale factor, so the aux groups' means are `opt_lr_mean / matrix_lr_multiplier`. Original measurement, **which qualifies I1 (that measured only the aux group)**: `peak_lr` 3e-5 is the AdamW/aux base. The Aurora matrix group runs at `matrix_lr_multiplier` 20, base **6e-4**, and within each iteration the `sqrt_release` window cycles it 6e-4 → 6e-5 (`min_scale` 0.1, `start_frac` 0.8). Measured mean `train/lr` = **5.306e-4**, mean/max 0.8843 against the 0.88 the duty cycle predicts. **`opt_lr` reads 6e-5 on all 42 rows because reporting happens at the end of the release ramp — it is the trough, not the operating LR**, and the checkpoint confirms it (`grp0 lr=6e-05`, `grp1-3 lr=3e-06`, i.e. the aux group caught at 0.1× its own 3e-5 too). Anyone reading `opt_lr` as "the learning rate" is reading a number ~9× below the mean the matrix group actually sees |
| I21 | `zclip_max_norm` can actually reach the weights it is argued about | `_polar_factor(cG)` vs `_polar_factor(G)` on a 512x512 gradient, c = the live clip factor | **FAILED-BY-IRRELEVANCE 2026-07-27 — the knob does not reach 28.6% of the net AT ALL, and the rest only second-order.** Aurora is Muon-family: `_aurora_update` takes the **polar factor** of the gradient via Newton-Schulz (`aurora.py:53-134`), and the polar factor is **scale-invariant**. A global-norm clip scales every gradient by one constant `c`, so the matrix update is unchanged. Measured: `max|polar(cG) - polar(G)|` = **1.6e-07 at c=0.90** (the live clip factor at median 5.54 against cap 5.0), **0.0 at c=0.50**, **1.8e-07 at c=0.10** — floating-point noise even at a 10x clip. The `matrix_optimizer_scope: mlp_out` group is **18,033,664 of 63,084,128 params = 28.6%** (I12), and for every one of them `zclip_max_norm` is a **no-op**. For the AdamW groups (71.4%) Adam's `m/sqrt(v)` is itself asymptotically invariant to a CONSTANT rescale, so only the step-to-step VARIATION in `c` (live range ~0.7-1.0) leaves a residual, and it is second-order. **This retires the framing both sides of the I11 argument were using.** The claim that a 95% hard-clip rate means 'the fixed cap is setting the step size rather than bounding outliers' is FALSE as stated: the clip rate is a real signal about the GRADIENT regime, but it is not throttling the trunk update. Equally, 'the cap is the only brake on the instability' is false — it brakes nothing in the matrix group. **AMENDED SAME DAY -- the 'low-reward' half is WRONG, and the error is instructive.** I established scale-invariance for the Aurora group and then generalised to the whole network WITHOUT measuring how the global norm splits between the groups. Measured on `checkpoint_000122`, 8 real batches through the real loss (batch 128; live is 512, so absolute norms are not comparable across batch size -- the SPLIT is the quantity relied on): **||g|| aurora 3.648, ||g|| adamw 12.137, global 12.673 -- the AdamW groups carry 91.7% of the global norm^2 while holding 71.4% of the params.** The scale-invariant group carries only **8.3%** of the norm. **So the clip's action is ~entirely on AdamW, the group where clipping CAN do something, and 'the cap is nearly inert' does not follow from 'the cap is inert for Aurora'.** Third instance today of quoting a number without checking what it was capable of showing (cf. the clip-rate-as-clip-effect and loader-capacity-as-throughput errors). **What survives:** raising 5.0 -> 6.5 is still LOW-RISK (Aurora unaffected by construction; AdamW's `m/sqrt(v)` cancels a slowly-varying rescale, so a slow ramp is largely self-normalised away). **What does not:** it is NOT low-reward-because-inert. The cap is load-bearing as **spike protection on Adam's second moment `v`** -- one outlier inflates `v` and suppresses that group's updates for ~1/(1-beta2) ~ 1000 steps, which IS a mechanism that breaks training. **ACTIONABLE, measured:** an AdamW-only cap at the same numeric 5.0 yields effective norm 5.0 against the current global-cap effect of 4.79 -- nearly identical, because AdamW dominates the norm. **Restricting the clip to the AdamW param groups is therefore a SEMANTICS fix that leaves behaviour roughly where it is**, not a behaviour change needing large recalibration, and it makes `grad_hard_clip_rate` mean what its name says. **NOT established:** whether the AdamW ramp is harming training at all -- the self-normalisation argument says a slow drift is absorbed, and that argument is reasoning, not measurement. The instrument that would settle it is a per-group grad-norm metric logged every iteration (I9-style), which does not exist. The grad-norm ramp remains worth explaining as a regime signal (I11); the CAP is not the lever. **Method rule: before arguing about a knob, check that it reaches the parameters — a scale-invariant optimizer makes gradient-magnitude controls inert, and the clip-rate metric goes on reporting a large number either way.** |
| I20 | `zclip_max_norm` is a LIVE control surface, not a decorative one | edit the key in the live yaml, then read the next iteration's `grad_hard_clip_rate` | **CODE-ONLY 2026-07-27 (fix shipped; this row's own instrument NOT yet exercised).** The defect and the fix are argued from source plus mutation-tested unit tests. Nobody has yet edited the key in the live yaml and read the next iteration's `grad_hard_clip_rate`, because the value is deliberately pinned at 5.0 pending the I11 decision, so by this table's own rule the row is OPEN for decisions until that edit happens — which it will, the first time the cap moves. `ZClip` reads `max_grad_norm` once in its constructor and nothing pushed it afterwards, so a live yaml edit was a **silent no-op** — and worse than the `lr_schedule` case, which at least logs `requires restart — skipping`: `zclip_max_norm` is in NONE of the restart-required sets, so `_reload_yaml_into_config` overlaid it into the config every iteration and the operator got no signal at all. Fourth instance of the 2026-07-26 dead-key class (`soft_policy_temp`, `diff_focus_*`, `matrix_weight_decay`), and the one that mattered most, because I11 had just fired and the remedy was unreachable without a restart. *Fix:* `Trainer.set_grad_clip_max_norm` re-points the live cap and returns whether it changed; `trainable.py` pushes it each iteration beside the existing `buf.diff_focus_*` pushes and logs only on transition. The cap is a stateless per-step threshold, so re-pointing mid-run is safe and lands on the next optimizer step. **Consequence for I11: re-setting the cap is now a yaml edit that reverts as fast as it applies, not a restart** — which is what makes the open experiment cheap enough to run and back out. Regression cover: `tests/test_zclip_live_reload.py`, **verified failable by four independent mutations** — a setter that does not re-point the cap, a call site that regrows a bare `config.get`, the push deleted outright, and a resolver that drops the `grad_clip` alias each kill tests. **Pre-merge review caught the fix reproducing its own bug class:** the first revision pushed `config.get("zclip_max_norm")` while the constructor resolved `config.get("zclip_max_norm", config.get("grad_clip", 1.0))`, so any config setting only the `grad_clip` alias (`configs/default.yaml`, or a bare CLI run) would have had its hard cap **switched off at iteration 1** and logged as though the operator asked for it. Production was never exposed — `pbt2_small.yaml` sets the key — which is exactly why only a review caught it. Both sites now resolve through one exported `resolve_zclip_max_norm`. The setter also refuses non-finite and `<= 0` caps and keeps the current value: zclip's `clip_coef = cap / (norm + 1e-6)` is unclamped, so a live-yaml `0` zeroes every gradient and a negative cap flips the sign into gradient ASCENT, within one iteration now that the surface is live |
| I5 | WDL blend's SF component is non-zero | **`sf_wdl_frac` in `progress.csv` / `result.json`** — NOT the yaml | **VERIFIED 2026-07-26, but the number in the first version of this row was WRONG — corrected same day.** The invariant holds: the load-bearing SF component is intact and floored (`w_wdl 1.0`, nothing zeroed). **The correction: the realized fraction is 0.45, not the 0.50 this row originally claimed.** `_dynamic_sf_wdl_weight` interpolates from `sf_wdl_frac` (0.50) at `sf_pid_wdl_regret_max` (0.70) down to `sf_wdl_frac_floor` (0.45) at `sf_wdl_floor_at_regret` (0.10). Live `wdl_regret` is **0.0875**, below the knee, so the weight is pinned at the **floor**: `sf_wdl_frac = 0.45` on every one of the last 8 rows. The live split is **game 0.35 / SF 0.45 / search 0.20**, and the yaml's 0.50 has not been in effect at any point in this run. **Why this row was wrong is the point:** it was labelled "measured" but the instrument was "live yaml + trial realized params" — both of which are *inputs* to the resolution, not the resolved value, and the resolution is regret-dependent so it moves without any config changing. The audit's own bug class, inside the audit. `search_wdl_frac 0.20` is live-synced via `TRAINER_WEIGHT_KEYS` but is **reported nowhere**, so it stays CODE-ONLY |

## J. Checkpoint + best-model

| # | invariant | instrument | status |
|---|---|---|---|
| J1 | best-model never compares two different rulers | `best.json` `source` field | **VERIFIED** (PR #244) |
| J2 | best-model's ruler is itself stable | see G4 / G5 | **FAILED 2026-07-26 → CODE FIX MERGED (PR #261); re-read owed.** Inherited from G4/G5. Two halves: the ruler now survives a restart (G5), and `holdout_generation` is now **compared on** rather than merely recorded — a generation change triggers the same handover as a train→holdout change (adopt, don't compare), so `best_loss` can no longer defend a number earned on a set that no longer exists. That comparison was previously unsafe *because* the counter reset to 0 at every process start; making it durable is what unlocked it. A `best.json` with no recorded generation reads as unknown, not zero. Still owed: confirm across a real restart that `best_loss` is neither reset nor silently carried |
| J3 | a resume never overwrites live checkpoints | checkpoint-index ratchet | **VERIFIED** (PR #241) |
| J4 | `salvage-export` captures CURRENT state, not best-metric | the export's own printed iter vs live `result.json` | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — a real pre-restart export with `--metric training_iteration` printed `iter=41 ckpt=checkpoint_000040 (newest on disk: checkpoint_000040)` while live `result.json` read iter 41 on a 4.1-min-old row. Guard satisfied end to end on a pool that is now a live revert point |
| J5 | the source you read a "realized" config from is actually realized | `params.json` vs the live yaml vs `audit_realized_config.py` | **FAILED 2026-07-26 — `params.json` is the LAUNCH config, not the realized one.** Measured on the live trial: `params.json` reported `opening_fen_dole_per_iter = 1` and `opening_fen_list_path = ...retire_307.txt` while the live yaml — which is re-read every iteration and live-reloaded — held `0` and `retire_35`. Ray writes `params.json` once at trial construction, so for every live-reloadable key it is a snapshot of history wearing the name of current state. **This is the same bug class as the whole audit: a number that does not mean what its name says.** Rule: for a live-reloadable key read the live yaml (or `audit_realized_config.py`); `params.json` is only authoritative for keys fixed at construction. `scripts/loop_health.py` was fixed to consult the live yaml for exactly this reason |
| J6 | a config change you deployed is actually in effect | the startup banner / first new result row vs the yaml | **FAILED-THEN-FIXED 2026-07-26.** Setting `freeze_holdout_at: 0 -> 2000` in the live yaml and restarting **silently no-opped**: the resume restored the trial's config from Ray's `experiment_state`, and the startup banner still printed `0`. The ledger's PBT-pinned procedure covers this, but it is filed under *PB2-searched* keys and `freeze_holdout_at` is not one — **the resume preserves the whole trial config, so ANY construction-time key is exposed**, not just searched ones. Fix: atomic, parse-verified edit of the newest-BY-FILENAME `experiment_state`, patching `/config` and `/_Trial__unresolved_config` only and leaving `/last_result` alone (that is history, not state). Re-verified: banner reads 2000. **The generalisable rule is step 5 of that procedure — verify the deployed value on the first new row.** It is the only thing standing between a pre-registered experiment and a verdict read off a change that never happened |

| J7 | a trainer checkpoint round-trips losslessly | offline `Trainer.save` → `Trainer.load` bit-compare (`scratchpad/trainer_roundtrip.py`) | **VERIFIED 2026-07-26** — 0/86 weight tensors and 0/344 optimizer tensors differ; `step`, `peak_lr`, scheduler dict and `arch` all exact. Repeated on the **live** 685 MB `checkpoint_000042/trainer.pt`: **0/496** model tensors and **0/914** optimizer-state tensors differ |
| J8 | the compile-prefix strip works in BOTH directions | compiled-save → uncompiled-load, and the reverse | **VERIFIED 2026-07-26** — 0/86 differ both ways, optimizer and scheduler intact. **The 2026-04-27 model-destroying failure mode is closed** for `Trainer.save`/`Trainer.load`. That incident is the reason the strip exists, and this is the first time anyone has actually tested the direction it did not fail in |
| J9 | the PUBLISH path uses the same key convention as `save()` | key prefixes in `publish/latest_model.pt` vs `checkpoint_*/trainer.pt` | **FAILED 2026-07-26 — asymmetric.** `save()` strips `_orig_mod.`; `export_swa()` does not. Measured live: the published file carries the prefix on **496/496** keys, the checkpoint on **0/496**. Safe today only because every in-tree consumer routes through `load_state_dict_tolerant`. A plain `load_state_dict(pub, strict=False)` on that file returns `missing=86, unexpected=86` and leaves **0/86 tensors correct, silently** (measured in a repro) — i.e. a fresh-init model that loads without error. One line (`_strip_compile_prefix` in `export_swa`) closes it |
| J10 | the published model is the trained model, not an average | `swa_start` | **VERIFIED 2026-07-26, with a latent divergence worth knowing before anyone enables SWA** — `swa_start: -1`, `'swa_model'` absent from the live checkpoint, so `export_swa` emits `self.model`. But with SWA ON, `export_swa` would emit `_swa_model.module` while the ratchet arenas the raw model out of `checkpoint_*/trainer.pt` — measured in repro, those differ in **86/86** tensors. **Enabling SWA would silently make the strength ruler measure a different net than the workers play** |
| J11 | the model's parameter count is what the docs say | `named_parameters` vs `state_dict` on the production arch | **FAILED 2026-07-26 — independently reproduced (see A6).** 78.8M is a `state_dict` element sum counting one shared smolgen generator 16 times; distinct trainable params = **63,084,128**, and 63,084,128 + 15×1,048,576 = **78,812,768** exactly. `layer_smolgens` trainable is **26.7M (42.3%)**, not "42.4M of 78.8M (54%)". `blocks` 30.70M is right. Note `torch.save` dedups the storage, so the *file* is honest (≈63.08M fp32 worth of bytes) — only the documented count is wrong |
| J12 | a checkpoint named as a revert point still exists | `ls data/salvage/` | **VERIFIED 2026-07-26** — all four ledger revert points present: `pre_audit_window_20260726` (3.5G), `pre_durable_deploy_20260726` (3.8G), `pre_aot_deploy_20260714` (3.5G), `prechange_20260702_ckpt479` (2.3G), plus 21 pools in `data/best_pools/`. **Replace the vague "Ray prunes live checkpoints" warning with the number:** `tune_num_to_keep: 6` at ~11 min/iter is a live-checkpoint retention window of only **~70 minutes** |

## K. Publish → worker freshness

| # | invariant | instrument | status |
|---|---|---|---|
| K1 | workers pick up new weights (no frozen `model_sha`) | `distributed_stale_games`; worker `model_sha` | **VERIFIED 2026-07-26** — stale 0/20 iters |
| K2 | model freshness is not bound to thread 0 | `model tag switch sha=` per worker log | **VERIFIED 2026-07-26 (measured, was CODE-ONLY)** — all 4 workers logged 35-37 tag switches over 41 iterations and **all four end on the same sha** (`007c0d5b`), following the same sequence with only occasional single-sha skips (a worker polling after two publishes). No worker is pinned. This also independently refutes the loop_health alert that claimed frozen workers at 74-78% stale (see L6 / the alert fix) |
| K3 | AOT broker serves the current weights (externalized + rebound) | successive `model ready sha=` lines in `distributed_inference/broker.out`, comparing `model_step_s` | **VERIFIED 2026-07-26 (measured, was CODE-ONLY) — the rebind is real and it is free.** Two publishes on the live broker: `sha=007c0d5b … model_step_s=1.78 since_boot_s=67.30` (first bind, after `AOT packages loaded n=21 elapsed_s=7.08`) then `sha=eba60ee2 … model_step_s=0.00 since_boot_s=2835.92`. The second publish takes **0.00s** against 7.08s of AOT package loading at boot, which is only possible if the weights are externalized and rebound into the already-compiled packages rather than reloaded or recompiled. The sha genuinely changed, so it is serving the new weights, not silently pinned. This is the direct evidence behind the standing note that stale `.pt2` files on disk are expected and fine |
| K4 | the inference fast path is actually ON (compile + AOT + BF16) and batches are not starved | `broker.out` boot lines + per-window stats; `/proc/<broker>/maps`; `utils/amp.py` | **VERIFIED 2026-07-26 (measured — closes task #3).** All three are on: boot logs `compile=True aot=True`, **21 AOT packages** load in 7.08s, and `/proc` maps confirm AOTInductor `wrapper.so` for every batch bucket b16..b2720; `inference_autocast(dtype='auto')` resolves to **BF16** on this card. Occupancy over 38 x 10s windows: **median 211 pos/batch**, **5.9 of 16 slots**, median **6332 pos/s**. Per-batch time is **fwd 21.6ms = 76.4%** (pack 5.8%, out 5.9%, sync+scatter 11.9%), i.e. **compute-bound, not overhead-bound**. **Read the occupancy correctly:** '1.1% of the 19040 cap' is not idle capacity — the cap is 16 slots x 1190 and only ~6 slots have work pending at any instant, so the batch is DEMAND-limited, not config-limited. Consistent with the solved gather regime ([[broker_gather_regime_solved]]): do not sweep batching. The only real lever is positions in flight, and >2 workers is already known to lose to GPU contention |

| K5 | the published model is bit-identical to the checkpoint it claims | tensor-by-tensor compare against `checkpoint_000042` | **VERIFIED 2026-07-26 — and the match is specific, not vacuous.** 0/496 tensors differ against `_000042`, while against `_000041` and `_000040` **all 496** differ (max abs Δ 1.31e-2 / 1.46e-2). `arch` dicts identical across 41 keys; manifest `trainer_step` 58529 == checkpoint `step` 58529 |
| K6 | `model_sha` hashes the bytes actually served | `sha256sum` vs the manifest | **VERIFIED 2026-07-26** — `7a473d93…afd8` identical, and the ordering is correct (export → hash → manifest). The worker re-verifies after download (`_download_and_verify_shared(expected_sha256=…)`, delete-and-retry once, raises on persistent mismatch) and caches as `model_{sha}.pt` |
| K7 | publish is atomic (no torn read) | `atomic_write` usage | **VERIFIED-BY-CODE 2026-07-26, with one bounded in-process gap** — both `save` and `export_swa` write `<name>.tmp.<pid>.<uuid>` then `os.replace`. No `fsync` before rename, so crash-atomicity leans on FS ordering. The gap: `SharedSlotBroker._load_trial_weights` (`inference.py:2929`) records the *manifest's* sha but `torch.load`s the file, so a publish landing between those two lines makes the broker serve weights whose sha ≠ its recorded `model_sha` for one poll. Self-corrects at the next manifest change; bounded to one iteration |
| K8 | the published model reconstructs into a COMPLETE production net | build from manifest `model_config`, load with `require_complete=True` | **VERIFIED 2026-07-26** — passes from *both* the manifest's `model_config` and the checkpoint's own `arch`: 0 keys skipped, missing or unexpected. The two agree on all 40 `ModelConfig` fields; only `use_gradient_checkpointing` is not carried, deliberately forced `False` for inference |

## L. Evaluation / rulers

| # | invariant | instrument | status |
|---|---|---|---|
| L1 | a strength ruler exists and runs on a cadence | `scripts/daily_gate_ratchet.sh` via `scripts/ratchet_loop.sh` | **VERIFIED 2026-07-26** — fires, snapshots, arenas. Two self-audit defects found and **FIXED the same day**: (a) the Elo regex omitted `+`, so a POSITIVE result passed the whole log line into the CSV and corrupted it — negative Elo parsed fine, so it broke *precisely* when the net improved; now `\+?` plus a numeric reject-guard; (b) concurrency raised 4 → 16, see L6. Installed by **atomic rename**, not in-place edit, because the one-shot was still running (see method rule 9) |
| L2 | day-over-day regression is attributable to a day | `data/ratchet/ratchet.csv` `vs_prev` series | **PENDING** — starts on the second day (needs an earlier snapshot) |
| L3 | drift from a frozen anchor is tracked | `vs_boot512` series | **VERIFIED 2026-07-26** — first run in flight |
| L4 | the in-loop gate is not reporting a fake pass | `gate_decision`, `gate_games_cur`, `gate_games_prev` in a LIVE `progress.csv` | **CODE-ONLY (PR #285; this row's own instrument NOT yet exercised).** Same situation as I20, and marked the same way. The fix is real and mutation-tested: `gate_passed` is DELETED, not renamed — it was a constant 1 emitted while `gate_games: 0` — the metric is now `gate_decision` (−1 not-run / 0 demote / 1 promote), it travels with the games it was computed from, and `gate_metrics()` refuses to emit a promote with zero anchored games at all, so the old shape is unrepresentable rather than merely discouraged. **But no live iteration has ever emitted `gate_decision`**: the columns first appear on the next restart onto this code, so by this table's own rule ("`CODE-ONLY` — argued from source, not measured; treat as OPEN for anything load-bearing") the row is OPEN for decisions until a live row carries them. Promote to VERIFIED by reading a live `progress.csv` and checking (a) `gate_passed` is absent, (b) `gate_decision` is −1 while `gate_mode: off`, (c) `gate_sample_games_cur`/`_prev` are populated anyway. |
| L5 | `value_regret` comparisons are era-matched | `--min-pieces` default 8 (TB-excluded) | **CODE-ONLY** — historical 70–76 / BT4=43 were full-set, NOT comparable |
| L7 | the pentanomial Elo point estimate is textbook logistic Elo | known-answer table (`scratchpad/elo_validate.py`) | **VERIFIED 2026-07-26** — `_elo_from_score` equals 400·log10(p/(1−p)) to <1e-9 at p ∈ {0.001, 0.1, 0.25, 0.5, 0.51, 0.55, 0.6, 0.64, 0.75, 0.9}. 0.5 → +0.0 (not −0.0), 0.75 → +190.8485, 0.64 → +99.9510, 0.25 → −190.8485. p ∉ (0,1) returns `None`, never inf or NaN |
| L8 | the CI uses the PAIR as the sampling unit, not the game | variance construction + synthetic perfectly-correlated data | **VERIFIED 2026-07-26 — this is the whole point of pentanomial and it is genuinely being done.** 200 games as 100 perfectly correlated pairs: pentanomial se **0.05025** vs a games-independent (trinomial) se of **0.03544**, i.e. the pentanomial CI is **1.42× wider** because it is actually accounting for the correlation. Treating paired games as independent would have silently inflated confidence on every verdict in the ledger. Pairing is by adjacent index; both play paths construct that order explicitly (`2k`/`2k+1`) and the rolling path indexes by `gid`, so out-of-order completion cannot mis-pair |
| L9 | the 95% CI actually has 95% coverage | Monte Carlo, 8000 arenas per cell | **VERIFIED 2026-07-26 — the single most load-bearing negative result of the audit.** At **100 pairs** (the ratchet's own size) coverage is **94.3–95.4%** across five pentanomial regimes (drawish, sharp, +70 Elo, 95%-draw-draw, +300 Elo); at 500 pairs 94.6–95.1%. Neither conservative nor anti-conservative. Undercoverage appears only at 25 pairs (92.3–93.8%, and 71.2% in a degenerate 95%-DD regime) — so small capped runs are the regime to distrust, not the arithmetic. Because the score→Elo map is strictly monotone, score-CI coverage equals Elo-CI coverage exactly. **Every past ledger verdict rests on this code and it holds up** |
| L10 | a live arena's printed Elo/CI reproduces from its own pentanomial | recompute from the log | **VERIFIED 2026-07-26** — from `data/ratchet/arena_2026-07-26_vs_boot512.log`, counts (23,4,41,7,25) give mu 0.4825, se 0.035756, Elo **−12.2**, CI **[−61.5, +36.7]** — identical to the printed values |
| L11 | the ratchet parser never writes a malformed CSV row | drive `print_summary` with reachable counts; pipe real output through the script's `sed` | **FAILED 2026-07-26, FIXED SAME DAY (commit `e76dc0d65`).** The L1 fix hardened `elo` and stopped there. A one-sided `n/a` CI bound passes that check, and then `hi` falls through as the WHOLE log line — which contains commas — writing a **9-field row into an 8-field CSV**. Reproduced exactly: `[arena] Elo: +470.4  95% CI: [+311.5, n/a]` → `elo=470.4 lo=311.5 hi=<entire line>`. **Reachable at the ratchet's own defaults, and newly so:** PR #252's `REPORT_EVERY=16` puts the first RUNNING block at 8 pairs, where counts (6,2,0,0,0) produce precisely that line — *making the instrument cheaper made its unhappy path routine*. Also found: an all-DD pentanomial gives se exactly 0 and printed `[+0.0, +0.0]`, a zero-width CI claiming perfect certainty from a handful of pairs. Fix guards all three fields and warns on the degenerate case; verified against all six reachable outputs. **This is method rule 8 reintroduced by the fix to method rule 8** |
| L12 | the arena loads both sides COMPLETELY | the `load_state_dict_tolerant` call site | **CODE-ONLY hazard 2026-07-26** — `uci/model_loader.py::load_model_from_checkpoint`, used by `arena_standard.py:820` for **both** sides, calls the tolerant loader **without `require_complete=True`** even when the model was built from the checkpoint's own `arch` — exactly the case that function's own docstring reserves for `require_complete=True`. Up to 50% of keys can drop with only a stdout line before the catastrophic-load guard fires. Today's ratchet log has no `Tolerant load` line for either side, so that run was clean, but nothing asserts it: **method rule 7 (verify identity before believing a lopsided arena) is currently a human habit rather than code** |
| L13 | a time-truncated rolling arena is not a length-biased sample | compare pentanomial across RUNNING blocks | **OPEN 2026-07-26** — the rolling pool completes short games first, so a `--max-minutes` cap samples openings by game length rather than at random. Not detectable in the one available log (decisive-pair share 46.9% at 32 pairs → 48.0% at 100; Elo +16.3 → −12.2, well within CI). Needs a second capped run to price. **Note this is NOT optional-stopping bias** — the wall-clock stop rule is data-independent |
| L14 | `scripts/paired_compare.py` returns the right answer | known-answer + MC coverage (`scratchpad/paired_validate.py`) | **VERIFIED 2026-07-26** — constant delta −7 → CI exactly [−7.0000, −7.0000]; balanced ±1 at n=500 → [−0.0880, +0.0880] vs analytic ±0.0877; N(−5,20) at n=2000 → bootstrap [−6.451, −4.683] vs Wald [−6.437, −4.684]. Joins by **key, not file order** (a reversed B file still yields exactly −3.00). MC coverage 94.9 / 95.3 / 95.1% at n=50/200/2000 and **94.7 / 95.4%** on a symmetric tie-heavy audit-shaped population. The percentile bootstrap undercovers only under strong one-sided skew (skew +3.8 → 93.8% at n=200). **One real gap:** `load_dump` builds a dict, so duplicate join keys silently last-win and are **not** counted in the reported `dropped` |
| L6 | an observer costs less training than the signal it buys | `timestamp` deltas + `matching_games` in `result.json`, baseline vs arena window | **FAILED 2026-07-26, BUDGET SHIPPED (PR #252), CADENCE COST NOW BOUNDED.** The ratchet's first live run was self-defeating: `CONC=4`, **4h32m** for 200 games, across which training fell from **578s/iter, 483 games/iter (~3010 games/h)** to **3114s/iter, 462 games/iter (~534 games/h)** — an **82% throughput loss**, ≈**11,200 games ≈ 23 iterations** to buy one Elo point with a ±50 CI. **Concurrency helps but does NOT make arenas free, and the first read of this overstated it.** Measured the same day at conc 16: a **sims-1** rung ran 200 games in 12 min and cost nothing (iter 38 = 570s, dead on baseline); a **sims-32** rung overlapped ~15 min of iter 39, which took **1400s — 2.4x baseline**, and since only part of that iteration overlapped the arena, the *instantaneous* cost is well above 2.4x. So sims-1 screening is genuinely cheap and sims-32 never is, at any concurrency. Concurrency still matters a lot for the arena's own wall clock (64 games: 2770s at conc 4 vs 194s at conc 16 — superlinear, because at low concurrency the run is latency-bound on GPU round-trips and holds memory for hours while computing little), and since interference is duration-driven that shortens the damage window — it does not remove it. **The fix is a budget, not a tuning parameter:** PR #252 makes `--max-minutes 30` a hard deadline and lets the CI float, records games PLAYED rather than requested, and adds `--report-every` so a capped run still emits a reading (the default 64-game report interval meant the killed sims-32 rung printed nothing at all in 18 minutes). **Operator budget, 2026-07-26: ~30 min/day of extra GPU. Today spent ~5h.** Ad-hoc arenas are the actual root cause and are not bounded by any code — ask before starting one |

## M. Input encoding — train vs serve

The 175-plane `v2_threats` input is built in three places: the selfplay path that
writes shards, the training path that reads them, and the live inference path
(UCI / arena / rulers). A divergence between any two is the worst class of bug in
this pipeline — the net would train on one distribution and play on another, and
**every ruler would still look fine**, because rulers measure the net against
itself. Nothing in stages A–L covered this.

**Headline: the encoders are clean. The skews are around them** — train-only
transforms with no inference counterpart, and rulers that feed the net an input
regime it never otherwise sees.

| # | invariant | instrument | status |
|---|---|---|---|
| M1 | all encoder implementations agree bit-for-bit at the production setting | `scratchpad/probe_encoder_parity.py` — `_mcts_tree.batch_encode_146_lc0_root_legacy_meta` (writes shard `x`) vs `_lc0_ext.encode_full_batch` vs `CBoard.encode_full(2,63)` vs python `encode_position` | **VERIFIED 2026-07-26** — 49 boards spanning `hist_len` 0–7; all six pairwise diffs `max|d| = 0`, zero planes differing. Four wrappers, one shared kernel |
| M2 | a `CBoard` advanced by `push_index` encodes identically to one rebuilt by `from_board` | `probe_push_vs_fromboard.py`, including a forced 3-fold repetition line | **VERIFIED 2026-07-26** — 0 of 554 positions differ, at `history_rep_fix=True`. This is the incremental-vs-rebuilt path that selfplay and arena respectively use |
| M3 | the stored v2_threats extra block equals what the kernel recomputes from the same row's piece planes | `probe_extra_planes.py` — `plane_decode.recompute_extra_planes` vs stored `x[:,112:175]` on live replay shards | **VERIFIED 2026-07-26** — 2,050 rows over 41 shards spanning the whole 820-shard window, `max|d| = 0` exactly. The extra planes are stored, not recomputed at train time, and the stored values are reproducible |
| M4 | short history is filled the same way in every path | `probe_encoder_parity.py` startpos dump + a live-shard history-slot census | **VERIFIED 2026-07-26** — we **zero-fill**; we do NOT repeat the current position, which is lc0's convention and the thing worth checking given [[bt4_audit_comparison]]. Identical in all four encoders. In live data 99.8% of rows (4,392/4,400) have all 7 slots filled, because both opening books are ≥8 plies deep |
| M5 | POV canonicalisation is complete and identical in C and python | `probe_pov2.py` — `encode(b)` vs `encode(b.mirror())`, history-free | **VERIFIED 2026-07-26** — 250/250 boards differ in **exactly one plane, 108** (the intentional side-to-move flag). Every other plane, including castling, en-passant, rule50 and all feature planes, is colour-invariant. Live rows are 49.9% black-to-move, so a POV error here would have hit half the corpus |
| M6 | shard encoding identity == model arch identity == live yaml | `.zattrs` census over all 820 replay shards vs `publish/latest_model.pt["arch"]` | **VERIFIED 2026-07-26** — 820/820 shards report `lc0_root_legacy_meta` / `history_rep_fix: true` / `lc0_1858` / 175 planes / 1,498,553 rows, and the checkpoint arch matches all four. The `_legacy_x_to_synthetic_lc0_root` remap path is **inert** (0 rows need conversion) — which matters because of M12 |
| M7 | train-time and play-time inputs are the same numbers | fp16 round-trip over 200 encodings | **FAILED-BENIGN 2026-07-26** — shard `x` is float16, inference is float32. `max|d| = 2.35e-4`, max relative error 3.7e-4, and **5.75% of nonzero entries change**. The worst plane by far is 109 (rule50 = `min(hmc,100)/100`): 7,232 of 9,652 changed elements. The magnitude is negligible and this is not worth fixing — it is recorded because the asymmetry is real, was undocumented, and is the sort of thing that gets blamed later for an unexplained few-Elo gap |
| M8 | feature dropout is inference-compensated | `trainer._apply_feature_group_dropout` (`trainer.py:1903`) + `feature_dropout_p: 0.10` | **FAILED 2026-07-26 — the one I would actually chase.** Six independent feature groups are each zeroed with p=0.10 at train time, **with no `1/(1-p)` rescale**: the code is `x[:, base+off : base+off+len] *= (1.0 - drop)` and nothing multiplies the survivors back up. So `1 − 0.9⁶ = 46.9%` of training rows have at least one feature group zeroed, while **0% of inference rows do**. In expectation every extra plane arrives at 0.9× at train time and 1.0× at play, and the 112 LC0 planes are never dropped — so this is a systematic shift in the **LC0-vs-features balance** between training and play, not merely regularisation noise. It is applied in `_run_optimizer_step` only, so the holdout eval also sees undropped inputs, making `test_*` losses off-distribution from the train losses in the same direction. **NB it is a no-op in the C encoders** — `feature_dropout_p` is honoured only on the python `encode_position*` paths, so it never touches recorded `x`; this is purely a train-time input transform |
| M9 | mirror augmentation produces encodings the inference path could also produce | `probe_castling_mirror.py` | **VERIFIED-WITH-CAVEAT 2026-07-26** — the swap itself is correct: the measured layout is 104=us-Q, 105=us-K, 106=them-Q, 107=them-K, and `augment._LC0_ROOT_CASTLING_SWAP_PAIRS = ((104,105),(106,107))` is exactly the K↔Q relabel a file flip requires. The caveat is distributional: `mirror_prob` defaults to **0.5** (unset in the yaml) and 11.3% of live rows carry at least one castling right, so **~5.6% of training inputs are file-mirrored positions with castling rights** — king on the d-file with castling set, a configuration the inference encoder can never emit. Legal as tensors, unreachable as chess |
| M10 | the frozen rulers encode inputs from the distribution the net trains and plays on | `scripts/audit_targets.py:374`, `scripts/value_regret.py:91`, `scripts/blindspot_panel.py:77` — all `CBoard.from_board(chess.Board(fen))` | **FAILED 2026-07-26** — the audit set and the value yardstick are FEN-based, so **91 of 175 planes (all 7 history slots, 13–103) are identically zero** in every ruler input, against 99.8% of training rows having them filled and real play having them filled after ply 7. Both sides of a paired comparison get the same treatment, so this is **not a bias between checkpoints** — but both rulers score the net in a regime it never plays in, which is a mechanism for method rule 6 rather than a restatement of it. **The arena is NOT affected:** `arena_standard.py` uses `sample_starting_board`, which replays the book moves and keeps `move_stack` |
| M11 | every live encode call site passes the model's encoding explicitly | grep of all `encode_position` / `encode_cboard` / `encode_positions_batch` call sites | **FAILED-THEN-FIXED 2026-07-26.** *Fix:* `chess_anti_engine/encoding/model_inputs.py` reads the encoding off the model (`build_model` attaches `input_history_encoding` / `input_extra_features`) and **raises rather than guessing** when either is absent; `encode_position_for_model` / `encode_positions_batch_for_model` / `encode_cboard_for_model` / `model_input_plane_count` are the model-facing entry points. Converted: both `eval/puzzles.py` sites, `scripts/expand_opening_policybeam.py` (also dropped its hardcoded 146), `scripts/value_regret.py` and `scripts/blindspot_resolution.py` (their `getattr(..., "legacy")` fallbacks were the same trap one step less armed), and `scripts/generate_bootstrap.py`, which was the one *live producer* writing legacy/v1 shards. Remaining bare-default call sites are deliberate and non-model-facing: `model/infer_input_planes` (counts planes; identical across history modes), `bench/play_batch_timing.py` (timing only), and the `fuzz_*` / `bt4_audit` scripts, which sweep the mode on purpose. *Original finding:* two puzzle-eval sites silently defaulted to `"legacy"`, **98 of 175 planes differ** on the same board |
| M12 | the legacy→lc0_root fallback remap preserves side-to-move | read `trainer._legacy_x_to_synthetic_lc0_root` against the measured layout | **FAILED-THEN-FIXED 2026-07-26 — it never can, so it now refuses.** The remap sets plane 108 (side-to-move) to **0 unconditionally**: the legacy layout has no absolute colour to recover (its plane 101 is a constant 1.0 for every position), so this is unfixable in place. *Decision:* `select_input_history_arrays` / `select_input_history_samples` now **raise** when a row needs the synthetic remap — legacy-stored, no recorded `x_lc0_root` — unless the caller passes `allow_lossy_legacy_remap=True`. The training path never passes it, so a salvage restore from a pre-switch pool now stops with an actionable error instead of training a whole window as white-to-move. Offline tooling opts in through the harness's existing `synthetic_lc0_root_history` switch. Rows carrying a recorded root tensor are lossless and unaffected; 0 live rows are affected either way (M6) |
| M13 | `history_rep_fix` cannot differ between two models sharing a process | `encoding/rep_fix.py` is a process-global setter; `build_model` is the single chokepoint | **CODE-ONLY** — last-writer-wins if two checkpoints with different flags are built in one process, mitigated by each arena side running as its own subprocess and by `ARCH_SCHEMA_VERSION = 17` rejecting checkpoints that would default the key. Separately: the yaml's claim that the fix "affects ~0.2% of positions" **could not be reproduced** — on/off differed on 0 of 312 boards (`probe_repfix_delta.py`), all built via `from_board`. Not a contradiction (the fix targets `hash_stack` truncation, which that probe does not exercise) but the 0.2% figure is unverified |


---

**Tally as of 2026-07-27 — 156 invariants** (58 this morning). Stage M is
new; stages A, C, D, E, G, H, I, J, K and L were all extended. The late additions
are C16/C17/C25/C26/C27 from the first audit of the C search kernel against its
Python reference, and D6–D19 from a deep pass on SF labelling.

**Stage D is the cautionary tale in this table.** It sat at 5 invariants with
zero failures, on the stage that produces every training target, and that read
as the healthiest stage here. It was the least examined one: the deep pass
turned it into 19 invariants with three FAILED. **Treat a stage with a high
verified-fraction and a low invariant count as unexamined, not clean.**

| verdict | n | which |
|---|---|---|
| VERIFIED family (-BY-DESIGN / -WITH-CONTEXT / -WITH-CAVEAT / -DIVERGENT / -INERT / -BUT-NARROW / -BY-THROUGHPUT / -BY-CODE) | 96 | |
| RESOLVED | 1 | C5 |
| **FAILED** | 34 | A6, A7, B4, C13, C16, C17, C26, C27, C6, D11, D12, D17, D18, G11, G13, G14, G15, G4, G5, G6, G7, G8, H5, H7, I11, J11, J2, J5, J9, L11, L4, L6, M10, M8 |
| FAILED, qualified (-BY-DESIGN / -BENIGN / -COSMETICALLY / -DORMANT / -BY-IRRELEVANCE) | 4 | E14, I14, I21, M7 |
| FAILED-THEN-FIXED | 8 | C25, E13, I13, I7, I9, J6, M11, M12 |
| CODE-ONLY (argued from source, still unmeasured) | 6 | D19, G12, I20, L12, L5, M13 |
| OPEN (measured, no pre-committed rule) | 2 | I8, L13 |
| PENDING (needs elapsed time) | 1 | L2 |
| NOTE / DECIDED (not pass-fail rows) | 4 | C8, C12, D5, E9 |

**How to count this table — do NOT substring-match.** The verdict is the FIRST
bolded token of the status cell. Several rows carry the word "FAILED" inside a
*retraction* ("an earlier FAILED verdict here was premature" — C4, C7), so a
`grep -c FAILED` overcounts and reports rows as broken that were explicitly
cleared. That mistake was made on this very table on 2026-07-26, which is
method rule 2 biting the document that contains method rule 2. Count with a
parser that takes cell 4 and reads its first `**TOKEN`.

**Reading the trend honestly:** the FAILED count went from 7 to 34 (plus 3
qualified) on a day of
progress, and that is the expected shape. None of the new failures are new
breakages — every one was already true and simply unmeasured. G5/G6 hid behind a
G4 row that described the ruler's churn *within* a run and never asked what a
restart does to it. I13 has been decorative since roughly early June, E13 since
2026-03-02, and A6 is a number that was "corrected" in the wrong direction. **Closing a stage tends to
open the invariants underneath it, so treat a falling FAILED count with more
suspicion than a rising one** — and note that the count fell earlier today, when
the audit was shallower.

---

## 4. Standing open items, in priority order

0a. **TIME-CRITICAL: the replay window is 63% fifteen-day-old data and stops
   being so around 2026-07-27 17:00.** 520 of 820 shards are symlinks into the
   07-11 salvage pool (G8). At the measured 23.6 shards/h drain, that block
   finishes evicting ~22h from the measurement, and **window-wide metrics will
   move at that moment with nothing in the loop having changed.** Anything read
   across the transition is confounded, including the holdout-freeze yardstick if
   its 30-iteration window spans it. Either start that clock after the drain or
   record the crossing in the entry. Also decide whether restoring the 07-11
   window alongside the 07-11 weights was intended — it is defensible for the
   boot512 restart but is recorded nowhere, and the clean 1.6-day window from the
   previous trial was banked (`pre_durable_deploy_20260726`) and not used.

0. **C6 readout hygiene (act on this first, it invalidates other reads)** — the
   no-seed readout must start at **iter 33**, not 28. Iters 27–32 are a
   restart-induced mix transient.
1. ~~C5 boot512 control ladder~~ **DONE 2026-07-26 — LR damage, not structural.**
   boot512 does not widen across the sims ladder (−41.9 → −43.7, ~2 Elo) where
   the damaged net widened ~160. The restart is the right fix and value-head
   *ranking* is retired as the emergency priority.
1b. ~~C7 stage 1 stalled~~ **RESOLVED — no action.** The airbag fired correctly
   on a genuinely losing restart (raw winrate 0.36) and regret has been
   descending since iter 32 as the EMA caught up. Stage 2 is strength-gated
   (C8), not blocked. **Watch, don't touch:** confirm regret keeps falling over
   iters 35–60. If it plateaus while raw winrate sits above 0.50, revisit.
2. **G4 / G5 / J2 holdout ruler — CODE FIX MERGED (PR #261), LIVE VERIFICATION OWED.**
   `freeze_holdout_at: 2000`
   **SHIPPED 2026-07-26** and verified in the banner (the yaml edit alone silently
   no-opped on resume; it needed the experiment_state patch, see J6). But the
   iter-42 verification found the freeze **had not armed**, and the reason is
   G5, not a deployment failure: the restart emptied the holdout buffer (2000 →
   194 rows), and the freeze only fires at `len(holdout_buf) >= 2000`. It will
   arm once the buffer refills — measured at +653 rows on iter 43, so around
   **iter 45–46**.
   **Persisting the frozen set is therefore not a nice-to-have — it is the
   whole fix.** Without it the sequence at every restart is: ruler destroyed →
   ~3 iterations with no holdout eval at all → ~10 iterations judged against a
   growing set → freeze onto a fresh sample → next restart repeats. `best_loss`
   walks across all of that unchanged, defending a number against a ruler that
   no longer exists.
   **DONE in code 2026-07-26 (PR #261) — verification owed at the next
   restart.** `holdout_buf` is serialised into the Ray checkpoint as
   `holdout.npz`, `holdout_frozen` / `holdout_generation` into that
   checkpoint's `trial_meta.json`, and both are restored in
   `_init_replay_buffers` instead of always constructing an empty
   `ArrayReplayBuffer`. The checkpoint rather than `_durable_trial_dir`
   because it is the only location that also survives a **salvage restore**
   (different trial dir; salvage copies a fixed file list out of the
   checkpoint), and because it keeps the ruler in lockstep with the weights it
   judges. `holdout_generation` is now compared on in `_update_best_model`, so
   a ruler change hands the record over instead of letting `best_loss` defend
   it — see J2.
   **The historic caveat still applies to historic rows: do not compare
   `test_loss` or `best_loss` across any restart boundary before this lands
   live** — including today's 06:02 and 17:05 boundaries.
   Two things to verify, in order, at the first restart on this code:
   (a) the first new row's `test_replay` matches the pre-restart value and
   `test_loss` is finite on iteration 1 (G5); (b) `holdout_frozen == 1` on the
   row where the freeze arms, before starting the 30-iteration clock for the
   pre-committed yardstick — the same step-5 discipline that caught J6. If the
   restore is refused for any reason it says so on stdout
   (`[trial] holdout init: restored_rows=...`), and the run degrades to the old
   empty-and-refill rather than failing.
1c. **E13 `soft_policy_temp` — PLUMBING SHIPPED 2026-07-26 (PR #257).** The key
   is now published and consumed, and the production yaml reads 2.0, so the
   realized target did not move. What remains is the *experiment*, unchanged
   from the reasoning below: 2.0 → 3.0 needs a ledger entry, a pre-committed
   yardstick and a kill rule before anyone edits that line. The instrument fix
   (A7) shipped alongside and immediately found a second unpublished group,
   `diff_focus_*`, which is still open and is now the top item of this class.
   Original text follows.
   The yaml has
   said 3.0 since 2026-03-02; the workers have used 2.0 the whole time, because
   the key is published by nobody and consumed by nobody. `soft_policy_ce` is
   ~41% of weighted trunk gradient, so this is not a corner. **Two edits are
   required** — add it to `recommended_worker` in `distributed_runtime.py` AND
   read it in `worker.py:_build_selfplay_configs`; a yaml edit alone cannot
   take. Then fix the instrument that missed it (A7): A4's reco-vs-yaml diff
   must run over the UNION of key sets, because this defect class is a key that
   is absent rather than wrong.
   **Do not just flip it to 3.0.** Five months of the current net were trained
   at T=2.0, the replay window holds ~a day of it, and 3.0 is an untested
   preference inherited from LC0. Ship the plumbing first so the key is real,
   then treat 2.0 → 3.0 as its own ledger experiment with a pre-committed
   yardstick and a kill rule.

1d. **L11 ratchet parser — FIXED today (`e76dc0d65`), but read the lesson.**
   The L1 fix hardened `elo` and stopped there; a one-sided `n/a` bound let `hi`
   fall through as the whole log line. What makes it worth remembering is *why
   it became reachable*: PR #252 gave the ratchet a wall-clock budget and
   `--report-every 16`, so the first RUNNING block now lands at 8 pairs — where
   degenerate pentanomials are common. **Making an instrument cheaper moved its
   unhappy path from exotic to routine.** Price that when budgeting any observer.

1e. **J9 / L12 — two one-line hardenings, both latent, both worth doing now.**
   `export_swa` should `_strip_compile_prefix` like `save()` does (today the
   published file is only safe because every consumer happens to use the
   tolerant loader). `load_model_from_checkpoint` should pass
   `require_complete=True` when the arch came from `ckpt["arch"]` — that is
   exactly the case its own docstring reserves it for, and it would turn method
   rule 7 from a human habit into code. Neither changes behaviour today.

2b. **M8 uncompensated feature dropout — the highest-value new code fix.**
   Six feature groups dropped at p=0.10 with no `1/(1-p)` rescale means the extra
   planes arrive at 0.9x expectation in training and 1.0x in play, while the 112
   LC0 planes are never dropped. That is a standing train/serve shift in the
   LC0-vs-features balance affecting 46.9% of training rows. Two defensible
   fixes — rescale the survivors (inverted dropout, the conventional choice), or
   set `feature_dropout_p: 0` and drop the regulariser. **Do not ship either
   blind:** it is a training-affecting change, so it needs a ledger entry with a
   pre-committed yardstick first, and it is data-affecting enough that it cannot
   share a readout window with the holdout-persistence fix.

2c. **I13 `matrix_weight_decay` is a decorative config key.** — **DONE (PR #260).**
   `Trainer.load()` re-applies the construction-time snapshot of every
   config-derived param-group key, so the whole class is covered, not only
   `weight_decay`. The yaml was set to `0.0001` — what actually ran — so the fix
   ships numerics-neutral; changing the value is now a real experiment and needs
   its own ledger entry.

2d. **I9/I11 promote clip-rate and grad-norm into the Ray metric stream.** —
   **DONE (PR #260).** Eight per-iteration columns, aggregated over every step
   rather than the 10-step TB sample; the yaml comment now carries the 2026-07-26
   measurement and the pre-committed median-4.75 threshold, mirrored in code as
   `GRAD_NORM_MEDIAN_WATCH` with a per-iteration warning when it trips.

2e. **M10 rulers are FEN-only.** `audit_targets` and `value_regret` zero 91 of
   175 planes. Both sides of a paired comparison get the same treatment so no
   verdict flips, but it means the frozen rulers score the net in a regime it
   never plays. Cheapest useful step is to measure whether history-filled inputs
   move the ruler at all before deciding whether to change them — that is one
   small GPU job, and it belongs in a day with budget headroom alongside L5.

3. **E4 row-integrity live check** — two poisoning paths were found and fixed
   with no standing assertion that a third is not open.
4. ~~I3 per-head gradient share~~ **DONE 2026-07-26 — no dead heads, and the
   grouping follow-up is closed too.** The diagnostic `wdl_ce` left the weighted
   denominator, so the shares now sum to 100%: **POLICY 80.74% / VALUE 19.24% /
   OTHER 0.02%**. 19.2% is the number to reason about `w_wdl` with.
5. **C3 / C4 PID health** — re-read now that seeding is off and the mix changed.
6. **A4 / A5 config divergence** — A5 **DONE 2026-07-26**: the live yaml was
   reconciled against `main` in the audit restart (merge auto-resolved the yaml
   cleanly; all live values verified preserved afterwards — lr 3e-5, dole 0, the
   rotating FEN list at its merge-time value, and `gumbel_c_scale: 0.1` arriving
   from #249 as a no-op). A4 published-reco diff still owed.
7. ~~K3 AOT broker weight freshness~~ **DONE 2026-07-26 — measured on the live
   broker.** Second publish rebinds in `model_step_s=0.00` against 7.08s of AOT
   package loading at boot, on a genuinely changed sha. **L5 is the only
   remaining CODE-ONLY row** and is documentation-only: it says historical
   `value_regret` figures (70–76, BT4=43) were taken full-set while the ruler now
   defaults to `--min-pieces 8`, so they are not comparable. Closing it means
   re-running one checkpoint at both settings — cheap, but it is GPU against the
   30 min/day budget, so it waits for a day with headroom.

## 5. Cadence

- **Every deploy / restart:** A1, A2, A5, K1.
- **Daily, automatic:** L1/L2/L3 (the ratchet — nothing to run by hand).
- **Every readout window:** the stage owning the knob under test, plus G1 and H1.
- **After any "the numbers look wrong" moment:** section 2 first, *then* measure.

## 6. Audit log

| date | scope | outcome |
|---|---|---|
| 2026-07-26 | A1–A3, B1–B3, C1, D1, D4, F2, G1, G2, H1, I1, I2, K1, L1, L3 | all VERIFIED |
| 2026-07-26 | G4 / J2 holdout ruler | **FAILED** — rolling ring, ~13-iter turnover; fix identified, deferred |
| 2026-07-26 | B4 seed pool re-vet | **FAILED** — 124/202 false positives; seeding disabled |
| 2026-07-26 | C5 sims ladder | **FAILED** — deficit widens with search; control queued |
| 2026-07-26 | B1/B2 dole cap | VERIFIED — **an earlier "violation" here was an artifact of a bad ad-hoc sum; see method rules 1–3** |
| 2026-07-26 | C3, C4, C6, D2, I4 | D2 VERIFIED (0.947–0.999); C3 VERIFIED-with-caveat; **C6 FAILED** (restart-key toggle distorted 6 iters of mix). C4's initial FAILED was **retracted** — the nodes pin is correct staging; the real issue is C7 |
| 2026-07-26 | C4→C7 re-read | C4 VERIFIED-BY-DESIGN; **C7 FAILED** — regret flat ~0.089 for 32 iters after an iter-2 airbag fire, so stage 2 is unreachable |
| 2026-07-26 | A4, A5, E4, G3, H2 | all VERIFIED. A4: 63/64 exact, 1 intentional (`sf_nodes` floor). A5: 4 known divergences, reconciliation table added. E4: 18,273 rows CLEAN. G3: window at configured max. H2: priority CV 0.94, negatives safely clamped |
| 2026-07-26 | L1 self-check | **2 defects in the new ratchet** (task #22): Elo regex drops `+` and corrupts the CSV on improvement; 200 games @4 concurrent ≈ 6h |
| 2026-07-26 | K3, H4 (new) | **K3 VERIFIED** — second broker publish rebinds in `model_step_s=0.00` vs 7.08s of AOT loading at boot, on a changed sha. **H4 VERIFIED-INERT** — every train and holdout batch is WDL-rebalanced, but at the live mix (W 0.321 / D 0.321 / L 0.358, W:L 0.895) the 1.5:1 clamp does not fire; a replayed 512-batch is identical to the natural one |
| 2026-07-26 | **stages J/K/L: checkpoint fidelity, publish integrity, and the Elo math** | **The Elo arithmetic is CORRECT — the most load-bearing negative result of the audit.** `_elo_from_score` matches textbook logistic Elo to <1e-9 (L7); the CI genuinely treats the PAIR as the sampling unit, 1.42x wider than a games-independent se on perfectly correlated data (L8); and Monte Carlo over 8000 arenas per cell gives **94.3–95.4% coverage at 100 pairs** across five regimes (L9). `paired_compare.py` likewise checks out at 94.7–95.4% (L14). Every past ledger verdict rests on this code and it holds. Checkpoints round-trip bit-exactly including both compile-prefix directions, closing the 2026-04-27 failure mode (J7/J8), and the published model is bit-identical to the checkpoint it names — specifically, not vacuously, since the two neighbours differ in all 496 tensors (K5/K6/K8). Defects found: **L11** the ratchet parser still corrupted the CSV on a one-sided `n/a` CI bound — my own L1 fix guarded `elo` only, and PR #252 made the 8-pair block that triggers it routine (**fixed same day, `e76dc0d65`**); **J9** `export_swa` does not strip the compile prefix while `save()` does, so a non-tolerant loader would silently get 0/86 correct tensors; **J11** independently reproduces A6's 63,084,128; **L12** the arena loads both sides without `require_complete=True`. Also: undercoverage appears only at 25 pairs (92.3%, and 71.2% degenerate), so **small capped runs are the regime to distrust — not the arithmetic** |
| 2026-07-26 | **stage G/H internals: replay buffer + shard lifecycle** | **G8 FAILED, and it is time-critical** — 520 of 820 window shards are symlinks into the 07-11 salvage pool with content **15.5–18.6 days old**; only 300 local shards (0.1–12.7h) are fresh. CLAUDE.md's "~a day of data" is false right now, and the stale block finishes evicting ~2026-07-27 17:00, which will move window-wide metrics on its own. **H5 FAILED** — the shuffle refresh weights shards **linearly by recency** (`np.arange(1, n+1)`), so newest:oldest = 820:1, median trained-row age 10.4h vs 386.6h uniform, Kish **ESS 0.747** — a 1.5M window behaving like 1.12M, documented nowhere. **G7 FAILED** — 1.37% duplicated rows, byte-identical, from two concurrent writers into one shard dir on 07-11; baked into a designated revert point. **G11 FAILED** — composition drifts across the window and `seed_id` is absent from 60% of it. Clean: eviction is FIFO (G9), symlink eviction does not touch the salvage pool (G10, verified 815 shards intact), the eviction/prefetch race degrades safely across 25 real evictions (H8), and duplicate-row-in-batch is 0.37% by-design resampling (H6) |
| 2026-07-26 | **stage C/E extension: selfplay search → policy target** | **E13 FAILED, the day's biggest find** — `soft_policy_temp` is 2.0 live against a configured 3.0, because the key is in neither the published reco (83 keys, verified absent) nor the worker's config builder; the yaml has been wrong since 2026-03-02 and `soft_policy_ce` is ~41% of trunk gradient. **A7 FAILED** — 7 worker-affecting keys are never published; A4 cannot catch this because it diffs the *intersection* of key sets. **C13 FAILED** — 28–29 of 32 selfplay threads per worker die in a 30s startup race against broker readiness and are never respawned (magnitude unestablished). Clean and decisive: the 4672→1858 projection is a strict bijection over 394,660 legal moves incl. 55,821 underpromotions (E12); Gumbel noise decays exactly as configured and never enters the target (C9); `policy_target` is the completed-Q improved policy with zero off-legal mass (E10/E11). Also corrected: live `policy_target` entropy is **0.63 nats, not the 1.02** recorded as the reference |
| 2026-07-26 | **stage M (new): input encoding, train vs serve** | **Encoders CLEAN — four implementations bit-identical (M1/M2), stored extra planes reproduce exactly on 2,050 live rows (M3), POV differs in exactly one plane (M5), 820/820 shards agree with the checkpoint arch (M6).** The real skews are around them: **M8 FAILED** — feature dropout zeroes 6 groups at p=0.10 with **no `1/(1-p)` rescale**, so 46.9% of training rows have a group zeroed and 0% of inference rows do; **M10 FAILED** — `audit_targets` and `value_regret` build inputs from FENs, leaving **91 of 175 planes zero**, so both frozen rulers score the net off-distribution (the arena is unaffected). M11/M12 are dormant landmines |
| 2026-07-26 | **stage I extension: optimizer + loss mechanics** | **I13 FAILED — `matrix_weight_decay: 0` in the yaml, `1e-4` in the live optimizer**, because `Optimizer.load_state_dict` overwrites group hyperparameters on every resume and `weight_decay` is the one with no re-application path. Harm is ~0.35% shrink over 58k steps — report it as a broken control surface, not damage. **I7 FAILED** — the reported `wdl_loss` is a diagnostic one-hot CE, not the blended loss the optimizer sees (0.7599 vs 0.7859 at iter 42). **I11 FAILED** — the yaml's "median grad norm 1.81, cap inert" is 2.3x stale; measured median 4.244 and drifting up. **I19** — `opt_lr` reports the trough of the `sqrt_release` cycle, ~9x below the matrix group's mean LR. Clean: masked_mean denominators (I15), illegal-move masking (I16), effective batch 512.0 (I18) |
| 2026-07-26 | **I7 / I3 / M11 / M12 fixes** | **Four reporting/latent defects closed, no training numerics moved.** I7: `wdl_loss` (and `test_wdl_loss`, and the five splits) now report the blended loss the optimizer sees; the one-hot diagnostic moved to `wdl_onehot_loss`. Expect a one-time **+0.026 step in the `wdl_loss` column** at the deploy — quantity change, not a regression; best-model selection reads the total loss and is untouched. I3: the probe's denominator dropped the gradient-free diagnostic, so shares sum to 100% and the **value share is 19.24%, not 15.51%**. M11: a model-aware encoder (`encoding/model_inputs.py`) that raises rather than defaulting to legacy, wired into puzzle eval, the beam-expander, both frozen-ruler scripts, and `generate_bootstrap.py` — which had been the one live producer of legacy/v1 shards. M12: the POV-lossy legacy remap now raises instead of silently emitting white-to-move rows |
| 2026-07-26 | **A6 param count** | **FAILED** — CLAUDE.md's 78.8M is a `state_dict` sum that double-counts one shared smolgen tensor 16 times. True count **63.08M**; the "stale ~63M" that line claims to have corrected was the right number |
| 2026-07-26 | **I9 / I11 / I13 / I19 control surfaces FIXED (PR #260)** | **I13:** `Trainer.load()` re-applies the construction-time snapshot of every config-derived optimizer param-group key, with an explicit exception list (`lr`, `initial_lr`, `soda_k`) for what the checkpoint owns — so `aurora_uw_floor` and the SODA markers are fixed alongside `weight_decay`. `matrix_weight_decay` set to `0.0001` in the yaml = what actually ran, so **zero numerics change**; moving it is now a real experiment. **I9:** eight per-iteration grad-norm/clip columns in `result.json`/`progress.csv`, aggregated over EVERY step. **I11:** yaml comment rewritten with the measurement + the pre-committed 4.75 median threshold, mirrored as `GRAD_NORM_MEDIAN_WATCH` with a live warning. **I19:** `opt_lr` → `opt_lr_final`, plus `opt_lr_mean` / `opt_lr_max`; `status.csv`'s `lr` → `lr_mean` |
| 2026-07-26 | G5, G6 (new) | **BOTH FAILED — found by verifying a deployment rather than trusting it.** The iter-42 check on the holdout freeze reported `holdout_frozen 0`, and the cause was not the deploy: **a restart destroys the holdout buffer** (2000 → 194 rows; ~13 iterations to refill; ~3 iterations with no holdout eval at all). `best_loss` crosses that boundary unchanged, so it defends a value measured against a ruler that no longer exists — a concrete mechanism for the old "`best_loss` rises across restarts" puzzle. G6 is the smaller sibling: `test_size` is the buffer size, not the evaluated-row count (the eval resamples 5×512 from ≤2000 rows). **Persisting the holdout is now the top code fix** |
| 2026-07-26 | G5, G6, J2 fix | **CODE FIX MERGED (PR #261) — not yet a verification.** The holdout's rows now ride in the Ray checkpoint (`holdout.npz`, 2.09 MB compressed for 2000 real rows) and its `frozen`/`generation` scalars in that checkpoint's `trial_meta.json`; the checkpoint, not `_durable_trial_dir`, because it is the only home that also survives a salvage restore and keeps the ruler in lockstep with the weights it judges. `holdout_generation` became durable and is therefore now **compared on** — a ruler change hands `best_loss` over instead of letting it defend a number earned on a set that no longer exists. `test_size` now means the rows the eval draws (`test_steps × batch_size`), with the buffer size left under `test_replay`. **Read G5's instrument on the first row after the next restart before calling any of this VERIFIED** |
| 2026-07-26 | **E13 / A7 fix (PR #257)** | **E13 FIXED behaviour-preservingly, A7's instrument fixed and the invariant promptly failed again on a NEW key.** `soft_policy_temp` is now published by `build_recommended_worker` and consumed by `_build_selfplay_configs`; the production yaml moves 3.0 → **2.0**, which is what the loop has realized since 2026-03-02, so the training target does not move — the point was to stop the config lying, not to run the experiment. The A4 reco-vs-yaml diff now runs over the **union** (`--reco-diff`), with allowlists that self-invalidate: `_RECO_WORKER_DEFAULT` pins each intentionally-unpublished key to its dataclass default, so editing the yaml off that default converts it back into a finding. First run of the repaired instrument found the **`diff_focus` group** — five yaml keys, none published, worker runs `DiffFocusConfig()` 6.0/3.5/3.0/0.025 against the yaml's 4.8/3.8/4.0/0.09. Left open (fixing it IS data-affecting) and pinned by name in `tests/test_reco_coverage.py`. Method note: the manual A7 pass enumerated `GameConfig`/`SearchConfig` fields and therefore could not see a missing config *group*; enumerate the callee's parameters, not one dataclass's fields |
