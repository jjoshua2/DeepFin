# Actual table prefix intervals and schedule refinement

## Baseline and scope

Continue PR #822 at `b85b4b3620a815150736e4a6ac2858d372160917`, complete tree
`919b77ab4c7188a7c7d69f7880d72e7540be52ca`. The entire recovered archive tree,
file modes and original commit object were verified. The pinned compiler remains
`aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae`, Bend 2.0.21 plus U64; all 84 source
inputs retain fingerprint
`d9550e30dbf17f12013aa5db89b27cf6724957fe68213c9c7e409e99f1405ef4`.

This is a proof/test/docs-only increment. Production sources, prior accepted
laws/gates, compiler/checker inputs, existing tests/configurations and routine
perft budgets are unchanged. Work is isolated from live processes/checkouts.
No merge, deployment, model or training work is part of this task.

## Acceptance timing and rule

Constructive source development precedes this record; this is not backdated
preregistration. Final qualification requires the exact candidate source identity,
the six-law public gate and rejection controls, all unchanged inherited gates,
four native modes, original compiler source/pin checks and whole-repository lint.
A failed gate must remain failed until its cause is resolved; neither crashes nor
missing imports count as semantic rejection. Only after qualification may hosted
evidence be described as passing. Intermediate results below have narrower scope.

## Six public contracts

| Contract | Actual connection and domain |
| --- | --- |
| `prefix_widening` | Every cumulative endpoint k<=128 zero-extends to the widened cumulative sum. |
| `prefix_in_allocation` | Every such endpoint is >=512 and <131072. |
| `step_no_overflow` | For k<128, adding the actual block size in U64 gives exactly the zero-extended next U32 endpoint. |
| `blocks_ordered` | For every i<j<128, the end of block i is no later than the start of block j. |
| `tables_follow_prefix` | For every n+k<=128 and actual affine initial array, actual Tables.tables equals the explicit independent-prefix schedule as a complete array value. |
| `logical_end` | The endpoint after 128 slider blocks is exactly 108160. |

The geometric schedule uses the existing independent rank/file population formula,
not host-provided prefix answers. Finite source cases discharge all endpoints and
transitions. The actual-size correspondence comes from the already proved public
layout law, whose producer is explicitly imported. Callers do not assume the
needed size equalities. Induction over the original builder consumes internally
constructed size certificates; the comparison schedule retains actual array writes
and the original fill algorithm, rather than manufacturing candidate attacks.

## Development evidence and failed attempts

The arithmetic Facts module passed a bounded local recheck in 110.09 seconds;
a preceding 45-second command did not finish and was not a pass. The generic
array-loop refinement core, Nat/order helpers and size bridge checked separately.
The initial combined public consumer timed out after 600 seconds with no accepted
verdict. A diagnostic observer (outside the repository) identified the expensive
closed-root refinement; the original checker files were not modified. The final
accepted set is to be determined by the exact public gate, not these intermediate
component outcomes.

The extra proposed closed Tables.build equality was never accepted and is not
included among the six contracts. The universal actual table-loop equation retains
its full n+k<=128 domain and arbitrary affine arrays. The consumer uses its
parametric theorem rather than expanding all 107,648 slider writes during type
conversion. The separate closed runtime build remains in native verification.
No existing accepted obligation is weakened, removed or skipped.

The four local native modes passed five complete buffers each (655,360 cells per
mode), covering zero/one/64/128 original table-loop prefixes and actual Tables.build.
The oracle computes masks and attacks from signed coordinates and deposits compact
indices directly, rather than copying the carry-rippler. Every returned cell is
compared, including reserved metadata, initialized entries and unused slack.
Seven invalid requests are rejected per mode. These are repeated fixtures, not
exhaustive arbitrary-input native testing or a source proof of geometry.

## Remaining obligations and trust

These arithmetic endpoints and the full-array schedule equation do not yet derive
separation/clear certificates for every interior storage path, prove computed
attack contents, or connect actual Chess.bend lookup to an independent blocker-ray
specification. Those remain the next P2 acceptance. Native complete-buffer parity
does not discharge those universal source statements.

Self-review only, not independent review. Source results trust the pinned checker
and Base semantics. Native lowering, array allocation/ownership/lifetime, effects,
ABI, toolchain, OS/hardware and model libraries remain separate boundaries. The
fork's historical strict-TypeScript and diagnostic-depth issues are not suppressed.
Python export, external references, data/control orchestration and training remain;
C++/LibTorch/AOTI remains transitional inference. No additional Python responsibility,
full-engine theorem, model/GPU qualification, performance or strength result follows.


## Subsequent local acceptance

The six-law public consumer passed with exactly `All terms check.` in 374.41
seconds. All 19 new controls passed in a separately labeled controls-only
development execution. Final source qualification retains the universal
`n+k<=128` table-loop equation; the extra closed-root statement is not counted.
A final focused-gate recheck is required after whitespace-only consumer cleanup;
its report, rather than the earlier draft report, identifies the final sources.
No local full 74-law aggregate is claimed. The unchanged whole-repository lint
command was attempted and failed because Ruff, Basedpyright and Vulture are
absent; hosted locked-environment lint must resolve that gap.

During direct source transfer a duplicated tuple item was detected in Facts'
case 99. Recovery required both the observed incorrect blob identity and the
exact original locally checked blob `4caa1366a6e7a87e8be0b7dfcd2085443fe1b828`.
The corrected original source, not the malformed copy, is the candidate. An
unreferenced alternative transport blob was discarded without application or
execution. These are transfer artifacts, not source-law or compiler changes.
The full candidate tree must match the local tree before hosted execution.


## Hosted qualification and publication

Hosted run **35747413010**, temporary workflow commit `647db192fd1004c897824e72a06699171a8a68de`, passes **74 accepted laws and 126 rejection controls**, all four native modes, original compiler source/pin checks and unchanged whole-repository lint on source `34d40b0a8e897fb48536a40fee1c2ea443431b70`.

The complete tree ee69ab6c6053899f3b990771c1f072b66a85d096 matches the inspected local source. Every native/proof source digest is checked before and after qualification. The aggregate retains the unchanged 68-law/107-control separation gate, then checks the six-law/19-control prefix gate. No earlier law or rejection control is removed. Local full aggregate execution is not claimed.

The public caller provides only n+k<=128 or the stated finite key-order bounds. All actual-size certificates are constructed using the explicitly imported existing layout proof. The actual table-loop equality quantifies over the complete affine array, not only scalar counters or an ideal buffer. It equates schedules of the original operations; it does not independently prove the slider function or every initialized final lookup.

Ten controls require ordinary affected-refinement failures, including wrong prefixes, widened arithmetic, geometric sizes, actual header addresses, next-prefix updates and fill counts. Nine additional controls protect proof/import manifests, existing certificate producers, no holes/foreign/symlinked proofs and exact successful checker output. Crashes, missing-file errors and timeouts are not counted as semantic rejection.

Each native mode compares all 131,072 returned cells in five executions: zero, one, 64 and 128 actual slider-block loops with nonzero seeds, plus actual Tables.build with zero initialization and extras. That is 655,360 cell comparisons per mode; all 128 chess keys, reserved metadata and unused slack are covered. Seven invalid requests are rejected per mode. The full native report matches the local canonical result except the separately recorded C compiler identity. Modes repeat the same fixtures, not disjoint datasets or arbitrary-U64 exhaustiveness.

Tools: Bun 1.4.2, Ubuntu clang version 18.1.3 (1ubuntu1), locked Python 3.13 CPU environment with uv 0.12.10. Original compiler source laws/seven controls and all 12 pin contracts pass. The unchanged Ruff/Basedpyright/Vulture gate passes, resolving the historical local missing-tool lint gap. Compiler-fork strict-TypeScript and diagnostic-depth limitations remain separately unresolved and unsuppressed.

The six public contracts remain unchanged after qualification. A separate proposed closed Tables.build source equality was never accepted and is not counted: its direct normalization exceeded a bounded construction check. Actual build execution is native-tested, while the general n+k<=128 table-loop theorem is source-proved. No earlier accepted theorem was weakened to handle this distinction.

After qualification only compact evidence, the experiment index and the migration matrix are added. Full logs are retained in artifact bend-prefix-intervals-qualification for 30 days; compact reports are committed. Self-review only, not independent review. No production runtime code, previous law, compiler input, existing test, permanent workflow or routine perft budget changed.

The feature branch `feat/bend-prefix-intervals-20260922` receives only a fast-forward evidence/documentation commit. No merge, force push, deployment or live-process change. Temporary recovery/qualification workflows and unused transfer objects are excluded from the feature tree.

Next acceptance: derive normalized separation/clear certificates for every interior address from the now bounded ordered prefix intervals, combine them with frame/ordinal lemmas to prove final computed contents, and connect actual lookup to independent blocker-ray geometry. Endpoint normalization and native full-buffer parity do not replace those source contracts. Python export/data/control/training and transitional C++/LibTorch/AOTI remain dependencies; no model/GPU, engine strength or speedup claim is added.
