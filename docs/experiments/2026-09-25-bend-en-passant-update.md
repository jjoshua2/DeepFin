# Public en-passant update contracts

## Basis and acceptance, recorded before hosted qualification

Parent #885 at `dc69dfc4f44c1d0a1a6ad9615a98eb6e82327829` was refreshed and remains
open/unmerged. The complete local reconstruction matches its 3,781-path Git tree
and all 472 inherited native-source hashes. Its archived actual en-passant proof
and mask helper are promoted byte-for-byte, as is its probe. These two registered
contracts reuse already checked mathematics rather than claiming two discoveries.

The unchanged compiler is `aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae`, with 84 inputs
and fingerprint `d9550e30dbf17f12013aa5db89b27cf6724957fe68213c9c7e409e99f1405ef4`.
Repository guidance, development, branch lifecycle and experiment index were read.
Work is isolated; no merge, deployment or live-process changes are authorized.

Acceptance requires exact safe source success for the two public contracts and
consumer, all 17 classified controls, generic/portable/native-target/UBSan native
complete-Board comparisons and both actual capture-location regressions, original
compiler source/pin checks, and unchanged whole-repository lint. Hosted checks are
bounded and confined to a temporary development workflow. Parent 150/436 evidence
may be retained only on exact source and successful receipts. The full aggregate
is not implied. No perft, GPU/model, search, training or performance work is added.

## Contracts and local development

`en_passant_make_move_exact_update` proves actual flag1/promotion0 execution equals
the full clearing/reinsertion and existing metadata specification. Input Board,
metadata, and raw U32 source/destination are arbitrary.
`en_passant_make_move_preserves_representation` proves partition preservation from
the input-consistency premise. No source pawn, rank, history, legality or king
safety is proved. Native squares are bounded to0..63; source mask behavior remains
raw. Correct metadata as chess rules is a distinct target.

The final focused local gate passed both laws and all17 controls. Eight controls
are intended source-refinement failures, eight are manifest/import checks, and
one tests synthetic warning output. The native suite passed all four modes:
4,316 complete Boards /82,004 U32 fields and nine invalid requests per mode.
The coordinate-set reference has the same expected outputs as the archived
supplementary reference while computing capture location via rank/file arithmetic.

One draft target-clearing mutation also changed flag0 behavior and was rejected
by the inherited Ordinary.unfold bridge, not the intended new lemma. It was not
counted as a successful new control. The final mutation is explicitly restricted
to flag1, and all eight intended source failures were rerun successfully. The
failed log remains in the external review package. A streaming-execution tool was
unavailable before execution; the ordinary synchronous command then completed.
The initial compiler-location probe used its archive's enclosing directory and
failed before verification; the correct nested checkout passed the unchanged pin.
No failed attempt, inherited failure location, or missing tool is relabeled a pass.

## Scope and remaining work

This is public-suite promotion and expanded native qualification, not legal-move
soundness. The complete returned Board is checked; a no-op preserving consistency
would still violate the exact-update statement. Existing production functions,
compiler input, earlier laws and routine test budgets remain unchanged.
Self-review only; no independent review is claimed. Python export/references,
data/control/training and transitional C++/LibTorch/AOTI remain dependencies.
Existing compiler, structural-snapshot lowering and closed-builder limitations
remain explicit. Next: castling refinement under sufficient input conditions,
then legal-move producers and king safety.


## Completed hosted qualification

Hosted run **36216251656** passes both public en-passant laws and importing consumer, all17 controls, four native modes and both actual capture corruptions, original compiler source/pin checks and unchanged whole-repository lint on source `59acb49cb4395884741803b448842d2100054b1d`.

Actual.bend, Mask.bend and the actual-update probe are byte-identical to the checked supplementary archive from PR #885. The two registered results promote that mathematics without narrowing raw U32 inputs or inventing legal-move assumptions. Exact full-Board equality and partition preservation are distinct contracts; preservation alone would permit a no-op.

The focused command executes its consumer and all17 controls. Eight are ordinary semantic/refinement failures at intended locations, eight enforce manifests/imports/no unsafe or holes, and one synthetic warning-output unit is not a compiler execution. The first local broad target-mask mutation hit an inherited flag0 proof; that rejected harness run was retained and the final mutation was scoped to flag1. No checker, producer, contract or expected behavior was weakened.

Exact-source150/436 parent evidence is retained on472 manifest entries and the successful parent job. Modular coverage is152 laws/453 controls, not a newly executed complete152-law wrapper. All482 candidate source hashes match. Full proof JSON matches local except consumer seconds; native JSON matches except the recorded C compiler.

Generic,forced-portable,native-target and UBSan each pass4,316 complete Board cases with4,252 distinct input tuples and82,004 U32 field comparisons. This covers all4,096 raw source/destination pairs plus28 pawn-coordinate examples,64 raw-metadata cases and128 inconsistent diagnostics;4,188 cases satisfy partition consistency. Nine malformed requests per mode are rejected. Both actual capture-square corruptions compile/execute and fail the independent square-set reference. Modes repeat fixtures,not exhaustive Boards or legal en-passant moves.

Native square inputs are0..63. Source contracts follow arbitrary U32 mask behavior without assuming modulo64 wrapping. The reference uses adjacent-rank coordinates for the extra captured square rather than receiving the candidate mask. Metadata follows existing raw helper behavior; no independent chess-metadata,source pawn,rank,previous double-push,EP-right or king-safety theorem is claimed.

Fresh unchanged Ruff/Basedpyright/Vulture passes in the locked Python3.13 CPU environment with uv0.12.10. C compiler selection is confined to native probes,not Python extension builds. The local missing-tools lint failure remains historical; hosted success resolves only that environment gap. Compiler16-laws/seven-controls and12 pin tests pass. Compiler pin and84-input fingerprint remain unchanged.

Only documentation,index and evidence follow source qualification. No production function,earlier accepted law,permanent workflow,routine perft,model/GPU,search,training or benchmark changes. Self-review only; checker/Base,native lowering/storage,ABI,toolchain,OS and hardware remain trust boundaries. Existing compiler,snapshot-lowering and closed-builder limitations remain. No application responsibility moved from Python; export/references,data/control/training and transitional C++/LibTorch/AOTI remain dependencies. No merge,force push,deployment or live-process action.

Next substantive acceptance is castling update refinement under explicit representation conditions,then connection of raw updates to legal-move producers and king safety.
