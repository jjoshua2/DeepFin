# Public actual-lookup contracts

## Scope and acceptance before execution

Parent #872 is `9e7f433e0a6e139751b8a3b5f0871868df25a635`, complete tree
`b2550922a6e10199947eba5960ed1d0b9ef54e66`. The recovered archive reproduces
all 3,482 tracked paths, modes and the original commit. Compiler pin remains
`aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae` (84 source inputs).

Promote the archived Route/Pipeline/Canonical/MaskedLookup derivations into
LAWS/PROOF and an importing consumer. Keep those five archived Bend files,
including the native probe, byte-identical. These are not newly discovered
mathematical results. The public contracts cover conditional header routing,
canonical masked recurrence state, and initialized pipeline lookup. The last
requires only symbolic depth=17, a bounded selected-block budget and bounded
extras budget, with arbitrary seed/occupancy; header/data correctness is derived.

Acceptance: exact safe consumer success, semantic mutations of the real lookup
and public statements rejected for intended reasons, mandatory law/import guards,
four native modes on real Tables.build/Chess.slide, original compiler checks,
exact-source retained parent evidence and unchanged whole-repository lint.
No permanent workflow, production code, prior proof or test-budget changes.
Checks remain bounded and opt-in. No model/GPU, perft or training work.

The result is equality to production Tables.slider on occupancy masked by the
production relevant mask. Independent ray/mask geometry is still separate.
Source equality does not prove native ownership, allocation or lifetime. Existing
checker/Base, lowering, ABI and hardware trust remains; self-review only.

## Execution

Candidate prepared; results will be recorded after each check completes.
