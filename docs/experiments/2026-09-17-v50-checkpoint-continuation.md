# V50 bootstrap checkpoint continuation through four total epochs

Status: the three-additional-epoch continuation is running. Its first 528 additional optimizer updates completed successfully (cumulative step at least 69,502); no completed additional epoch or playing-strength result is claimed.

The completed 35M V50 donor trained once on 35,314,577 rows and saved step 68,974. This experiment tests whether substantially longer training on the same data yields useful checkpoints before changing corpus size or recipe. The donor is `runs/combined35m_v50_seed101/checkpoint.pt`, SHA256 `6d36f93d040c8babed040159419279af62a444f6466f42b720e12bb72d67ab02`.

Resume the same model, optimizer moments/groups, scheduler, ZClip state, peak learning rate and global step. The CLI verifies checkpoint identity, exact model keys/shapes/dtypes and equality of all restored states; a silent optimizer reset is a failure. It uses the original 21 V50 corpus roots and training configuration, batch 512 and 88-step windows. The sampler visits each row once in each additional epoch, with new ordering seeds 102, 103 and 104. Torch RNG is explicitly reseeded 102; complete uninterrupted RNG replay is not claimed. Augmentation RNG remains continuous between the three new epochs.

Retain checkpoints after each additional epoch, corresponding to total exposures of two, three and four epochs. Their global optimizer steps continue from 68,974; receipts separately record added and cumulative steps. Intermediate files remain pending until the final realized-loss guards pass, while preserving completed boundary states for recovery.

The original epoch charged 14,248.8 seconds (3.96 hours), suggesting roughly 12 hours for three additional epochs; this is a planning estimate. The registered job has a 15-hour total bound including cleanup, an exclusive GPU lease, two numeric threads, two planning/loading workers, a 32 GiB available-RAM floor and 150 GiB free-disk floor. Keep the GPU lease through owned-process cleanup. STOP, resource failure or timeout preserves prior donor and newly written boundary files. The new run directory is `runs/combined35m_v50_seed101_continue_epochs2_4`.

This job does not depend on finishing Ceres labeling: it reuses the already admitted V50 corpus. The initial launcher failed before model/data/training because absolute script execution resolved an old project import. The fresh retry uses module execution with an explicit frozen working directory and PYTHONPATH; direct import preflight passed. The active runtime remains isolated at commit `64e34a5e5` while publication proceeds from a separate worktree.

Evaluate learning progress by comparing saved horizons under common search settings. Those games follow completed checkpoints and are not a prerequisite for continuing the registered training. Expanded-corpus preparation proceeds independently on CPU; its exact eligible row count and completion must be established before a larger-data training comparison. Longer training on this same corpus is not additional unique data.

Validation: 15 focused tests passed, including actual tiny first-epoch training followed by three resumed epochs, every intermediate checkpoint's cumulative step count, optimizer fallback rejection, and historical multi-epoch behavior. An actual CPU load of the 35M donor passed equality checks for all restored states. Ruff and diff checks passed; independent review passed the implementation and launcher cleanup. See the [registered command and adoption evidence](evidence/2026-09-17-v50-checkpoint-continuation.json).

Actual launch verification: process 84270 restored the CUDA model and full checkpoint state, completed exact-epoch planning and CUDA warmup, then reported successful 88-step windows through 528 new updates. Its TensorBoard stream records `zclip/restored=1.0` at donor step 68,974. The existing supervisor will run the independently reviewed remaining Ceres labeling after this training job releases the GPU.

## Queued full 50.55M continuation

After the final four-exposure 35M checkpoint and the first expanded corpus are complete, the supervisor now has a registered one-epoch continuation over **all 50,548,069 positions**, comprising the original 35,314,577 plus 15,233,492 new rows (6,185 shards, 31 cohorts). This does not wait for the later 58M preparation. Policy remains B100 at teacher temperature 0.5; V50 remains the equal SF/BT4 value blend.

The job binds the actual successful donor receipt and final checkpoint hash at execution, requires cumulative donor step 275,896, copies that checkpoint into its own namespace, and restores model, optimizer, scheduler and ZClip state. New sampler seed 105 supplies one complete expanded epoch. The existing seed-101 prospective report establishes corpus/recipe admission; the actual seed-105 sampler records its own order, hash and optimizer-step count. Roughly 98,727 additional updates is a planning estimate, not a realized count. This comparison changes both data and training exposure; a matched-additional-step 35M control remains separate work.

Status is **queued conditional, not launched**. The wrapper waits at most six hours for successful donor and dataset receipts without holding the GPU lease, then permits eight hours of training. The existing 35M run and queued Ceres work remain ahead of it. Registration extended the operator deadline consistently to 2026-09-18 16:59:37 UTC. Failures stop this job rather than silently substituting a smaller corpus or an intermediate checkpoint.

Validation: four donor-binding tests passed; host preflight verified every static pin. Independent review caught and closed an admission import/ABI mismatch: the metadata binder uses the qualified Python 3.13 environment and audited-source runtime, while training keeps its proven Python 3.10 runtime. Actual transitive admission/native imports passed. No extra training or corpus payload scan was run for this preparation. Frozen [plan, scripts and adoption receipt](evidence/2026-09-17-expanded50m-continuation/) preserve the concrete queue contract; the host path and descriptor hash remain in the receipt.

## Completed horizons and dataset handoff recovery

The three additional 35M epochs completed successfully in 53,132.97 seconds (14.76 hours), adding 206,922 optimizer updates and ending at cumulative step 275,896. Checkpoints for total exposures two, three and four were preserved. The final checkpoint SHA256 is `e579bd08f90009924941bb5c773d01af836f6500f95a0250eca2918d7b58679c`. This establishes successful training completion, not a playing-strength improvement; horizon arenas follow independently.

The queued Ceres recovery also completed successfully: 2,643,373 remaining rows across the three tail/cohort unions in 2,628.26 seconds (43.8 minutes). Existing completed labels were reused.

The first50M continuation did **not** train: after 4,860 seconds waiting, it stopped on the producer's explicit failure. All ten new cohort writers completed; the failure was in the final union/prospective-schedule integration, whose historical-sampler import guard detected an already imported incompatible sampler. This is a runtime integration failure, not evidence of bad labels. Recovery preserves the completed corpus and trained donor and isolates the admission and historical prospective sampler processes; a fresh training queue namespace will bind the corrected readiness receipt. The original failure artifacts remain intact. Actual completion and failure receipts are [archived here](evidence/2026-09-17-expanded50m-continuation/completed-and-recovery.json).

The isolation repair is frozen at `f8f14d928`: 23 focused tests passed and an actual 8,192-row, three-arm sample passed in a fresh historical-sampler child after the parent had imported the conflicting sampler. The original sampler path/hash checks remain intact. Corrected readiness will refer to the unchanged original union and a fresh `union_recovery_v1/union_admission.json`; neither the ten completed cohort writers nor any GPU training is repeated by that repair.

The corrected **50M recovery is now actually queued** behind four horizon arenas. Its descriptor is `77a49241b87ce1fb2f1a9f152aadc64dfe02dd5d7374d56bc623833be61ffced`; independent review verified the unchanged training wrapper and all 759 listed pins. Only the fresh output/readiness paths and additional completed-donor pins changed. The queue deadline remains September18 16:59:37 UTC; all bounds fit without extension. It remains conditional until corrected CPU readiness succeeds, while the first horizon arena has already banked a game. The [retry adoption](evidence/2026-09-17-expanded50m-continuation/retry-adoption.json) and [compact plan delta](evidence/2026-09-17-expanded50m-continuation/retry-plan-delta.json) retain its exact identity without duplicating the full archived plan.

## September18: handoff correction and sustained expanded training

The ready50M retry remained queued after the arenas because its descriptor included the 666MB donor in the operator's small-file pin list. The supervisor correctly rejected that descriptor before launch; this handoff error caused idle time. The earlier custom preflight was insufficient because it did not run the actual operator validator. A fresh namespace removes that redundant descriptor entry while retaining the pinned successful donor receipt, full checkpoint hashing in the binder, and the trainer's strict resume validation. Both newly registered jobs pass the operator's actual `registered_spec`, and the failed handoff remains preserved.

The first full50M pass now has an actual training process (PID1555633), restored-input binding and the preserved donor copy; optimizer progress is recorded separately when observed. The new supervisor PID1555328 owns it. After this pass, a registered full-state continuation adds three more complete passes with seeds106–108, retaining each epoch boundary. That makes **four total50M passes**, all using the unchanged V50 recipe and full31-cohort union. Expected training is about28hours in total; the global hard envelope ends September20 at12:35:25UTC. These are learning-horizon experiments, not a claim that the longest checkpoint wins. Bounded playing comparisons assess the resulting checkpoints.

Independent review passed first-pass descriptor `4ff6516b…` and follow-on `1115f9c6…`; four follow-on donor-binding tests passed. Actual multi-epoch receipt structure was checked against the completed35M run, including per-epoch planned rows and seeds. The [first adoption](evidence/2026-09-18-expanded50m-sustained/first-adoption.json), [follow-on adoption](evidence/2026-09-18-expanded50m-sustained/followon-adoption.json), compact plan summaries and changed wrapper sources preserve the registered work. CPU58M preparation proceeds independently and does not alter this frozen50M comparison.
