# Independent ray geometry through actual initialized lookup

## Baseline, hypothesis and bounded acceptance

Continue PR #873 at `5bf2220356b50431f88b1785404a8a25d4030fc9`, exact tree
`c3f1c20d0c8e77c932c984adf2d2e99cf0d43f98`. The complete baseline source and
original commit object were recovered through read-only workspace run35999214926.
All 3,523 tracked paths, including ignored tracked files and modes, reproduce that
tree. The compiler remains `aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae`, Bend2.0.21
plus U64, 84 inputs, fingerprint
`d9550e30dbf17f12013aa5db89b27cf6724957fe68213c9c7e409e99f1405ef4`.

This record follows constructive local development and precedes hosted
qualification, not a falsely backdated preregistration. The deciding result is
safe checking of all five public contracts, including actual initialized lookup
equal to independent first-blocker-inclusive coordinate rays for arbitrary U64
occupancies. Internal step/mask certificates must be derived, not assumed as
public desired-result premises. Acceptance also requires all 19 controls, fresh
four-mode native ray and real lookup tests, unchanged compiler checks and full
repository lint. No failure may be relabeled as proof rejection or ignored.

Keep exact-source modular parent110/276 evidence separate from new execution.
The complete 115-law wrapper remains opt-in and is not implied by a focused pass.
No production/checker/old proof changes, merge, deployment, live-process work,
model/GPU/training/benchmark workload, or routine perft budget increase.

## Contracts and connection

The five public laws are seven_steps_reach_edge, ray_matches_independent_path,
slider_matches_independent_rays, relevant_mask_preserves_attacks, and
initialized_lookup_matches_independent_rays. See the ray suite README for exact
domains and commands. Valid square/direction/key bounds are explicit. The final
contract needs symbolic depth=17, a selected-block table budget <=128 and an
extras square budget <=64. Seed and occupancy are arbitrary; the actual array
and complete final returned pair remain in the statement.

The independent specification uses Nat coordinates, explicit boundaries and a
finite list traversal that includes the first blocker. The source checker checks
all geometric routes and that seven steps reach an edge. Occupancies are not
case-enumerated. The production accumulator loop is refined by structural fuel
and path induction. Structural bitwise lemmas show the actual relevant mask,
via its earlier independently proved full-mask identity, preserves all interior
blocker observations. Terminal occupancy is irrelevant by the saved path lemma.
The final composition obtains stored headers, computed data and bounds from
existing actual-pipeline proofs rather than assuming their desired values.

## Initial local execution status

The general traversal and complete seven-step ray refinements pass. The final
structural masking and unmasked/masked slider composition also passes with exact
safe success (Geometry.bend, 316.581 seconds). The final importing public consumer
and final controls are running separately; no five-law/full-aggregate result is
claimed by this prequalification record.

All four native modes already pass 7,008 distinct rows each: 4,960 rays and
1,024 each masked/unmasked sliders. The first-blocker budget covers all1,456
positions across all512 square/direction domains. Each mode rejects nine invalid
requests. Both actual ignore-blocker and omit-blocker corruptions compile/run and
are rejected at row2. These modes repeat fixtures, not disjoint or exhaustive
U64 coverage. Source/runtime candidates never receive oracle answers.

Original compiler16-law/seven-control suite and all12 pin tests pass locally.
Unchanged local repository lint fails because Ruff/Basedpyright/Vulture are
absent. Hosted locked-environment lint is required; that failure is retained.

## Construction failures and optimization

An initial algebra substitution was reversed and rejected normally; it was
replaced by explicit equality congruence/transitivity without changing the
statement. A finite interior-mask draft passed in388.166 seconds but was replaced
by a stronger generic structural mask-superset certificate. A separate finite
symbolic-occupancy slider draft was stopped after399.264 seconds without result;
scalar-only key certificates avoid redundant occupancy-dependent expansions.
No stopped draft is counted as a pass. The earlier terminal-path and geometry
archive files are reused byte-for-byte. No checker, theorem domain or trusted
assumption changed to optimize proof construction.

## Trust and remaining scope

If all acceptance gates pass, the initialized source pipeline's returned attack
is related to an independent coordinate/blocker specification, including masked
occupancy irrelevance. This is not a theorem of compiler lowering, physical array
allocation/ownership/lifetime, ABI, C/C++ toolchain, libraries, OS or hardware.
It is not a complete chess legality, parser, search, model or training theorem.
No separate closed normalization of the enormous literal Tables.build expression
is asserted; the universal budgets include its full configuration.

Self-review only, not independent review. Pinned checker/Base and existing
compiler limitations remain explicit. Python export/references/data/control/
training and transitional C++/LibTorch/AOTI remain application dependencies.
