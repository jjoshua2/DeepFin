{
  "status": "FAIL: exploratory native C generation, not a successful native qualification",
  "compiler_revision": "aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae",
  "bun": "1.4.2",
  "failed_stage": "Bend to C emission before C compilation or native execution",
  "diagnostic": "an open Array element type",
  "expected_status": 0,
  "observed_status": 1,
  "source_relative_import_base": "native/bend_engine/standalone/proofs/normalization/",
  "source_sha256": "36f97d4efc54850f3b58ed565a6ebc8936b93f347458705844408edf126bc786",
  "raw_source": "# Opt-in comparison of public wrappers and their actual unmasked Base routines.\n# No proof representation or oracle is linked into this executable.\nimport Base\nimport ../../Text.bend as Text\n\ndef show(+v: U64) -> String:\n  U32.show(U64.high(v)) ++ \" \" ++ U32.show(U64.low(v))\n\ndef read(op: U32,a: Array<U64>,i: U32) -> Array<U64> & U64:\n  match op:\n    case 0: Array.get(U64,a,i)\n    case _: Array.get.go(U64,a,131072,i)\n\ndef write(op: U32,a: Array<U64>,i: U32,v: U64) -> Array<U64>:\n  match op:\n    case 0: Array.set(U64,a,i,v)\n    case _: Array.set.fin(U64,Array.swap.go(U64,a,131072,i,v))\n\ndef emit(id: U32,+i: U32,initial: U64,after: U64,r: Array<U64> & U32) -> IO(Unit):\n  (a,n) = r\n  IO.print(U32.show(id) ++ \" \" ++ U32.show(U32.and(i,U32.sub(131072,1))) ++ \" \" ++ U32.show(n)\n    ++ \" \" ++ show(initial) ++ \" \" ++ show(after))\n\ndef after(id: U32,i: U32,initial: U64,r: Array<U64> & U64) -> IO(Unit):\n  (a,v) = r\n  emit(id,i,initial,v,Array.size(U64,a))\n\ndef initial(id: U32,+op: U32,+i: U32,+j: U32,v: U64,r: Array<U64> & U64) -> IO(Unit):\n  (a,old) = r\n  after(id,i,old,read(op,write(op,a,i,v),j))\n\ndef run(id: U32,+op: U32,+i: U32,+j: U32,v: U64) -> IO(Unit):\n  initial(id,op,i,j,v,read(op,Array.new(U64,17n,U64.from_parts(3735928559,2309737967)),j))\n\ndef checked(ok: Bool,id: U32,op: U32,i: U32,j: U32,v: U64) -> IO(Unit):\n  match ok:\n    case True{}: run(id,op,i,j,v)\n    case False{}: IO.die(Unit,2,\"invalid normalization fixture or budget\")\n\ndef parse(+id: U32,op: Maybe<&2,U32>,i: Maybe<&2,U32>,j: Maybe<&2,U32>,hi: Maybe<&2,U32>,lo: Maybe<&2,U32>) -> IO(Unit):\n  match op i j hi lo:\n    case Some{+o} Some{+x} Some{+y} Some{h} Some{l}:\n      checked(Bool.and(U32.is_lt(id,256),Bool.or(U32.is_eq(o,0),\n        Bool.and(U32.is_eq(o,1),Bool.and(U32.is_lt(x,131072),U32.is_lt(y,131072))))),id,o,x,y,U64.from_parts(h,l))\n    case _ _ _ _ _: IO.die(Unit,2,\"invalid normalization integer\")\n\ndef cases(xs: List<String>,+id: U32) -> IO(Unit):\n  match xs:\n    case Nil{}: IO.pure(Unit,Unit{})\n    case op <> i <> j <> hi <> lo <> rest:\n      do IO<Unit>:\n        parse(id,Text.number(op),Text.number(i),Text.number(j),Text.number(hi),Text.number(lo))\n        cases(rest,U32.inc(id))\n    case _: IO.die(Unit,2,\"expected five normalization operands\")\n\ndef main() -> IO(Unit):\n  do IO<Unit>:\n    xs : List<String> <- IO.args()\n    cases(xs,0)\n",
  "log_sha256": "a4f4b2c797f57ef0d44953eae981e59729caf9c9ab7310c76bacc4d0756c9f69",
  "raw_log": "12 | const root=path.resolve(suite,'../..'),engine=path.dirname(root),cc=process.env.CC||'clang';\n13 | const temp=fs.mkdtempSync(path.join(os.tmpdir(),'deepfin-normalization-native-'));\n14 | const sha=b=>createHash('sha256').update(b).digest('hex');\n15 | function invoke(cmd,argv,timeout=90000){const r=spawnSync(cmd,argv,{encoding:'utf8',timeout,maxBuffer:16<<20,env:{...process.env,BEND_NO_TELEMETRY:'1'}});\n16 |  assert.equal(r.error,undefined,`${r.error}`);assert.equal(r.signal,null);return r;}\n17 | function run(cmd,argv,timeout){const r=invoke(cmd,argv,timeout);assert.equal(r.status,0,r.stderr+'\\n'+r.stdout.slice(0,3000));assert.equal(r.stderr,'');return r.stdout;}\n                                                                            ^\nAssertionError: Error: an open Array element type\n\n\n\n1 !== 0\n\n generatedMessage: false,\n     actual: 1,\n   expected: 0,\n   operator: \"strictEqual\",\n       diff: \"simple\",\n       code: \"ERR_ASSERTION\"\n\n      at run (/mnt/data/path-work/native/bend_engine/standalone/proofs/normalization/verify_native.js:17:72)\n\nBun v1.4.2 (Linux x64)\n",
  "resolution": "No compiler fix. Final native probe exercises supported Array.get/set wrappers only; internal unmasked routines retain source-only qualification."
}
