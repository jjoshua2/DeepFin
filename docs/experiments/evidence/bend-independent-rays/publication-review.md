# Independent-ray publication verification and full-domain witness

## Exact hosted source and outcome

Run **36002630448** passed preparation, source, controls, runtime/lint and
publication on its first attempt. Qualified source is
`58c9136bccdf6667a36776fb48480eab7ebfe5a2`, complete tree
`69648f55cc70d02f667576b3345140860e648f8a`. The evidence successor is
`885b363fbc532673575c72e180b32e03aef36f8d`, tree
`f2e8851fdd299da77a7eb7aec9b9ad46d36b5bd9`. Its changes are documentation and
qualification evidence only. Base is #873 at
`5bf2220356b50431f88b1785404a8a25d4030fc9`.

Jobs: source **107642854018**, controls **107642854103**, runtime/lint
**107642854096**, publication **107646841854**. The five public laws and importing
consumer executed in one safe source check; the nineteen controls executed
separately. The latter correctly records its consumer/focused status as NOT_RUN.
Retaining the unchanged exact-source 110-law/276-control parent gives modular
**115-law/295-control evidence**, not a freshly executed combined wrapper.

The P2 result now relates actual initialized Chess.slide to independent natural
file/rank first-blocker-inclusive attacks for arbitrary U64 occupancies. The
complete final array is part of the result. Caller premises are symbolic depth
17 and bounded selected-block/extras budgets, not assumed masks, headers, data,
index results, geometry equalities or per-write correctness. Actual producers
supply those intermediate certificates. Seven-step edge completeness ensures the
independent path is not an unjustified truncated ray.

The independent Spec.rays result calls Path/Rays/Grid and Base, not production
ray/slider/lookup. The same Spec file also has input-route checks referring to
production steps; those are internal certificates, not the independent answer.
The five earlier archived coordinate/mask/path helpers are retained byte-for-byte.
Masking is linked structurally to interior occupancy observations and the saved
terminal-path frame theorem, rather than assumed irrelevant.

## Downloaded artifact and source identity

Artifact **10809054000**, ray-final, ZIP SHA-256:
`842bb78052e9c6a3b66138ae7f29c5bc0d7f4c5634a12f718c1f2cb2ba493099`.

The complete downloaded archive reconstructs all **3,562 tracked paths**, the
exact evidence-head tree and original commit object. All **367** candidate native
source entries match both the artifact and final local candidate. The original
137,058-byte source patch has SHA-256
`b364f599776dc662970f57c6e7e98c15e3d95235adc06676cb3ada0ef9ed34f2`;
reapplication to a fresh exact baseline produces the qualified source tree.

The new ray-native JSON matches the saved local report except cc. The freshly
executed old actual-lookup report matches its prior qualified JSON except cc.
All nineteen control classifications and original diagnostic strings/hashes match
the local report. The sole source-closure difference is a recorded trailing
blank line removed from new Algebra.bend before hosted execution:

- Local checked hash: `1378638271f834e4b8e3e71292ff050df92003558228764c44ef18869032c592`.
- Exact hosted hash: `9b654dc68a5bb5c1d47232e2be23200eebcbd1bfa6b071a5bee60d7c7077ab69`.

No term or statement changed; both hosted source and controls rechecked the exact
clean candidate. The local proof report is not falsely called byte-identical to
the final report. The original final local consumer passed in 695.930 seconds,
controls in 704.726 seconds, and the geometry module in 316.581 seconds.

Native ray/slider tests check 7,008 distinct fixture rows per mode: 4,960 ray,
1,024 unmasked slider and 1,024 masked slider cases, all 512 square/direction
combinations, all 128 keys and all 1,456 possible first-blocker positions. Actual
ignore-blocker and omit-blocker mutations compile/run before rejection. The
separate actual builder/lookup verifier checks 1,024 rows, 1,022 distinct inputs,
all 128 keys and its shifted-prefix corruption. Both suites run generic, portable,
native-target and UBSan. These modes repeat fixtures, not disjoint or exhaustive
U64 datasets; query checks are not full-array or native lifetime proofs.

Original compiler 16-law/seven-control checks and all 12 compiler-pin tests pass.
Whole-repository Ruff/Basedpyright/Vulture passes unchanged. The historical local
missing-tools lint failure remains a failure; the hosted locked environment
resolves its qualification gap. Compiler pin and its 84-file fingerprint remain
unchanged. Clang18 is scoped to native checks, not Python extension compilation.

## Supplementary full-build domain certificate

`FullDomain.bend.txt` is the exact separately checked source, archived as text
rather than added to the registered five-law suite. For every Nat key k<128 it
constructs the before/after decomposition with before=k, after=127-k and start=0,
proving the block count is exactly128, the selected key is k, and the table budget
holds. A separate closed witness establishes extras count64/start0. Thus the
initialized theorem's domain contains a full-build instance for every supported
key, not only first/last examples or partial builds.

This is a domain certificate, not an additional attack theorem or a separately
normalized enormous literal Tables.build term. The public initialized theorem
already quantifies over the parameters it supplies. It is not counted as a sixth
law or added to hosted295 controls, and no native execution of this certificate
is claimed.

The final supplementary file passed with exit0 and exactly `All terms check.` in
0.910 seconds. Its SHA-256 is
`df2868f0969d88c879ab288d344c3048a07ad7815da09f08e376f7dc4f72133a`.
Two disposable changes were rejected at full_configuration with ordinary
expected/observed diagnostics: using126 rather than127 for the suffix count, and
selecting k+1 instead of k. The first standalone check exposed eight inherited
TODO obligations from a missing proof import; adding the unchanged existing
Sequence PROOF import resolves them. That initial nonzero result was not ignored
or counted as proof rejection. Exact receipts are in full-domain-review.json.

To reproduce, copy FullDomain.bend.txt into a disposable qualified checkout as
native/bend_engine/standalone/proofs/full_domain_review/FullDomain.bend and run:

```sh
BEND_NO_TELEMETRY=1 bun /path/to/pinned/bend/bend2/main.ts \
  native/bend_engine/standalone/proofs/full_domain_review/FullDomain.bend
```

## Retained development failures and review limits

An initial algebra substitution was rejected and replaced with explicit equality
congruence/transitivity. A finite interior-mask draft passed in388.166 seconds;
a stronger structural mask-superset proof replaced it. A separate finite symbolic
slider draft was stopped after399.264 seconds without a result. Its replacement
factors scalar-only key metadata instead of repeatedly expanding occupancies.
Earlier native binder errors and the final whitespace-only cleanup were fixed
before hosted qualification. Original draft sources, receipts and logs remain in
the review package. No checker, theorem domain or accepted prior law was changed.

This final archive/review addition changes documentation/evidence only. Qualified
primary sources are unchanged; no redundant full suite or lint was run for it.
The supplementary domain consumer and two diagnostics were newly executed and
reported separately. Self-review only, not independent review. Source/hash/report
agreement is provenance, not a second reviewer.

Source results trust the pinned checker/Base; native lowering, allocation,
ownership/lifetime, ABI, toolchain, libraries, OS and hardware remain separate
trust boundaries. Existing compiler TypeScript and diagnostic/raw-helper issues
are not fixed or suppressed. No production runtime, earlier proof, permanent
workflow, routine perft budget, model/GPU, training or benchmark changes. No new
Python application responsibility moved into Bend; export/references/data/control/
training and transitional C++/LibTorch/AOTI remain dependencies. Nothing merged,
force-pushed, deployed or changed in a live process.

The independent attack/storage connection is now source-proved for the supported
initialized pipeline. Follow-on work should review and consolidate that P2
certificate at the public builder boundary, then advance the separate board/move,
protocol, search and model obligations. It should not repeat the now-proved
universal ray/masking connection or call this a bug-free whole engine.
