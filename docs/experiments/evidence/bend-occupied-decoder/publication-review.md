# Occupied-decoder publication verification

## Exact source and completed qualification

Hosted run **36083420058**, job **107910029481**, passed every stage on its first attempt: source recovery, retained-parent verification, four-law consumer and eighteen controls, four native modes and three behavioral mutations, original compiler checks, locked CPU environment, unchanged repository lint, and publication.

Qualified implementation: `4c5232cc1e6fceb8adc66dcc2dd5479b373ead3f`, complete source tree `c6d133f9122ac3438d9b9a7e4560ac03e67dd4e4`.
Evidence successor: `8d8600e4ee9b5b484bc941aac6302c9ddcb80cd0`, tree `c6e3c3b49f196b294b5e1fa02a3f951ce290b99f`.
Parent: #878 at `1337d5a30e42a1b2999819a36e3b538ace3e7071`, tree `f4bf89c8bb0cfd266d3438a3707795c5fa5d5281`.

Compiler remains `aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae`, Bend 2.0.21 plus U64, 84 inputs and fingerprint `d9550e30dbf17f12013aa5db89b27cf6724957fe68213c9c7e409e99f1405ef4`. The compiler branch was refreshed; its separately recorded strict-TypeScript failures remain unresolved. No checker or compiler source was modified.

This final review file is documentation only. All primary executable/proof/test files remain those qualified. No redundant source/native/lint run is implied by this addition, and no merge, force push, deployment or live-process operation occurred.

## Download and source reconstruction

Qualification artifact **10842512428**, ZIP SHA-256 `a6fe9b6540b062f9a32a4df897939f1cbcef222bb1ec44670a0a237ae0732e00`, was downloaded and inspected. Its complete archive reproduces all **3,644 tracked files/symlinks**, the evidence-head Git tree, and its original commit object. Directory entries are not included in that tracked-file count.

All **404 candidate native-source manifest entries** match both the hosted archive and local final candidate. The entire focused proof/control report equals the local report except measured consumer seconds: 7.557725304 hosted versus 6.793853886 local. The complete native report equals the local report except the recorded C compiler version. Originals were retained, not normalized or rewritten to force agreement.

The 103,763-byte source patch has SHA-256 `a2e0de3c815aac55090ccf6a137d82dfab7e5c9d4ed94667a5f46fd53147ac5d`. Applying it to an independent Git index of the exact complete parent reproduces the qualified source tree and passes Git whitespace checks. The two transport fragments matched their expected Git hashes without repair.

The exact-source parent result is retained after checking all 391 previous native-source entries, its 123-law/332-control qualification, focused consumer/control report, and successful hosted job. With the newly executed four laws/eighteen controls, coverage is **modular 127 laws / 350 controls**. The complete 127-law wrapper was not executed; its inherited gates are unchanged and remain opt-in.

## Claim and assumptions

All four new laws are universal. The global per-square partition and square index below 64 are explicit input conditions. Only the raw occupied-decoder law additionally requires the actual occupancy observation to be true. Metadata remains arbitrary.

The independent classifier recognizes thirteen states: empty or exactly one of six kinds of one color. It does not use the optimized decoder's priority fallback. Structural Word projection and a checked correspondence to actual U64.test_bit connect the global invariant to real square observations. A shared finite Boolean proof supplies both abstract encode/classify reconstruction and the decoder bridge. This is not an inference from native sample counts.

The importing consumer combines occupied decoding with abstract reconstruction to prove that the **actual decoded kind and color reproduce all eight observed kind/color bits**, not merely that the tag lies in range. It consumes actual initial-board consistency and fresh-insertion examples at low/high-limb boundaries. Empty and inconsistent cases establish the needed domain distinctions.

`Spec.decoded` is an explicitly guarded proof-interface observation, not a new production API. The native probe does not import or execute Spec, the abstract classifier, or the proof representation; it observes the corresponding actual Chess functions directly. Production Chess.piece retains its empty-square fallback 5. Its occupied contract never treats that fallback as an empty-square tag.

This establishes pointwise abstract-square correspondence. It does not establish an unrestricted Cell encode/decode bijection, a full Board/abstract-board bijection, valid metadata, parser reachability, legal positions, or move correctness. The existing board invariant permits positions that are not legal chess positions.

## Checks and regression witnesses

The focused command actually executed its four-law consumer and all eighteen controls. Nine are ordinary semantic/refinement failures: seven mutations to actual operations or the independent classifier, plus two concrete false-premise consumers. Eight enforce manifests/imports/regular files/no unsafe or holes. One synthetic warning-output unit is not an additional compiler execution. Status zero with extra warnings does not pass; exact `All terms check.` is required. Missing files, malformed terms, affine-use errors, crashes and timeouts are not counted as semantic rejection.

Each native mode passes **1,920 rows and 21,120 output fields**: all 832 square/local-state combinations, all 768 fresh square/kind/color insertions, 64 actual initial-board queries, and 256 additional consistent-board queries. There are 1,751 occupied and 169 empty observations. The occupied rows also reconstruct the eight plane bits from actual kind/color outputs. Empty rows test the unchanged raw fallback explicitly outside the occupied premise. Nine malformed requests are rejected by the bounded probe, not by an asserted input-validation feature of the raw Chess API.

The independent reference uses a 64-square array of optional kind/color values. Candidate code receives input bitboards, not expected decoder tags. It executes real Chess reads and Position.put/start. Modes repeat fixtures; they are not disjoint or exhaustive arbitrary-board datasets. These are square observations rather than complete Board-result or unrelated-field preservation comparisons.

The three native mutations have occupied, consistent witnesses only. A wrong pawn tag fails row 0/field 2 with 1 rather than 0; a wrong king fallback fails row 10/field 2 with 4 rather than 5; intersecting rather than unioning color planes fails row 0/field 1 with 0 rather than 1. Each deliberately corrupted program compiles and executes before the independent values reject it. They are deliberate controls, not discovered production defects. The pawn corruption returns another valid tag, demonstrating that a range-only check would be insufficient.

The common native output hash is `f1eb0aec05aac221ccc564cbaccab282abe0e75bb83cf2a4deef9cf33844e91d`. Generic, forced-portable, native-target and UBSan all pass. Local Bun 1.4.2/Clang 17; hosted Bun 1.4.2/Clang 18.1.3. Original compiler 16-law/seven-control checks, including cyclic-template rejection, and all twelve pin tests pass locally and hosted.

## Retained failures and review limits

Earlier bit-projection drafts failed explicit duplication/pattern and empty-domain elimination details. The native probe initially reused an unmarked square argument and was rejected before C generation. Those were corrected without changing the compiler or previous accepted sources. A shared row-case proof replaced duplicate finite proofs while preserving the public statements.

Several surrounding execution calls ended before completing a focused run. Partial logs are not counted as passes. The final supervised local focused command completed successfully in 49.437385563 seconds. Concrete empty/inconsistent witnesses replaced overly broad error-producing control drafts; the four laws and their intended domains were not changed. No claim is made that the earlier incomplete calls produced valid semantic rejection.

Local whole-repository lint returned nonzero because Ruff, Basedpyright and Vulture were absent. Its original log is retained in the review package. The fresh hosted locked Python 3.13/uv 0.12.10 CPU environment passed the unchanged lint command. Clang 18 was scoped to native probes, not exported into the Python extension build.

Self-review only, not independent review. Exact source/report agreement establishes provenance rather than a separate reviewer. The pinned checker/Base, native lowering/storage, ABI, C/C++ toolchain, libraries, OS and hardware remain trust boundaries. Previously recorded compiler and closed-builder limitations are unchanged.

No production runtime, earlier accepted law, compiler input, permanent workflow, routine perft budget, search, model/GPU, training or benchmark change. No additional Python application responsibility moved into Bend; export, references, data/control orchestration and training remain dependencies, with C++/LibTorch/AOTI transitional inference.

Next decisive P3 work is parser-derived freshness and whole-board abstraction, then invariant-preserving removal/move/special-move operations. King safety, legal-move generation soundness/completeness/no duplicates, reachability and perft remain separate obligations.
