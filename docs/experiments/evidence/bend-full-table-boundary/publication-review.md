# Full-table boundary: publication review and evidence distinctions

## Completed hosted qualification and exact publication

Run **36009970976** completed every preparation, source, controls, runtime/lint and evidence-publication job successfully. Source commit `12327c34c5279fd41ae66eb10ab42470bb06d199`, tree `7199d988f42077327279caeae493e6241c8d80e0`. Evidence head `b26e63d80c2cd398638499bd4027698273093594`, tree `322a84389b14408c30a510a84d51f6de1996b96f`. The hosted consumer passed with exact safe output in623.433seconds. Jobs: source107667936016, controls107667936093, runtime/lint107667936118, publication107672383599.

The new consumer and20-control gate ran separately. Exact-source115-law/295-control parent evidence is retained, yielding modular117-law/315-control coverage. The full117-law wrapper was not run. Four controls are actual semantic/refinement failures, seven are public-recipe guard failures, eight are policy failures, and one is a synthetic output unit check. Those categories must not be flattened into20 compiler rejections.

Downloaded qualification artifact10812567330, ZIP SHA-256 `e194f377d67664331a405b12c28ac102d61fbd3f2e408b2b289e6bc1109a5b29`, reconstructs all3,591 tracked paths, the exact evidence-head tree and its original commit object. All379 native-source manifest entries match the local candidate; all20 controls match the local JSON exactly. The native baseline report matches apart from cc. Public-builder mutation diagnostics differ by temporary path/CI annotations (928 local bytes,1287 hosted), while both reject the same final-key result. No full outer-native-report byte identity is claimed.

The source patch is42,257bytes, SHA-256 `6bc9f65ad86bcfc1f73d1e5d6b32bcbc04dc2f96b8710f59a3d52a5959d4a94c`. Its reapplication to a fresh index of the exact original parent reproduces the qualified tree. Hosted Ruff/Basedpyright/Vulture passes unchanged; original compiler16-laws/seven-controls and12 pin tests pass. Both generic/portable/native-target/UBSan lookup tests and the actual short-builder regression passed. No source was changed after these checks.

## What this increment changes

The two public builder contracts remove the selected block's before/after decomposition from the interface. They quantify over the same symbolic full-table expression for every key, expose Nat and actual engine U32 keys, and return the complete final array paired with independently specified attacks. The existing ray theorem and archived full-domain certificate are reused; this is not a new independent proof of ray geometry or unrelated chess rules.

The public zero-argument Tables.build function remains unchanged. A separate token-level source guard matches its current signature and complete body to the certified construction defaults: depth17,128 table blocks from key0/offset512,64 extras from square0,zero seed. The symbolic Spec.run is also checked. This check is intentionally stricter than semantic equivalence, and is not a Bend source-equality witness.

The failed direct closed-equality attempt was bounded at30seconds. It is retained as a timeout, not as evidence of failure of the existing supported symbolic theorem and not as a discharged public closed-builder law. No checker or earlier proof statement was modified to evade that limit.

## Local work actually executed

The final local consumer discharged both public contracts and their importing use with status0 and exactly `All terms check.` in672.967seconds. The20-control run passed separately on the final cleaned sources. Its report is JSON-identical to the independently downloaded hosted controls, including144 source-closure identities. Controls-only reports intentionally do not claim consumer execution.

The native wrapper freshly ran all four unchanged actual builder/Chess.slide modes and preserved every query, malformed request and shifted-lookup regression. Its new isolated mutation shortens only Tables.build to127 blocks. The modified program compiles and runs, then the unchanged oracle rejects row1016, key127: `127 65793 16843134` versus `127 4202512 134480385`. The source-recipe guard independently rejects the changed wrapper. This is not a discovered production bug: the deliberate incorrect builder is a regression control.

Original compiler source proofs and all12 pin tests passed locally. Local lint failed because its tools were missing; that original nonzero log remains retained rather than overwritten. Hosted lint results are reported separately.

One trailing blank line in Binding.bend was removed before hosted checking. The local consumer began with the prior whitespace; no source term changed. Hash before cleanup: `0d0807b005324b0a0b556fd7348945c73aab0ce6be33eb1b67fb28b717b2710d`; hosted hash: `a5148d071387d9f7f800cb17047af050c4da994372a57820e8ff2a8ed6d8e122`. Final local controls checked the cleaned file. Both hashes and the actual consumer receipt are preserved.

The complete original baseline tree was restored exactly. Initial metadata reconstruction failed because the API normalizes timestamps to UTC. A subsequent bounded local reconstruction recovered the original author/committer offsets (-0400) and absence of a trailing message newline; its raw Git object hashes exactly to6046edb83dad6a30528f736eb0bc1546cedc3254. That exact object is preserved. The earlier mismatch and explicitly labeled local snapshot remain historical; remote publication always used the real parent SHA. Tree identity and commit metadata are not conflated.

## Supplementary token-guard robustness

A separate small JavaScript test accepts harmless comments/spacing and a commented-out fake definition. It rejects a second actual definition, altered symbolic depth/count/seed, an extra statement, and unsupported rewritten expressions. Those are eight supplementary guard checks, not new public laws or additions to the20 registered controls. The complete driver and report are archived; the guard itself is unchanged.

The seven registered builder-default mutations and these supplementary checks test source linkage, not native behavior or formal equality. The new native short-builder test supplies distinct behavioral evidence. Missing files, parser/affine/termination errors, crashes and timeouts never count as successful formal semantic rejections.

## Supplementary P3 premise checks, not a new invariant suite

Three closed source-derived facts passed the same pinned checker with exact safe output. On `Position.empty()`, `Chess.piece(...,0)` returns5 (the king fallback), not an empty-square tag. Inserting a black knight and then a white pawn onto the same square using the additive parser helper leaves both color bits and both piece-kind bits set. These are counterexamples to an unconditionally total empty-square decoder or replacement insertion specification, **not demonstrated production defects**.

They establish useful domain boundaries for the next P3 proof: decode under an occupied-square invariant, and constrain parser insertion by freshness or another explicit invariant-preservation condition. They do not prove board legality, parser reachability, move semantics or any FIDE rule. Wrong empty-tag and unconditional disjointness claims are rejected with ordinary expected/observed diagnostics at the intended functions. These three closed facts and two supplementary diagnostics do not increase the117/315 counts, and no new native execution is claimed.

Reproduce by copying `Preconditions.bend.txt` to `native/bend_engine/standalone/P3PreconditionsReview.bend` in a disposable qualified checkout, removing only `.txt`, then run the pinned `bend2/main.ts` checker on that file. The archived source SHA-256 is `13c09343be832cbaa2d5ad516dacccb8777743cc2f2e2142a0e258a045db46f7`. Existing Chess and Position source hashes are recorded in `p3-preconditions-receipt.json`. Remove the disposable file afterward; it is not a permanent source-gate dependency.

Reproduce the guard-only supplementary checks by copying `recipe-review.js.txt` to a `.js` file and running `bun /path/to/recipe-review.js /path/to/checkout`. Its report is in `recipe-review.json`; it changes no original source file and adds no registered rejection count.

## Scope and trust

Every earlier accepted law, production function, compiler input and permanent workflow remains unchanged. This final review/evidence addition must not alter the primary proof/test sources. The full expensive117-law wrapper is not implied by modular receipts. Self-review only, not independent review; source/hash agreement is provenance rather than a second reviewer.

No extra perft depth or routine compute budget, model/GPU execution, training, deployment or benchmark was added. No Python application responsibility moved into Bend. Export/references/data/control/training and transitional C++/LibTorch/AOTI remain dependencies. Source checker/Base, native lowering, allocation/ownership/lifetime, ABI, toolchain, OS and hardware remain trust boundaries. Existing compiler strict-TypeScript and diagnostic/raw-helper limitations are not addressed here.

The next substantive step is the independent board-representation/move specification and its invariants (P3), retaining separate protocol/search/model obligations. A direct closed Tables.build equality remains explicitly unqualified; the current public wrapper instead has exact source linkage plus native validation to the proved uniform symbolic recipe.
