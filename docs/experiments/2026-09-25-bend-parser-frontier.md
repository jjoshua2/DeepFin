# Accepted parser frontier and fresh insertion

## Scope recorded during construction, before final qualification

Continue #881 at c3f9dd30cf58c90f91dbac6a7f74ec0d8ee18241. Preserve the separate saved parser-rejection candidate at 71f391281bb0e6b6da0ba11b6a47f4d9873f4999; its proof names and public-FEN result are not silently substituted for the published prefix suite. Repository instructions, development guidance, branch lifecycle and experiment index were read. Work uses an isolated restored checkout; no live process, merge, deployment or production edit.

This is not a backdated preregistration: mask and one-step construction checks precede this record. Acceptance requires exact safe success for the public consumer, intended semantic/policy controls, actual-parser native differentials, unchanged compiler checks and repository lint. Full parent aggregate execution is not implied by modular receipts. The proof compiler stays aaeb9bc91ff0ff0b3f58dba6a9744c6607e167ae with its existing 84-input fingerprint. All new work is opt-in. No model/GPU, training, search, benchmark or extra perft compute is used.

The intended invariant says that a true actual validity flag implies bounded cursor coordinates, representation consistency, and no color occupancy in unvisited squares. The unvisited mask is defined by independent natural file/rank observations, not the parser's output board or desired insertion result. Rejected states may contain modified temporary boards; the invariant is conditional on validity and does not claim rollback. Initial bounds and empty storage come from the actual fixed initialization, not a caller-supplied true flag.

Earlier drafts used a shifted frontier formula, then switched to direct coordinate Word construction to keep checking bounded. No previously accepted theorem changed. Draft pattern-order and irrelevant-field normalization mismatches were corrected before the relevant helper checks passed. An outer Facts check timed out without a completed receipt; it is not counted as successful qualification.

## Execution status

Under construction. No completed public-law or final native qualification is claimed in this initial record.

## Local candidate results, before hosted qualification

The four-law importing consumer and all seventeen controls passed in one final
focused command. The native verifier passed all four modes on1,614 distinct
placement strings and29,526 complete character-boundary states per mode, with
679,098 field comparisons. It observes26,879 live states,2,647 invalid states,
899 accepted final strings and7,197 live typed piece transitions. Every one of
768 single-piece square/kind/color placements is included. Two deliberately
broken actual cursor updates compile and execute before wrong-state rejection.
The two bounded probe-budget inputs are rejected in each mode.

These fixture counts repeat across modes and are not exhaustive String/Board
coverage. The probe runs the real character transition and optional finalizer,
not proof predicates; the String induction relates it to the actual full parser.
Complete state fields include the cursor, validity, optional acceptance, eight
bitboards and three metadata fields. No full six-field FEN result or legal chess
position is claimed.

The public result predicate treats None explicitly and certifies the Board only
when a placement result is present. Its nonvacuity is demonstrated by an accepted
mixed complete placement and by full native acceptance comparisons. The separate
exact typed-transition theorem is inherited; mere invariant preservation is not
presented as an exact abstract parsing theorem.

Four universal contracts add to exact-source retained parent137/384. Modular
coverage would be141 laws/401 controls once final evidence is recorded; the full
141-law wrapper is not claimed executed. All429 inherited native-source manifest
entries must match before publication. The existing saved local rejection patch
and published parser-prefix branch remain distinct and preserved.

Constructive drafts encountered explicit-binder, match-order and False-versus-Nat
proof mismatch errors; they were corrected before acceptance, not counted as
successful controls. A shift-based independent frontier expression was replaced
by a direct coordinate Word construction to avoid costly normalization. No
accepted public law or compiler changed. An initial native probe used a nested
match in a do block and failed parsing; the final top-level match compiled.
The final proof and native verifier completed without weakening domains.

Original compiler16-law/seven-control and12 compiler-pin gates passed locally.
Local unchanged repository lint failed because its tools are unavailable; its
exact log is retained. Hosted lint remains required for publication. One trailing
blank line in Finite.bend was removed after the first successful focused run;
a full final local focused run was started on the cleaned source before staging.


## Completed hosted qualification across exact-source runs

Qualification combines the passed four-law/17-control source gate from run36150116903 with fresh native/compiler/lint success in recovery run36150660570, both on exact source tree efa431703a1d47208655595f816a3008d0c4a27a. Published source commit: `be5afc6308d159d52f42b2fcccb59a9c20109b4c`.

The first workflow passed the four-law consumer and all17 controls, then failed native-target compilation because Clang18 emitted invalid-feature-combination warnings for the host AVX10 feature set. Generic and portable fixtures had passed, but no complete native result or lint pass is attributed to that run. Its original failure log is committed.

Recovery retained the exact passed source result, checked the complete444-entry manifest, and selected an installed host compiler accepting the unchanged native flags without warnings. The full actual four-mode native gate was rerun successfully; no warning-suppression flag, expected result, proof statement or candidate source was changed. The selected compiler and every compatibility attempt are recorded. It is scoped to native tests only; Python tools use the normal compiler.

All four public laws and17 controls ran together in the first focused command. Eight controls are ordinary semantic/refinement failures, eight enforce manifests/import policy, and one synthetic warning-output test is not a compiler execution. Two actual cursor mutations fail existing implementation bridges and are explicitly not credited as isolated new lemma failures. Missing files, crashes, affine errors and timeouts are not semantic rejection.

The invariant derives bounds, partition consistency and empty unvisited squares along the actual initialized parser trajectory. Every live typed insertion has a target below64 that was fresh in its input Board. Every Some result of actual placement finalization is consistent. Rejected temporary Boards may be inconsistent; no raw rollback or full six-field FEN correctness is claimed.

The new native verifier passes1614 distinct strings,29526 full character-boundary Layout states and679098 fields per mode:26879 live,2647 invalid,899 accepted final strings and7197 live typed insertions. All768 single-piece combinations are included. Two malformed probe-budget requests and two compiled/executed cursor-corruption controls are rejected. Modes repeat fixtures, not exhaustive strings or legal-game coverage. The probe exercises actual layout_char/layout_result; source induction connects those to actual layout.

Exact-source parent137/384 is retained, producing modular141-law/401-control coverage. The full141-law wrapper was not executed. The first passed source gate is not falsely described as rerun in recovery. Fresh original compiler16-law/seven-control and12 pin tests pass. Locked Python3.13/uv0.12.10 CPU tools pass unchanged Ruff/Basedpyright/Vulture, resolving only the local missing-tools lint gap.

All444 candidate sources and native outputs agree with the cleaned local candidate; the full native report differs only in cc. Earlier proof construction, native parsing and discarded transport identity errors remain recorded. The saved parser-rejection variant remains unchanged and is not substituted for the qualified #881 parent.

Only evidence, index and inventory are appended after checking. Self-review only. No production function, earlier law, compiler input, permanent workflow, routine perft, search, model/GPU, training or benchmark change. No additional Python responsibility moves into Bend; export/references/data/control/training and transitional C++/LibTorch/AOTI remain dependencies. No merge,force push,deployment or live-process action.

Next: invariant-preserving removal and moves, plus separately scoped exact placement and full FEN/metadata semantics. Legal reachability, king safety and generation completeness remain distinct. Pinned checker/Base, native lowering/storage, ABI, toolchain, OS and hardware are trust boundaries; prior snapshot-lowering and closed-builder limitations remain unresolved.
