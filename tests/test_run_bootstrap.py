from __future__ import annotations

# Bootstrap checkpoint loading tests were removed when _run_single was deleted.
# Bootstrap logic now lives in chess_anti_engine/tune/trainable.py:train_trial()
# and is exercised through the tune harness with scheduler="none".
