name: Bend benchmark development snapshot
on:
  push:
    branches: [feat/bend-perft-benchmark]
    paths: ['.github/workflows/bend-bench-dev.yml']
permissions:
  contents: read
jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: |
          mkdir -p /tmp/bend-bench-source
          git archive --format=tar.gz HEAD native chess_anti_engine tests scripts docs .agents AGENTS.md CLAUDE.md .github pyproject.toml > /tmp/bend-bench-source/deepfin.tgz
          git rev-parse HEAD > /tmp/bend-bench-source/revision.txt
      - uses: actions/upload-artifact@v4
        with:
          name: bend-bench-source
          path: /tmp/bend-bench-source/
          retention-days: 2
