diff --git a/native/bend_engine/legal_probe/bench_clock.c b/native/bend_engine/legal_probe/bench_clock.c
index 9a69c20..dbdc516 100644
--- a/native/bend_engine/legal_probe/bench_clock.c
+++ b/native/bend_engine/legal_probe/bench_clock.c
@@ -7,7 +7,8 @@ Term bench_start_run(Env e, Term *f, IoWork *w) {
 }
 Term bench_finish_run(Env e, Term *f, IoWork *w) {
     (void)w;
-    if (cid_arity(CID_BENCH_FINISH) != 4) err_fail("benchmark timer ABI changed");
+    /* Four arguments plus the IO continuation occupy five fields. */
+    if (cid_arity(CID_BENCH_FINISH) != 5) err_fail("benchmark timer ABI changed");
     perft_clock_finish("bend-pext", (unsigned)f[1],
         ((uint64_t)(uint32_t)f[2] << 32) | (uint32_t)f[3]);
     /* Keep the last table owner alive until AFTER the measurement. */
diff --git a/native/bend_engine/legal_probe/benchmark.py b/native/bend_engine/legal_probe/benchmark.py
index 5db9339..ec2cfb7 100644
--- a/native/bend_engine/legal_probe/benchmark.py
+++ b/native/bend_engine/legal_probe/benchmark.py
@@ -127,9 +127,9 @@ def benchmark(source: Path, bun: str, cc: str, mode: str, cases: list[str],
                 raw: list[dict[str, object]] = []
                 names = list(binaries)
                 for trial in range(samples):
-                    # Rotate the first engine and reverse on odd rounds.
+                    # Balance each complete rotation cycle; reverse alternate cycles.
                     order = names[trial % len(names):] + names[:trial % len(names)]
-                    if trial % 2:
+                    if (trial // len(names)) % 2:
                         order = order[::-1]
                     for name in order:
                         args = [str(binaries[name])] + ([] if name == "cboard" else ["--threads", "1"])
