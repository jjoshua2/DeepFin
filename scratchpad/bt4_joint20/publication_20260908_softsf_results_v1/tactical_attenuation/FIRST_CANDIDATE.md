# First tactical policy candidate selected for implementation qualification

Use the 100 cp onset, 100 cp decay scale and 0.1 relative attenuation floor from
the descriptive screen. This is a deliberate substantive first contrast, not an
estimated optimum: the 200 cp alternative changed only 2.1% of top choices while
100 cp changed 3.9%. The intervention retains the BT4 confidence ratios among
acceptable moves. Do not fit extra parameters before testing this family.

Keep the B100 temperature-0.5 policy as the starting distribution. Use the
categorical mate treatment specified in MATE_FOLLOWUP.md rather than whole-row
exclusion. Preserve the SF value and every non-policy target. This is distinct
from the separately selected B100V50 value arm and from a joint correction.

Implementation must consume full legal-move raw d9 scores joined to the original
18.91M source rows. The existing truncated rank sidecar alone is insufficient for
an all-move filter. Reuse the qualified raw-source reconstruction and exact
history/row joins from the SF rewrite tools, together with the B100 policy source
identity. Score bounds, source-phase semantics and mate encoding need checking
on that actual path before accepting the corpus.

Before training, verify normalized finite stored targets, illegal zeros,
float16 support losses and ratio perturbations, exact non-policy byte preservation,
source completeness and actual launcher consumption. This note selects a recipe
for qualification; it is not a runnable training preregistration or a claim that
these full-source checks have passed.

The deciding comparison will use unchanged B100 with the original one-epoch
schedule if compatible, otherwise a newly matched control. Preserve the established
100-simulation ordered development screen plus a protected 400-simulation probe;
freeze exact openings, runtime, checkpoint lineage and resource caps in a launch
manifest. A negative shallow result alone must not cancel the protected probe.

## Joint policy/value follow-up

Consider the BT4-policy-weighted mass on large SF deficits together with disagreement
between the two root WDL distributions. Large move deficits and large root value
disagreement are separate observations; neither alone proves a tactical error.
The gate can increase SF's value share and apply the policy attenuation together,
but first keep the simple policy-only and fixed-value controls to identify the
benefit or harm of each component. Do not invent move-specific BT4 value labels
from a single root evaluation.

Deeper-SF fitting remains conditional on a joined deeper-score bank with
source-qualified game separation. Evaluate a small prespecified function family,
report held-out SF agreement as a proxy, and judge playing strength in a fresh
paired arena. The original d9-only bank cannot supply depth-stability evidence.
