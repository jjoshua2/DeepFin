# Fixed training-bank target geometry

Prepared before results. Reuse exactly the qualified 128 rows / 43 source-qualified games, seed20260906, sampled from the first frozen 8192-row SF shard. No new rows, inference, model evaluation, corpus materialization or payload scan. This is a seen-training mechanism diagnostic; it is neither representative population evidence nor a strength test.

Reconstruct the exact C set using original raw MultiPV order (`rank_observation`) and current `_source_candidate_set`: all stored SF maxima union the top-three d9 ranks with gap <=20 effective cp. Report selected size (1/2/3/>3), stored maximum count, selected-set enlargement, selected SF mass, raw-score maxima count, cp spread among stored maxima and selected moves, and finite-q spread. Report global q-flat (`spread<1e-9`) and exact-flat separately, not the historical incomplete recovery-skip counter. Attribute broad maxima only descriptively: distinguish different raw cp tied after the q map from additional ties after softmax/storage; do not assert causal attribution from rounded equality alone.

SF evaluation strata use phase-0 d9 rank-one effective cp from the mover's view: <-200, inclusive [-200,200], >200. Rows with any legal move in the existing mate band (abs(cp)>32000) form a separate mate-present stratum; also record whether rank one is in that band. These are policy-observation strata, not a replacement for stored value labels. Record strata intersections with exact C-size/maxima/q-flat categories, including empty/small strata.

Compare actual stored SF and C with banked BT4 raw and BT4 T0.5. For raw-effective-cp temperatures10,20,40,80, use max-shifted softmax over all legal scores, preserving the existing effective-cp mate mapping and mover sign. Show both ideal float64 and float32->float16 stored then legal-renormalized distributions. Report entropy, top-one mass/concentration, non-top-one tail mass, mass outside C, support, and stored mass error per row and by fixed strata. Top-one mass means maximum probability; raw cp maxima membership uses equality, not an arbitrary single tied move.

Closest mean entropy to actual C and SF is descriptive only (smaller temperature breaks exact numerical ties). No final temperature or gameplay selection, no confidence intervals or optimization claim on 128 correlated rows. Preserve per-row identities and metrics. Reuse current source functions via AST extraction to avoid importing training/inference modules; pin complete defining files and mapping dependencies before/after. Run only after parent's simple semantic check, at most120+10seconds, hidden GPU, two CPUs, nice19/ionice3. Outputs exclusive; failures preserved without retry.
