# Worker01 common-input preparation with bounded adapter/rank overlap

Prospective registration before processing: exactly w01-00000 through w01-00127 from each original run06_g10 and run07_g10_companion4 source. The frozen metadata selection contains2,126,537 raw rows across256 closed, BT4-labelled shards: run06 contributes1,063,526 rows/5,407 games; run07 contributes1,063,011 rows/5,441 games. Original source/config identities and closed receipt snapshots remain at G10_common_worker01_readiness_v1. Its source-qualified game check reports no overlap with the prior registered worker00 selection and no within-selection shard-game overlap. No new raw census or teacher inference is authorized by this preparation.

Preserve policy/value semantics: uniform-d9 policy from complete phase-zero d9, temperature0.0005/floor0; separately latest-phase d9 search value; seed0,8,192 output rows/shard and compact full-history row provenance. Actual eligible rows are UNKNOWN. Allow at most64 evidenced phase-zero support exclusions per source and2% missing-result rows (run06 ceiling21,270; run07 ceiling21,260). No envelope or additional malformed-row omissions. Existing independent rank eligibility/injectivity, source-qualified physical/history refs and exact population cardinality must prove the survivor complement. No counter-only qualification and no silent label-semantic change.

Use merged PR558 runtime f1b0f50b47465a5d3983d789217f6000df6b9878 and the existing qualified Python3.13/TorchCPU environment. Explicit overlap_adapt_rank:true runs adapter and rank after derivation/snapshot/adapter-manifest creation. They share each existing lane group, deadline and two-core affinity; qualification waits for both. Two source lanes run concurrently on2,3 and4,5; reserve0,1 for the pinned B100 post-training schedule verifier and6,7 for separate CPU match preparation, each deriveworkers2 and numeric/compressionthreads2. Use nice19, idle I/O and hidden GPU. No native rebuild, policy mixing, training or automatic retry.

The entire invocation, including preflight and cleanup, is bounded by14,400 seconds: outer GNU timeout TERM14,370 plus30 seconds to KILL, with one absolute deadline computed before launch and inherited by both lane timeouts. State or parent STOP,150GiB free-space reserve, stage failure or deadline cancels owned work only. The8GiB aggregate new output/cache cap is sampled every60seconds plus boundaries; it is NOT a RAM cap or instantaneous quota. Adapter/rank private index caches are64MiB each per lane. Preserve all partials and failure receipts; no adoption of prior outputs.

Accept only if both final common-input qualifiers pass, all selected input storage remains unchanged, survivor/exclusion counts close independently, and stable final output sampling stays within limits. This is common data preparation, not a target recipe, training schedule or strength result. Record actual stage wall/CPU/RSS, inherited resources, mode, omitted/surviving rows and sampled disk usage. Historical row-linear output estimate is about2.25GB; historical summed adapter/rank peakRSS about2GiB/lane. Neither is a bound. A historical ideal25% critical-path saving assumes no contention and is not a measured speedup. This new worker/source slice is a substantive incremental batch, not a controlled timing repeat. Parent refreshes shared-host resources before launch and records interference; no worker/group or B100 trainer change is included.
