# B100 materialization preparation

Prepared only. The parent chooses when H20 arena I/O leaves sufficient room. Run the exact command in `command.txt` once; this launches no audit, inference, training, follow-up, retry or watcher.

The frozen 44aafa625 mixer runs global alpha=1 and BT4 temperature=0.5 over original SF/history data. It retains every SF value/nonpolicy column. The completed 4000-position B100 audit is pinned; it is a descriptive bank, not full-corpus admission. Ordinary mixer source/history/target checks still run.

The supervisor is the reviewed H20 recovery pattern adapted to absent final and partial outputs. It rechecks pins after taking the same preparation lock, passes the lock into an owned GNU-timeout process group, records exclusive status/logs, and kills only that group during cleanup. The 14370-second timeout plus 30-second kill grace bounds bulk child lifetime to four hours. SIGKILL of the coordinator leaves the inherited lease and timeout alive, but ceases coordinator STOP/disk sampling; its receipt remains incomplete for reconciliation.

Both the coordinator command and mixer are nice19/ionice3/CPU0,1. Numeric and compression threads are two, CUDA is hidden, and the exact previously qualified private bootstrap remains pinned. Free-space and STOP checks run every two seconds, except the bounded 20-second output-allocation sample every five minutes. The 32 GiB allocation threshold is provisional and sampled, with possible overshoot; it is not a filesystem quota. The sample reads new output metadata only. Source and partial data are never removed by the supervisor, including final-publication validation failure.

A successful process must publish both summaries with 18,910,484 rows, 2309 shards, global alpha1 and temperature0.5. Summary identities and final allocation are recorded. This is materialization completion, not prospective schedule qualification or permission to train.

Five tiny real subprocess/filesystem checks pass in `supervisor_qualification.json`. Publication metadata and free-space observations were substituted in orchestration fixtures; actual operational qualification was not rerun. `supervisor.patch` shows the exact change from the accepted recovery supervisor. Runtime/native evidence is reused, while all execution pins are rehashed by the supervisor before spawning. The full preparation receipt distinguishes these checks.
